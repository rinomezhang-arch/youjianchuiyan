# P0修复任务 — 交给地龙

> 天龙整理 | 2026-07-31

## 修复1：StaffController 双路径支持

**文件**: `banquet_project/src/main/java/com/youjian/banquet/controller/StaffController.java`

**修改**: 第21行
```java
// 原来：
@RequestMapping("/api/hr")
// 改成：
@RequestMapping({"/api/hr", "/menu-api/hr"})
```

**原因**: 前端API请求走 `/menu-api/hr/staff`，但后端只认 `/api/hr/staff`，导致404。

## 修复2：前端staff API路径对齐

**文件**: `frontend_v3/src/api/booking.js`

**修改**: 第104行
```javascript
// 原来：
return request({ url: '/menu-api/staff', method: 'get', params })
// 改成：
return request({ url: '/menu-api/hr/staff', method: 'get', params })
```

**原因**: 对应修复1的后端路径变化。

## 提交规范

```bash
git checkout -b feat-dl-p0-fixes
git add banquet_project/src/main/java/com/youjian/banquet/controller/StaffController.java frontend_v3/src/api/booking.js
git commit -m "【地龙】P0修复-StaffController双路径+前端API对齐"
git push origin feat-dl-p0-fixes
```

然后到GitHub提PR给秋哥审核合并到main。

## 其他已知P0（优先级从高到低）

1. **N4**: 密码硬编码 — `banquet_project/src/main/java/com/youjian/banquet/aop/AuditLogAspect.java:56` 和 `StoreDataScopeAspect.java:44` 的jwt.secret默认值含Wo002323，应改为空字符串走环境变量
2. **N5**: 5张表无store_id隔离 — categories、dishes、dish_tag、dish_usage、packages 表缺少store_id字段
3. **N7**: 财务流程断裂 — ApprovalService.java:393-398 硬编码回退，缺少FinanceController
4. **N8**: 盘点流程断裂 — stock_take相关代码完全缺失

详见 `SYSTEM_AUDIT_REPORT_V3.md`。
