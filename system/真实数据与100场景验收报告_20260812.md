# 真实数据与 100 场景验收报告

- 执行日期：2026-08-12
- 验收提交：`85d8c9f`
- 环境：Java 17、Spring Boot 3.2.5、MariaDB 10.5（MySQL 兼容隔离实例）、Vue 3/Vite 5
- 原则：不伪造业务源数据、不将 Mock/演示兜底计为通过、不掩盖真实失败

## 1. 已完成项

1. 后端 Maven 测试通过。
2. 生产发布门禁通过：`PRODUCTION GATE: PASSED (25 migrations scanned)`。
3. 前端生产构建通过；补齐了代码已引用但清单缺失的 `dayjs` 与 `@element-plus/icons-vue` 依赖。
4. 规范库结构成功加载，共 127 张表。
5. 修复 8 处 Spring Data 派生查询/参数类型与实体字段不一致问题，应用可完成 Spring 容器初始化。
6. 修复提交已同步到 `governance/full-system`，父仓库 gitlink 更新为 `30c513c`。

## 2. 真实数据灌入结论

**结论：未通过，未伪造通过。**

当前规范库核心数据计数：

| 表 | 记录数 |
|---|---:|
| store_info | 2 |
| staff_master | 0 |
| customer_master | 0 |
| supplier_master | 0 |
| ingredient_master | 0 |
| dish_master | 0 |
| dish_recipe | 0 |
| dish_cost_card | 0 |
| purchase_order | 0 |
| purchase_receipt | 0 |

阻断证据：

- 仓库种子 `seed_e2e_test_data.sql` 在 `customer_master.status` 处失败，规范表不存在该字段。
- 需求链路指定的 `ingredient_cost_price` 表在当前 127 表规范结构中不存在。
- `scripts/seed/banquet_full_seed.sql` 含旧版字段契约，与当前规范结构不一致。
- 未发现用户提供的供应商/食材/菜品/BOM/采购收货权威源文件；因此没有自行编造业务数据。

## 3. 100 场景结论

**结论：未执行为“通过”；前置条件失败，记为阻断。**

100 场景要求依赖供应商、食材、菜品、BOM、采购收货、成本价和成本卡真实数据。上述权威数据未成功灌入，若继续运行只会得到空库响应，不能形成有效业务验收。故本次不以重复 GET、Mock 数据或兜底响应凑足 100 个“通过”场景。

## 4. 已确认的真实缺陷

1. 数据库初始化脚本、JPA 实体和种子脚本存在版本漂移。
2. 记忆文件宣称 126 表，当前规范转储实际为 127 表。
3. 需求指定 `ingredient_cost_price`，当前数据库没有该表，成本价只散落在 `ingredient_master.avg_price/unit_price`、`dish_recipe.unit_price/net_unit_price` 与采购明细中。
4. 前端 `package.json` 原先漏列实际引用依赖，干净环境无法构建。

## 5. 验收判定

- 代码编译与生产门禁：通过
- 前端生产构建：通过
- 数据结构加载：通过
- 真实业务数据灌入：阻断
- 采购→成本价→菜品成本卡链路：阻断
- 100 场景：阻断
- 总体：**不满足“完整真实数据 + 100 场景全部通过”的上线条件**

## 6. 解除阻断所需输入

必须提供或确认至少一种权威数据源：

1. 最新生产脱敏 SQL 备份；或
2. 供应商、食材、菜品、BOM、采购单、收货单 Excel/CSV；或
3. 明确授权将仓库旧种子迁移为当前 127 表结构的验收基准数据。

获得权威输入后，应先建立版本化迁移，再执行逐表计数、外键孤儿、金额恒等式、加权成本与菜品成本卡断言，最后运行 100 场景。
