# 又见炊烟 iPad 点餐分系统 - 三人团队分工（修订版）

> 修订时间：2026-07-25 16:17
> 修订人：天龙（秋哥授权）
> 修订原因：原分工不合理，重新按实际能力划分

---

## 一、团队能力说明

| 角色 | 位置 | 能力 |
|------|------|------|
| 🦞 **天龙** | 腾讯云 Ubuntu 服务器 | 后端、数据库、部署、运维、全栈 |
| 🐉 **地龙** | 本地 Windows | 前端、后端、部署、全栈 |
| 🌟 **SOLO** | 本地 TRAE IDE | 全栈、测试、对接、协调、部署 |

**核心事实**：三个都能写代码、都能部署、都能进服务器。

---

## 二、分工原则

1. **不打架**：主责明确，不重复劳动
2. **按效率**：谁做最快谁负责，不是谁不能做
3. **可补位**：主责忙不过来，其他人能顶上

---

## 三、主责分工

### 🦞 天龙（服务器）— 后端负责人

**主责**：
- 后端 37 个 API 接口开发（6 大模块）
- 数据库设计与维护（新增 ipad_device_info 表、config 初始化）
- WebSocket 服务搭建（8081 端口）
- 权限拦截器、请求头校验
- 接口文档输出

**默认负责**：
- 部署上线（因为最快，不是别人不能做）
- Nginx 配置
- 数据库脚本执行

**不碰**：
- 前端页面开发

---

### 🐉 地龙（本地）— 前端负责人

**主责**：
- 前端 78 个窗体开发（50 主页面 + 28 弹窗）
- UI/交互设计（轻奢主题、毛玻璃效果）
- iPad 平板适配（11 寸 / 12.9 寸）
- 前端路由、组件封装
- 请求拦截器、全局 Header 注入

**可负责**：
- 前端部署（build + 上传 dist）
- 联调测试

**不碰**：
- 后端接口代码

---

### 🌟 SOLO（本地）— 测试 + 对接 + 协调

**主责**：
- 全量测试（功能、异常、兼容性、性能）
- 打印服务对接（9100 端口）
- AI 对接（OpenClaw 27860 端口）
- 项目管理（进度跟踪、站会、文档）
- 代码评审（前后端交叉检查）

**可负责**：
- 部署（需要时顶上）
- 紧急 Bug 修复（前后端都能改）

**不抢**：
- 不写常规业务代码（除非补位）

---

## 四、协作机制

### 每日站会
- 时间：早 9:00，10 分钟
- 内容：昨日完成、今日计划、阻塞问题
- 主持：SOLO

### 代码提交
- 天龙：后端代码提交到 `tianlong/backend` 分支
- 地龙：前端代码提交到 `dilong/frontend` 分支
- SOLO：测试用例、文档提交到 `solo/docs` 分支
- 合并：SOLO 负责合并到 `dev-ipad`，上线前合并到 `master`

### 联调流程
1. 天龙完成接口 → 通知地龙联调
2. 地龙完成页面 → 通知 SOLO 测试
3. SOLO 发现 Bug → 记录台账，指派天龙/地龙修复
4. 修复完成 → SOLO 回归测试

### 文档输出
| 文档 | 责任人 |
|------|--------|
| 接口文档（含字段映射） | 天龙 |
| 前端组件库说明 | 地龙 |
| 测试报告 + Bug 台账 | SOLO |
| 部署运维手册 | 天龙（主）+ SOLO（辅） |
| 项目进度台账 | SOLO |

---

## 五、补位规则

| 场景 | 补位人 |
|------|--------|
| 天龙忙不过来 | 地龙/SOLO 可帮写后端 |
| 地龙忙不过来 | SOLO 可帮写前端 |
| 部署紧急 | 谁有空谁上 |
| 测试忙不过来 | 天龙/地龙可帮测 |

**原则**：主责不变，补位是临时支援。

---

## 六、工期要求

**秋哥指示**：不要用 30 天，要快速。

**实际工期**：按实际进度，能多快就多快，不卡天数。

**阶段划分**（不按天卡，按完成度）：
1. 底座搭建：接口 + 路由 + 基础组件
2. 点餐核心：桌台 + 菜品 + 订单全流程
3. 结算会员：支付 + 会员 + 营销
4. 联调上线：测试 + 修复 + 部署

---

*修订完成，通知地龙、SOLO 确认。*
