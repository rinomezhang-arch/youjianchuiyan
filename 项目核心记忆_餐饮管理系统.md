# 又见炊烟餐饮管理系统 — 核心记忆文件 (Memory Hub)

> **⚠️ 记忆路由规则 (Memory Routing Rules)**:
> 1. **唯一入口**: 提及本项目时，优先读取此文件获取全局索引。
> 2. **强制同步**: **每次发生代码变更、数据变更、需求变更、环境变更后，必须同步更新此文件中的相关章节。**
> 3. **分层存储**: 凭证（Pointer Only）、硬约束、路由索引在此处；详细设计文档保持独立（指针引用）。
> 4. **切片索引**: 所有信息均通过带锚点的切片组织，支持快速跳转。

---

## 1. 项目全局索引 (Global Index)

- **[1. 项目概述与技术栈](#1-项目概述与技术栈)**
- **[2. 凭证与密钥 (Pointer Only)](#2-凭证与密钥pointer-only)**
- **[3. 核心硬约束 (Hard Constraints)](#3-核心硬约束hard-constraints)**
- **[4. UI/UX 设计偏好 (UI Preferences)](#4-uiux-设计偏好ui-preferences)**
- **[5. 工程规范 (Engineering Conventions)](#5-工程规范engineering-conventions)**
- **[6. 项目结构与路由 (Structure & Routing)](#6-项目结构与路由structure--routing)**
- **[7. 数据库核心索引 (DB Index)](#7-数据库核心索引db-index)**
- **[8. 脚本与自动化 (Scripts & Automation)](#8-脚本与自动化scripts--automation)**
- **[9. 审计与报告 (Audits & Reports)](#9-审计与报告audits--reports)**
- **[10. 开发规划 (Dev Roadmap)](#10-开发规划dev-roadmap)**

---

## 2. 凭证与密钥 (Pointer Only)

> **严禁将任何密钥/Token 明文写入代码库或本文件！**

| 类型 | 存储位置 / 形式 | 读取方式 |
| :--- | :--- | :--- |
| **腾讯云 COS** | `C:\Users\rinom\.cos.conf` | `scripts/backup/cos_upload.py` 自动解析 INI |
| **JWT 密钥** | 环境变量 `JWT_SECRET` | `application.yml` 中 `${JWT_SECRET}` |
| **AES-256 密钥** | 环境变量 `AES_SECRET_KEY` | `application.yml` 中 `${AES_SECRET_KEY}` |
| **天龙 Token** | 环境变量 `TIANLONG_TOKEN` | `application-prod.yml` 中 `${TIANLONG_TOKEN:}` |
| **MySQL 密码** | 开发环境硬编码 `application.yml`；生产环境变量 | - |
| **地龙(OpenClaw)** | `C:\Users\rinom\.openclaw\openclaw.json` | 含 API Key，由守护进程读取 |
| **API 密码本** | `C:\Users\rinom\.openclaw\密码本.md` | 记录所有外部 API Key |

**COS 路径约定**: `ai公共工作空间/项目管理/餐饮管理系统/`

---

## 3. 核心硬约束 (Hard Constraints)

> 这些是必须严格遵守的业务与技术规则。

### 3.1 数据库与架构
- **位置**: `f:\solo\project\又见炊烟餐饮管理系统2.0\youjian-docker\mysql\init\`
- **隔离**: 多租户隔离通过 `store_id` 字段实现（宁国店=1，宣城店=2）。AOP 切面自动注入过滤。
- **字段命名**: 前后端字段必须统一（snake_case vs camelCase 需明确约定并保持一致）。
- **数据完整性**: 禁止孤儿数据，保持级联关系完整。
- **时间戳**: 所有实体的 `created_at` / `updated_at` 必须通过 `@PrePersist` / `@PreUpdate` 自动填充。

### 3.2 安全与权限
- **密钥管理**: 生产密钥 **100%** 走环境变量，禁止硬编码默认值。
- **数据脱敏**: 银行账号、身份证号等敏感字段必须使用 AES-256-GCM 加密存储。
- **API 限流**: 防止接口滥用，配置 Rate Limiting。
- **权限对齐**: RBAC 权限 URL 必须与 Controller 路径严格一致。

### 3.3 部署与运维
- **云端优先**: 所有部署必须在腾讯云完成。
- **调试先行**: 必须在本地完成调试后，才能推送到 GitHub。
- **变更同步**: 代码变更必须同步更新本记忆文件和相关文档。
- **备份策略**: 每次重大变更必须打时间戳备份到 COS。

---

## 4. UI/UX 设计偏好 (UI Preferences)

> 客户对界面的具体要求，前端开发必须遵守。

- **布局**: 宽页面横向排列，多行对称。弹窗宽度 **50vw**。
- **视觉**: 徽派雅致配色，拒绝大红大紫。选中元素金边高亮，不使用圆角或仅使用 16px 圆角。
- **交互**: 
    - 输入框高度 32px，窄宽度。
    - 数字控件：减号(-)在左，加号(+)在右。
    - 创建时间必须单行显示。
    - 禁用图标（Emoji/SVG），使用纯文字标签。
- **响应式**: 必须移动端友好（Mobile-friendly），使用原生选择器和底部抽屉。

---

## 5. 工程规范 (Engineering Conventions)

- **技术栈**:
    - 前端: Vue 3.4 + Vite 5 + Element Plus + Pinia
    - 后端: Spring Boot 3.2.5 + Java 17 + Spring Data JPA
    - 数据库: MySQL 8.0
- **Git 工作流**:
    - **身份**: 天龙(TL-BOT/文档)、地龙(DL-BOT/后端)、Trae(TRAE-BOT/前端)、人工。
    - **分支**: AI 只能在专属前缀分支开发 (`feat-tl-*`, `feat-dl-*`, `feat-trae-*`)，禁止直接 push master/main。
    - **Commit**: 提交信息必须使用 `【工具名】模块-操作：内容` 格式。
    - **职责隔离**: 天龙禁改代码；地龙禁改前端；Trae禁改后端。

---

## 6. 项目结构与路由 (Structure & Routing)

### 核心目录
```text
f:\solo\project\又见炊烟餐饮管理系统2.0\
├── PROJECT_MEMORY.md (本文件)
├── 数据库设计使用说明书.md (详细设计文档)
├── banquet_project/ (后端 Spring Boot)
├── frontend_v3/ (前端 Vue 3)
├── youjian-docker/ (Docker 部署配置)
├── scripts/ (脚本库)
│   ├── migrations/ (数据库迁移)
│   ├── seed/ (种子数据)
│   └── backup/ (备份工具)
└── 体检/ (审计报告目录)
```

### API 路由前缀索引 (节选)
| 模块 | Controller | 前缀 |
| :--- | :--- | :--- |
| 认证 | AuthController | `/api/auth` |
| 预订 | BookingController | `/api/booking` |
| 菜品 | DishController | `/api/dish` |
| 财务 | FinanceController | `/api/finance` |
| 人事 | HRController | `/api/hr` |
| 采购 | PurchaseController | `/api/purchase` |
| iPad | IpadOrderController | `/ipad/order` |

---

## 7. 数据库核心索引 (DB Index)

> 详细表结构见 `数据库设计使用说明书.md`。

- **总表数**: 126 张（原115 + 补建11张缺失表, 已在MySQL实例执行）
- **核心表分类**:
    - **系统管理**: sys_role, sys_permission, sys_user 等
    - **核心业务**: booking_master, dish_master, customer_master 等
    - **财务人事**: finance_account, month_salary, attendance 等
    - **采购仓储**: ingredient_master, purchase_request, inventory_summary 等
- **多租户隔离**: 所有业务表均包含 `store_id` 字段。

---

## 8. 脚本与自动化 (Scripts & Automation)

| 脚本路径 | 功能 | 状态 |
| :--- | :--- | :--- |
| `scripts/backup/cos_upload.py` | 通用 COS XML API 上传 | ✅ 可用 |
| `scripts/migrations/db_fix_v1.sql` | 数据库核心结构修复 | ✅ 可用 |
| `scripts/migrations/create_missing_tables_v1.sql` | 补建11张缺失表(带外键/级联/注释) | ✅ 可用 |
| `体检/system_checkup_v3.py` | 智能体检脚本v3(自动扫描DB+实体+对齐+健康) | ✅ 可用 |
| `youjian-docker/mysql/init/banquet_init.sql` | 数据库初始化 (126表) | ✅ 可用 |

**COS 备份脚本使用**:
```bash
python scripts/backup/cos_upload.py <local_file> <cos_key>
```

---

## 9. 审计与报告 (Audits & Reports)

- **位置**: `体检/体检报告/`
- **关键报告**:
    - `SYSTEM_AUDIT_REPORT_V4.md`: 初始审计
    - `DEEP_AUDIT_REPORT_V7.md`: 深度审计
    - `ISOLATION_RBAC_CASCADE_V10.md`: 隔离与权限审计
    - `HIDDEN_RISKS_RESOLVED_V11.md`: 隐患消除报告

---

## 10. 开发规划 (Dev Roadmap)

### Phase 1: 后端与数据库对齐 (进行中)
- [x] **表名对齐**: 7个实体类 @Table 名称已修改对齐数据库（CostCard→dish_cost_card, PurchaseRequest→procurement_request, MaterialRequisition→requisition_order, GoodsReceipt→purchase_receipt 等）
- [x] **字段对齐**: 7个实体类字段已完全重写对齐数据库列定义
- [x] **补建缺失表**: 11张完全缺失的表已生成建表SQL并追加到 banquet_init.sql（带外键/级联/注释/store_id隔离）
- [x] **编译验证**: mvn compile 通过
- [x] **建表执行**: 11张缺失表已在MySQL实例中创建, 数据库表数 115→126
- [x] **智能体检**: system_checkup_v3.py 运行通过, FATAL=0 (无缺表)
  - 最新体检: 396项发现, FATAL=0, ERROR=76, WARNING=148, INFO=172
  - 报告位置: 体检/system_checkup_v3_latest.html
- [ ] **运行时验证**: 启动应用测试（待用户回来后执行）
- [ ] **数据流测试**: 验证各模块CRUD数据流打通
- [ ] **分店隔离验证**: 验证 store_id 多租户隔离

### Phase 2: 接口联调
- [ ] **Controller 测试**: 逐个测试 REST API。
- [ ] **前端对接**: 解决字段命名不匹配问题。
- [ ] **权限验证**: 验证 RBAC 拦截是否生效。

---

*最后更新时间: 2026-08-01 (后端数据库对齐: 补建11表+7实体类重写+编译通过)*
*当前负责人: Trae*
