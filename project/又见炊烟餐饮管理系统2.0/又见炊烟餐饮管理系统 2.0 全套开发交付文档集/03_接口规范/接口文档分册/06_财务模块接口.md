# 06 财务模块接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 财务模块 |
| Controller 数量 | 6 |
| 覆盖 Controller | FinanceController / FinanceAccountController / FinanceVoucherController / FinanceReportController / FinanceExpenseController / FinancePayableController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离 |
| Authorization | 是 | `Bearer <JWT>` |
| Content-Type | 否 | POST/PUT 为 application/json |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 角色无财务权限 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 主键查询为空 |
| 4003 | 状态不允许操作 | 凭证已审核不可改 |
| 5000 | 业务异常 | BusinessException（借贷不平） |
| 9000 | 系统异常 | 未捕获异常 |

---

## 6.1 FinanceController 财务基础

- **类映射前缀**：`/finance`
- **完整路径示例**：`/api/finance/today`
- **说明**：财务总览、资金余额、月度趋势、待处理单据、资金流水。

### 6.1.1 今日财务

- **方法**：GET
- **路径**：`/api/finance/today`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 否 | 门店 ID，不传则汇总 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "income": 12800.00,
    "expense": 4300.00,
    "netProfit": 8500.00,
    "orderCount": 36,
    "avgOrder": 355.56
  }
}
```

- **错误码**：0 / 2001

### 6.1.2 资金余额

- **方法**：GET
- **路径**：`/api/finance/balance`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 否 | 门店 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "cashBalance": 56000.00,
    "bankBalance": 120000.00,
    "wechatBalance": 8800.00,
    "alipayBalance": 6200.00,
    "totalBalance": 191000.00
  }
}
```

- **错误码**：0 / 2001

### 6.1.3 月度趋势

- **方法**：GET
- **路径**：`/api/finance/monthly-trend`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 否 | 门店 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "month": "2026-05", "income": 320000.00, "expense": 210000.00 },
    { "month": "2026-06", "income": 350000.00, "expense": 220000.00 },
    { "month": "2026-07", "income": 380000.00, "expense": 230000.00 }
  ]
}
```

- **错误码**：0 / 2001

### 6.1.4 待处理单据

- **方法**：GET
- **路径**：`/api/finance/pending-docs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "docType": "expense", "docId": 12, "amount": 500.00, "applyDate": "2026-08-01" },
    { "docType": "payable", "docId": 8, "amount": 3200.00, "applyDate": "2026-07-30" }
  ]
}
```

- **错误码**：0 / 2001

### 6.1.5 资金流水列表

- **方法**：GET
- **路径**：`/api/finance/transaction`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 是 | 门店 ID |
| startDate | Date | 否 | 起始日期 |
| endDate | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "transactionId": 1,
      "accountId": 1,
      "accountName": "现金",
      "direction": "in",
      "amount": 380.00,
      "bizType": "order",
      "createTime": "2026-08-01 12:00:00"
    }
  ]
}
```

- **错误码**：0 / 2001

### 6.1.6 新增资金流水

- **方法**：POST
- **路径**：`/api/finance/transaction`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| accountId | Long | 是 | 账户 ID |
| direction | String | 是 | 方向：in / out |
| amount | BigDecimal | 是 | 金额 |
| bizType | String | 否 | 业务类型 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "transactionId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 6.1.7 删除资金流水

- **方法**：DELETE
- **路径**：`/api/finance/transaction/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 流水 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

---

## 6.2 FinanceAccountController 账户管理

- **类映射前缀**：`/financeAccount`
- **完整路径示例**：`/api/financeAccount/list`
- **说明**：资金账户（现金 / 银行 / 微信 / 支付宝）维护。

### 6.2.1 账户列表

- **方法**：GET
- **路径**：`/api/financeAccount/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| accountType | String | 否 | 账户类型 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 4,
    "page": 1,
    "size": 10,
    "records": [
      {
        "accountId": 1,
        "accountName": "现金",
        "accountType": "cash",
        "balance": 56000.00,
        "status": 1,
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 6.2.2 账户详情

- **方法**：GET
- **路径**：`/api/financeAccount/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 账户 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": { "accountId": 1, "accountName": "现金", "accountType": "cash", "balance": 56000.00, "status": 1 }
}
```

- **错误码**：0 / 4001 / 3002

### 6.2.3 新增账户

- **方法**：POST
- **路径**：`/api/financeAccount`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| accountName | String | 是 | 账户名称 |
| accountType | String | 是 | 类型：cash / bank / wechat / alipay |
| balance | BigDecimal | 否 | 期初余额 |
| status | Integer | 否 | 状态，默认 1 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "accountId": 5 } }
```

- **错误码**：0 / 1001 / 1003

### 6.2.4 修改账户

- **方法**：PUT
- **路径**：`/api/financeAccount/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 账户 ID（路径参数） |
| accountName | String | 否 | 账户名称 |
| status | Integer | 否 | 状态 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 3002

### 6.2.5 删除账户

- **方法**：DELETE
- **路径**：`/api/financeAccount/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 账户 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5002（账户存在流水）

---

## 6.3 FinanceVoucherController 凭证管理

- **类映射前缀**：`/financeVoucher`
- **完整路径示例**：`/api/financeVoucher/list`
- **说明**：会计凭证（借贷分录），需借贷平衡。

### 6.3.1 凭证列表

- **方法**：GET
- **路径**：`/api/financeVoucher/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| voucherDate | Date | 否 | 凭证日期 |
| status | String | 否 | 状态：草稿 / 已审核 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 50,
    "page": 1,
    "size": 10,
    "records": [
      {
        "voucherId": 1,
        "voucherNo": "PZ-2026-0001",
        "voucherDate": "2026-07-31",
        "summary": "采购食材",
        "debitAmount": 2000.00,
        "creditAmount": 2000.00,
        "status": "已审核"
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 6.3.2 凭证详情

- **方法**：GET
- **路径**：`/api/financeVoucher/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 凭证 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "voucherId": 1,
    "voucherNo": "PZ-2026-0001",
    "voucherDate": "2026-07-31",
    "summary": "采购食材",
    "status": "已审核",
    "entries": [
      { "entryId": 1, "subjectCode": "1401", "subjectName": "原材料", "direction": "debit", "amount": 2000.00 },
      { "entryId": 2, "subjectCode": "1001", "subjectName": "库存现金", "direction": "credit", "amount": 2000.00 }
    ]
  }
}
```

- **错误码**：0 / 4001 / 3002

### 6.3.3 新增凭证

- **方法**：POST
- **路径**：`/api/financeVoucher`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| voucherDate | Date | 是 | 凭证日期 |
| summary | String | 是 | 摘要 |
| entries | Array | 是 | 分录数组 |
| entries[].subjectCode | String | 是 | 科目编码 |
| entries[].direction | String | 是 | 借贷方向：debit / credit |
| entries[].amount | BigDecimal | 是 | 金额 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "voucherId": 2 } }
```

- **错误码**：0 / 1001 / 5000（借贷不平）

### 6.3.4 修改凭证

- **方法**：PUT
- **路径**：`/api/financeVoucher/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 凭证 ID（路径参数） |
| summary | String | 否 | 摘要 |
| entries | Array | 否 | 分录数组 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已审核不可改）/ 5000

### 6.3.5 审核凭证

- **方法**：POST
- **路径**：`/api/financeVoucher/{id}/audit`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 凭证 ID（路径参数） |
| passed | Boolean | 是 | true 通过 / false 反审 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 6.3.6 删除凭证

- **方法**：DELETE
- **路径**：`/api/financeVoucher/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 凭证 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已审核不可删）

---

## 6.4 FinanceReportController 财务报表

- **类映射前缀**：`/financeReport`
- **完整路径示例**：`/api/financeReport/profit-report`
- **说明**：利润表、资产负债表。

### 6.4.1 利润表

- **方法**：GET
- **路径**：`/api/financeReport/profit-report`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "revenue": 380000.00,
    "cost": 210000.00,
    "grossProfit": 170000.00,
    "expense": 60000.00,
    "netProfit": 110000.00,
    "grossMargin": 44.74,
    "netMargin": 28.95
  }
}
```

- **错误码**：0 / 1001 / 2001

### 6.4.2 资产负债表

- **方法**：GET
- **路径**：`/api/financeReport/balance-report`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| asOfDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "assets": 560000.00,
    "liabilities": 120000.00,
    "equity": 440000.00,
    "details": [
      { "subjectCode": "1001", "subjectName": "库存现金", "amount": 56000.00, "side": "asset" }
    ]
  }
}
```

- **错误码**：0 / 1001 / 2001

### 6.4.3 现金流量表

- **方法**：GET
- **路径**：`/api/financeReport/cash-flow`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "operatingFlow": 88000.00,
    "investingFlow": -20000.00,
    "financingFlow": 0.00,
    "netIncrease": 68000.00
  }
}
```

- **错误码**：0 / 1001 / 2001

---

## 6.5 FinanceExpenseController 费用管理

- **类映射前缀**：`/financeExpense`
- **完整路径示例**：`/api/financeExpense/list`
- **说明**：日常费用报销申请。

### 6.5.1 费用列表

- **方法**：GET
- **路径**：`/api/financeExpense/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待审 / 已通过 / 已驳回 |
| expenseType | String | 否 | 费用类型 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 12,
    "page": 1,
    "size": 10,
    "records": [
      {
        "expenseId": 1,
        "expenseNo": "FY-2026-0001",
        "expenseType": "水电费",
        "amount": 800.00,
        "applyDate": "2026-07-30",
        "applicant": "张三",
        "status": "已通过"
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 6.5.2 费用详情

- **方法**：GET
- **路径**：`/api/financeExpense/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 费用 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "expenseId": 1,
    "expenseNo": "FY-2026-0001",
    "expenseType": "水电费",
    "amount": 800.00,
    "applyDate": "2026-07-30",
    "applicant": "张三",
    "accountId": 1,
    "remark": "7 月水电",
    "status": "已通过"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 6.5.3 提交费用报销

- **方法**：POST
- **路径**：`/api/financeExpense`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| expenseType | String | 是 | 费用类型 |
| amount | BigDecimal | 是 | 金额 |
| applyDate | Date | 是 | 申请日期 |
| accountId | Long | 否 | 支付账户 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "expenseId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 6.5.4 修改费用

- **方法**：PUT
- **路径**：`/api/financeExpense/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 费用 ID（路径参数） |
| amount | BigDecimal | 否 | 金额 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

### 6.5.5 删除费用

- **方法**：DELETE
- **路径**：`/api/financeExpense/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 费用 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

---

## 6.6 FinancePayableController 应付款管理

- **类映射前缀**：`/financePayable`
- **完整路径示例**：`/api/financePayable/list`
- **说明**：供应商应付款登记与核销。

### 6.6.1 应付款列表

- **方法**：GET
- **路径**：`/api/financePayable/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待付 / 已付 / 部分付 |
| supplierId | Long | 否 | 供应商 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 8,
    "page": 1,
    "size": 10,
    "records": [
      {
        "payableId": 1,
        "payableNo": "YF-2026-0001",
        "supplierId": 301,
        "supplierName": "鲜达食材",
        "amount": 3200.00,
        "paidAmount": 0.00,
        "dueDate": "2026-08-15",
        "status": "待付"
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 6.6.2 应付款详情

- **方法**：GET
- **路径**：`/api/financePayable/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 应付款 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "payableId": 1,
    "payableNo": "YF-2026-0001",
    "supplierId": 301,
    "supplierName": "鲜达食材",
    "amount": 3200.00,
    "paidAmount": 0.00,
    "dueDate": "2026-08-15",
    "status": "待付"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 6.6.3 登记应付款

- **方法**：POST
- **路径**：`/api/financePayable`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| supplierId | Long | 是 | 供应商 ID |
| amount | BigDecimal | 是 | 金额 |
| dueDate | Date | 是 | 到期日期 |
| sourceType | String | 否 | 来源：采购 / 手工 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "payableId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 6.6.4 修改应付款

- **方法**：PUT
- **路径**：`/api/financePayable/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 应付款 ID（路径参数） |
| dueDate | Date | 否 | 到期日期 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

### 6.6.5 付款核销

- **方法**：POST
- **路径**：`/api/financePayable/{id}/pay`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 应付款 ID（路径参数） |
| payAmount | BigDecimal | 是 | 本次付款金额 |
| accountId | Long | 是 | 支付账户 ID |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "paidAmount": 3200.00, "status": "已付" } }
```

- **错误码**：0 / 1001 / 4003 / 5000

### 6.6.6 删除应付款

- **方法**：DELETE
- **路径**：`/api/financePayable/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 应付款 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已付不可删）

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| FinanceController | /finance/today | GET | 今日财务 |
| FinanceController | /finance/balance | GET | 资金余额 |
| FinanceController | /finance/monthly-trend | GET | 月度趋势 |
| FinanceController | /finance/pending-docs | GET | 待处理单据 |
| FinanceController | /finance/transaction | GET/POST | 资金流水 |
| FinanceVoucherController | /financeVoucher/{id}/audit | POST | 凭证审核 |
| FinanceReportController | /financeReport/profit-report | GET | 利润表 |
| FinanceReportController | /financeReport/balance-report | GET | 资产负债表 |
| FinanceReportController | /financeReport/cash-flow | GET | 现金流量表 |
| FinanceExpenseController | /financeExpense | POST | 提交费用报销 |
| FinancePayableController | /financePayable/{id}/pay | POST | 付款核销 |
