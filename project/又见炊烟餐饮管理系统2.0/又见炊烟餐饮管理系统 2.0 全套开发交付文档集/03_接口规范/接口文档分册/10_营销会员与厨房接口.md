# 10 营销会员与厨房接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 营销会员与厨房 |
| Controller 数量 | 5 |
| 覆盖 Controller | MarketingController / MarketingOverviewController / MemberController / KitchenController / ReportController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离；店长强制限本店，总经理可指定任意门店 |
| Authorization | 是 | `Bearer <JWT>` |
| Content-Type | 否 | POST/PUT 为 application/json |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 1003 | 重复数据 | 活动 / 优惠券名称重复 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 活动 / 优惠券创建仅总经理 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 主键查询为空 |
| 4003 | 状态不允许操作 | 活动已结束不可改 |
| 5000 | 业务异常 | BusinessException |
| 9000 | 系统异常 | 未捕获异常 |

---

## 10.1 MarketingController 营销管理

- **类映射前缀**：`/marketing`
- **完整路径示例**：`/api/marketing/activities`
- **说明**：营销活动、优惠券、折扣规则、分享、签到、任务。

### 10.1.1 活动列表

- **方法**：GET
- **路径**：`/api/marketing/activities`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：未开始 / 进行中 / 已结束 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 5,
    "page": 1,
    "size": 10,
    "records": [
      {
        "activityId": 1,
        "activityName": "七夕双人套餐",
        "startDate": "2026-08-01",
        "endDate": "2026-08-07",
        "discount": 0.88,
        "status": "进行中",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001 / 3002

### 10.1.2 新增活动

- **方法**：POST
- **路径**：`/api/marketing/activities`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| activityName | String | 是 | 活动名称 |
| startDate | Date | 是 | 开始日期 |
| endDate | Date | 是 | 结束日期 |
| discount | BigDecimal | 否 | 折扣 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "activityId": 2 } }
```

- **错误码**：0 / 1001 / 3001（仅总经理）/ 5000

### 10.1.3 修改活动

- **方法**：PUT
- **路径**：`/api/marketing/activities/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 活动 ID（路径参数） |
| activityName | String | 否 | 活动名称 |
| endDate | Date | 否 | 结束日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 10.1.4 删除活动

- **方法**：DELETE
- **路径**：`/api/marketing/activities/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 活动 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 10.1.5 优惠券列表

- **方法**：GET
- **路径**：`/api/marketing/coupons`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：生效 / 失效 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "couponId": 1, "couponName": "满 200 减 30", "faceValue": 30.00, "minSpend": 200.00, "status": "生效" }
  ]
}
```

- **错误码**：0 / 2001

### 10.1.6 新增优惠券

- **方法**：POST
- **路径**：`/api/marketing/coupons`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| couponName | String | 是 | 优惠券名称 |
| faceValue | BigDecimal | 是 | 面值 |
| minSpend | BigDecimal | 否 | 最低消费 |
| validDays | Integer | 否 | 有效天数 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "couponId": 2 } }
```

- **错误码**：0 / 1001 / 3001 / 5000

### 10.1.7 折扣规则列表

- **方法**：GET
- **路径**：`/api/marketing/discount-rules`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "ruleId": 1, "ruleName": "会员 9 折", "discount": 0.9, "scope": "member", "status": 1 }
  ]
}
```

- **错误码**：0 / 2001

### 10.1.8 分享记录

- **方法**：GET
- **路径**：`/api/marketing/shares`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "shareId": 1, "memberName": "赵六", "channel": "wechat", "shareDate": "2026-07-30", "rewardPoints": 10 }
  ]
}
```

- **错误码**：0 / 2001

### 10.1.9 签到列表

- **方法**：GET
- **路径**：`/api/marketing/signins`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "signinId": 1, "memberName": "赵六", "signinDate": "2026-08-01", "continuousDays": 7, "rewardPoints": 5 }
  ]
}
```

- **错误码**：0 / 2001

### 10.1.10 签到记录

- **方法**：GET
- **路径**：`/api/marketing/signin-records`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| memberId | Long | 否 | 会员 ID |
| startDate | Date | 否 | 起始日期 |
| endDate | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "recordId": 1, "memberName": "赵六", "signinDate": "2026-08-01", "rewardPoints": 5 }
  ]
}
```

- **错误码**：0 / 2001

### 10.1.11 任务列表

- **方法**：GET
- **路径**：`/api/marketing/tasks`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "taskId": 1, "taskName": "分享朋友圈", "rewardPoints": 20, "status": 1 }
  ]
}
```

- **错误码**：0 / 2001

---

## 10.2 MarketingOverviewController 营销概览

- **类映射前缀**：`/marketingOverview`
- **完整路径示例**：`/api/marketingOverview/overview`
- **说明**：营销数据总览，店长强制本店，总经理可指定门店。

### 10.2.1 营销概览

- **方法**：GET
- **路径**：`/api/marketingOverview/overview`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 否 | 门店 ID，店长强制覆盖为本店 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "activeActivities": 3,
    "couponsIssued": 1200,
    "couponsUsed": 380,
    "newMembers": 56,
    "shareCount": 220,
    "signinCount": 880,
    "conversionRate": 0.316
  }
}
```

- **错误码**：0 / 2001 / 3002

---

## 10.3 MemberController 会员管理

- **类映射前缀**：`/member`
- **完整路径示例**：`/api/member/list`
- **说明**：会员档案、等级、余额、积分、消费记录、充值。

### 10.3.1 会员列表

- **方法**：GET
- **路径**：`/api/member/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| keyword | String | 否 | 姓名 / 手机号 |
| levelId | Long | 否 | 等级 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 320,
    "page": 1,
    "size": 10,
    "records": [
      {
        "memberId": 1,
        "memberName": "赵六",
        "phone": "13800000006",
        "levelId": 2,
        "levelName": "银卡",
        "balance": 500.00,
        "points": 1200,
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001 / 3002

### 10.3.2 会员详情

- **方法**：GET
- **路径**：`/api/member/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会员 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "memberId": 1,
    "memberName": "赵六",
    "phone": "13800000006",
    "levelId": 2,
    "levelName": "银卡",
    "balance": 500.00,
    "points": 1200,
    "totalSpent": 8800.00,
    "joinDate": "2024-05-01"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 10.3.3 新增会员

- **方法**：POST
- **路径**：`/api/member`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| memberName | String | 是 | 会员姓名 |
| phone | String | 是 | 手机号 |
| levelId | Long | 否 | 等级 ID，默认最低级 |
| balance | BigDecimal | 否 | 初始余额 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "memberId": 2 } }
```

- **错误码**：0 / 1001 / 1003 / 5000

### 10.3.4 修改会员

- **方法**：PUT
- **路径**：`/api/member/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会员 ID（路径参数） |
| memberName | String | 否 | 姓名 |
| phone | String | 否 | 手机号 |
| levelId | Long | 否 | 等级 ID |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5000

### 10.3.5 消费记录

- **方法**：GET
- **路径**：`/api/member/{id}/consume-records`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会员 ID（路径参数） |
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "recordId": 1, "orderId": "O20260801001", "amount": 380.00, "consumeDate": "2026-08-01", "earnedPoints": 38 }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 10.3.6 积分记录

- **方法**：GET
- **路径**：`/api/member/{id}/points`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会员 ID（路径参数） |
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "logId": 1, "type": "earn", "points": 38, "source": "消费", "logDate": "2026-08-01" }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 10.3.7 会员充值

- **方法**：POST
- **路径**：`/api/member/{id}/recharge`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 会员 ID（路径参数） |
| amount | BigDecimal | 是 | 充值金额 |
| giftAmount | BigDecimal | 否 | 赠送金额 |
| accountId | Long | 是 | 支付账户 ID |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "balance": 1500.00 } }
```

- **错误码**：0 / 1001 / 4001 / 5000

### 10.3.8 会员等级列表

- **方法**：GET
- **路径**：`/api/member/levels`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "levelId": 1, "levelName": "普通", "discount": 1.0, "minSpent": 0 },
    { "levelId": 2, "levelName": "银卡", "discount": 0.95, "minSpent": 5000 },
    { "levelId": 3, "levelName": "金卡", "discount": 0.9, "minSpent": 20000 }
  ]
}
```

- **错误码**：0 / 2001

### 10.3.9 新增会员等级

- **方法**：POST
- **路径**：`/api/member/levels`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| levelName | String | 是 | 等级名称 |
| discount | BigDecimal | 是 | 折扣 |
| minSpent | BigDecimal | 是 | 升级门槛消费额 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "levelId": 4 } }
```

- **错误码**：0 / 1001 / 5000

### 10.3.10 会员档案列表

- **方法**：GET
- **路径**：`/api/member/master`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "masterId": 1, "memberName": "赵六", "idCard": "110101199001011234", "birthday": "1990-01-01", "gender": 1 }
  ]
}
```

- **错误码**：0 / 2001

### 10.3.11 余额变动列表

- **方法**：GET
- **路径**：`/api/member/balance`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| memberId | Long | 否 | 会员 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "logId": 1, "memberName": "赵六", "type": "recharge", "amount": 1000.00, "balanceAfter": 1500.00, "logDate": "2026-08-01" }
  ]
}
```

- **错误码**：0 / 2001

---

## 10.4 KitchenController 厨房管理

- **类映射前缀**：`/kitchen`
- **完整路径示例**：`/api/kitchen/logs`
- **说明**：后厨日志与厨房能耗记录。

### 10.4.1 后厨日志列表

- **方法**：GET
- **路径**：`/api/kitchen/logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| action | String | 否 | 动作类型 |
| keyword | String | 否 | 关键字 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "logId": 1, "action": "出菜", "dishName": "宫保鸡丁", "operator": "厨师A", "logTime": "2026-08-01 12:05:00", "remark": "" }
  ]
}
```

- **错误码**：0 / 2001

### 10.4.2 新增后厨日志

- **方法**：POST
- **路径**：`/api/kitchen/logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| action | String | 是 | 动作：出菜 / 退菜 / 报损等 |
| dishId | Long | 否 | 菜品 ID |
| operator | String | 是 | 操作人 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 10.4.3 厨房能耗列表

- **方法**：GET
- **路径**：`/api/kitchen/energy`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| energyType | String | 否 | 类型：water / elec / gas |
| start | Date | 否 | 起始日期 |
| end | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "energyId": 1, "energyType": "gas", "consumption": 50, "amount": 150.00, "recordDate": "2026-07-31" }
  ]
}
```

- **错误码**：0 / 2001

### 10.4.4 录入厨房能耗

- **方法**：POST
- **路径**：`/api/kitchen/energy`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| energyType | String | 是 | 类型 |
| consumption | BigDecimal | 是 | 消耗量 |
| amount | BigDecimal | 否 | 金额 |
| recordDate | Date | 是 | 记录日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "energyId": 2 } }
```

- **错误码**：0 / 1001 / 5000

---

## 10.5 ReportController 报表统计

- **类映射前缀**：`/report`
- **完整路径示例**：`/api/report/daily-summary`
- **说明**：日报、营业额、菜品销量、部门成本、员工绩效、总览。

### 10.5.1 日报汇总

- **方法**：GET
- **路径**：`/api/report/daily-summary`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "reportDate": "2026-08-01", "revenue": 12800.00, "orderCount": 36, "guestCount": 180, "avgOrder": 355.56 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 10.5.2 营业额报表

- **方法**：GET
- **路径**：`/api/report/revenue`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |
| groupBy | String | 否 | 分组：day / month |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "period": "2026-08-01", "revenue": 12800.00, "cashRevenue": 5600.00, "wechatRevenue": 5000.00, "memberRevenue": 2200.00 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 10.5.3 菜品销量报表

- **方法**：GET
- **路径**：`/api/report/dish-sales`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |
| categoryId | Long | 否 | 分类 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "dishId": 1001, "dishName": "宫保鸡丁", "qty": 86, "revenue": 3268.00, "rank": 1 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 10.5.4 部门成本报表

- **方法**：GET
- **路径**：`/api/report/department-cost`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "deptId": 10, "deptName": "前厅部", "salaryCost": 40000.00, "materialCost": 0.00, "totalCost": 40000.00 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 10.5.5 员工绩效报表

- **方法**：GET
- **路径**：`/api/report/staff-kpi`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "staffId": 1001, "staffName": "张三", "salesAmount": 38000.00, "orderCount": 120, "kpiScore": 95.5 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 10.5.6 报表总览

- **方法**：GET
- **路径**：`/api/report/overview`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | Long | 否 | 门店 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "todayRevenue": 12800.00,
    "monthRevenue": 380000.00,
    "todayOrders": 36,
    "monthOrders": 1080,
    "topDish": "宫保鸡丁",
    "avgDailyRevenue": 12258.06
  }
}
```

- **错误码**：0 / 2001

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| MarketingController | /marketing/activities | GET/POST/PUT/DELETE | 营销活动 |
| MarketingController | /marketing/coupons | GET/POST | 优惠券 |
| MarketingController | /marketing/discount-rules | GET | 折扣规则 |
| MarketingController | /marketing/shares | GET | 分享记录 |
| MarketingController | /marketing/signins | GET | 签到列表 |
| MarketingController | /marketing/signin-records | GET | 签到记录 |
| MarketingController | /marketing/tasks | GET | 任务列表 |
| MarketingOverviewController | /marketingOverview/overview | GET | 营销概览 |
| MemberController | /member/{id}/consume-records | GET | 消费记录 |
| MemberController | /member/{id}/points | GET | 积分记录 |
| MemberController | /member/{id}/recharge | POST | 会员充值 |
| MemberController | /member/levels | GET/POST | 会员等级 |
| MemberController | /member/master | GET | 会员档案 |
| MemberController | /member/balance | GET | 余额变动 |
| KitchenController | /kitchen/logs | GET/POST | 后厨日志 |
| KitchenController | /kitchen/energy | GET/POST | 厨房能耗 |
| ReportController | /report/daily-summary | GET | 日报汇总 |
| ReportController | /report/revenue | GET | 营业额报表 |
| ReportController | /report/dish-sales | GET | 菜品销量 |
| ReportController | /report/department-cost | GET | 部门成本 |
| ReportController | /report/staff-kpi | GET | 员工绩效 |
| ReportController | /report/overview | GET | 报表总览 |
