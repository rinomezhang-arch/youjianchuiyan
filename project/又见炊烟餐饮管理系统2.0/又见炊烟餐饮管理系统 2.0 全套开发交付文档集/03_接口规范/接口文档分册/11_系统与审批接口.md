# 11 系统与审批接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 系统与审批 |
| Controller 数量 | 8 |
| 覆盖 Controller | DictController / DashboardController / OperationLogController / UploadController / ChatController / ApprovalController / ReimbursementController / ScheduleMonthController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离；dashboard 支持 storeId=all 汇总 |
| Authorization | 是 | `Bearer <JWT>`，上传接口可放宽 |
| Content-Type | 否 | POST/PUT 为 application/json；上传为 multipart/form-data |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 1003 | 重复数据 | 字典编码 / 报销单号重复 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 审批 / 发布排班需对应角色 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 主键查询为空 |
| 4002 | 记录已被删除 | 命中逻辑删除标记 |
| 4003 | 状态不允许操作 | 审批已完成不可重复 |
| 5000 | 业务异常 | BusinessException |
| 9000 | 系统异常 | 未捕获异常 |

---

## 11.1 DictController 字典管理

- **类映射前缀**：`/dict`
- **完整路径示例**：`/api/dict/types`
- **说明**：字典类型与字典项、批量取字典、员工 / 客户下拉搜索。

### 11.1.1 字典类型列表

- **方法**：GET
- **路径**：`/api/dict/types`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "dictCode": "occasion_type", "dictName": "宴会场合", "itemCount": 6 }
  ]
}
```

- **错误码**：0 / 2001

### 11.1.2 字典项列表

- **方法**：GET
- **路径**：`/api/dict/items/{dictCode}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dictCode | String | 是 | 字典编码（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "itemId": 1, "dictCode": "occasion_type", "itemLabel": "婚宴", "itemValue": "wedding", "sort": 1 }
  ]
}
```

- **错误码**：0 / 4001 / 2001

### 11.1.3 批量取字典

- **方法**：GET
- **路径**：`/api/dict/batch`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| codes | String | 是 | 字典编码列表，逗号分隔 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "occasion_type": [{ "itemLabel": "婚宴", "itemValue": "wedding" }],
    "source_type": [{ "itemLabel": "线上", "itemValue": "online" }]
  }
}
```

- **错误码**：0 / 1001 / 2001

### 11.1.4 新增字典项

- **方法**：POST
- **路径**：`/api/dict/items`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dictCode | String | 是 | 字典编码 |
| itemLabel | String | 是 | 显示名称 |
| itemValue | String | 是 | 字典值 |
| sort | Integer | 否 | 排序号 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "itemId": 7 } }
```

- **错误码**：0 / 1001 / 1003

### 11.1.5 修改字典项

- **方法**：PUT
- **路径**：`/api/dict/items/{itemId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| itemId | Long | 是 | 字典项 ID（路径参数） |
| itemLabel | String | 否 | 显示名称 |
| itemValue | String | 否 | 字典值 |
| sort | Integer | 否 | 排序号 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001

### 11.1.6 删除字典项

- **方法**：DELETE
- **路径**：`/api/dict/items/{itemId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| itemId | Long | 是 | 字典项 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001

### 11.1.7 员工下拉搜索

- **方法**：GET
- **路径**：`/api/dict/staff`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| keyword | String | 否 | 姓名 / 手机号 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "staffId": 1001, "staffName": "张三", "phone": "13800000001" }
  ]
}
```

- **错误码**：0 / 2001

### 11.1.8 客户下拉搜索

- **方法**：GET
- **路径**：`/api/dict/customers`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| keyword | String | 否 | 姓名 / 手机号 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "customerId": 1, "name": "李四", "phone": "13900000002" }
  ]
}
```

- **错误码**：0 / 2001

---

## 11.2 DashboardController 数据总览

- **类映射前缀**：`/dashboard`
- **完整路径示例**：`/api/dashboard/today`
- **说明**：首页看板，店长仅本店，总经理支持 storeId=all 汇总。

### 11.2.1 今日看板

- **方法**：GET
- **路径**：`/api/dashboard/today`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | String | 否 | 门店 ID 或 all |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "todayRevenue": 12800.00,
    "todayOrders": 36,
    "todayGuests": 180,
    "tableOccupancy": 0.5,
    "pendingApprovals": 4,
    "lowStockCount": 3,
    "expiringContracts": 2
  }
}
```

- **错误码**：0 / 2001 / 3002

### 11.2.2 看板报表

- **方法**：GET
- **路径**：`/api/dashboard/report`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| storeId | String | 否 | 门店 ID 或 all |
| startDate | Date | 否 | 起始日期 |
| endDate | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalRevenue": 380000.00,
    "totalOrders": 1080,
    "totalGuests": 5400,
    "avgOrder": 351.85,
    "revenueTrend": [
      { "date": "2026-07-01", "revenue": 12000.00 }
    ],
    "topDishes": [
      { "dishName": "宫保鸡丁", "qty": 860 }
    ]
  }
}
```

- **错误码**：0 / 2001 / 3002

---

## 11.3 OperationLogController 操作日志

- **类映射前缀**：`/operationLog`
- **完整路径示例**：`/api/operationLog/change-logs`
- **说明**：业务变更日志与系统审计日志。

### 11.3.1 变更日志列表

- **方法**：GET
- **路径**：`/api/operationLog/change-logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| bizType | String | 否 | 业务类型 |
| keyword | String | 否 | 关键字 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 200,
    "page": 1,
    "size": 10,
    "records": [
      {
        "logId": 1,
        "bizType": "booking",
        "bizId": "B20260801001",
        "action": "update",
        "operator": "张三",
        "operateTime": "2026-08-01 10:00:00",
        "content": "修改桌数 18→20"
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 11.3.2 变更日志详情

- **方法**：GET
- **路径**：`/api/operationLog/change-logs/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 日志 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": { "logId": 1, "bizType": "booking", "action": "update", "operator": "张三", "content": "修改桌数 18→20", "beforeData": "{}", "afterData": "{}" }
}
```

- **错误码**：0 / 4001 / 3002

### 11.3.3 新增变更日志

- **方法**：POST
- **路径**：`/api/operationLog/change-logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bizType | String | 是 | 业务类型 |
| bizId | String | 是 | 业务 ID |
| action | String | 是 | 动作 |
| content | String | 否 | 内容 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 11.3.4 审计日志列表

- **方法**：GET
- **路径**：`/api/operationLog/audit-logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| operator | String | 否 | 操作人 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 500,
    "page": 1,
    "size": 10,
    "records": [
      { "logId": 1, "operator": "admin", "module": "staff", "action": "delete", "ip": "192.168.1.1", "operateTime": "2026-08-01 11:00:00" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 11.3.5 新增审计日志

- **方法**：POST
- **路径**：`/api/operationLog/audit-logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| module | String | 是 | 模块 |
| action | String | 是 | 动作 |
| ip | String | 否 | IP |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 2 } }
```

- **错误码**：0 / 1001 / 5000

---

## 11.4 UploadController 文件上传

- **类映射前缀**：`/upload`
- **完整路径示例**：`/api/upload/image`
- **说明**：图片上传至对象存储（COS）。

### 11.4.1 上传图片

- **方法**：POST
- **路径**：`/api/upload/image`
- **请求头**：storeId、Authorization、Content-Type: multipart/form-data

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | File | 是 | 图片文件，支持 jpg/png/webp |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "url": "https://cos.example.com/upload/2026/08/xxx.jpg",
    "fileName": "xxx.jpg",
    "size": 102400
  }
}
```

- **错误码**：0 / 1001（文件为空）/ 5000（上传失败）

### 11.4.2 上传文件

- **方法**：POST
- **路径**：`/api/upload/file`
- **请求头**：storeId、Authorization、Content-Type: multipart/form-data

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | File | 是 | 文件，支持 pdf/doc/xls 等 |
| bizType | String | 否 | 业务类型，决定存储目录 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "url": "https://cos.example.com/upload/2026/08/contract.pdf",
    "fileName": "contract.pdf",
    "size": 204800
  }
}
```

- **错误码**：0 / 1001 / 5000

---

## 11.5 ChatController 聊天系统

- **类映射前缀**：`/chat`
- **完整路径示例**：`/api/chat/greeting`
- **说明**：智能助手欢迎语与会话。

### 11.5.1 欢迎语

- **方法**：GET
- **路径**：`/api/chat/greeting`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 从 Token 解析当前员工 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "greeting": "张三，下午好！今日已接 12 单，营业额 4500 元。",
    "suggestions": ["查看今日报表", "待审批事项", "低库存预警"]
  }
}
```

- **错误码**：0 / 2001

### 11.5.2 发送消息

- **方法**：POST
- **路径**：`/api/chat/send`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| message | String | 是 | 用户消息 |
| sessionId | String | 否 | 会话 ID，不传则新建 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "sessionId": "S20260801001",
    "reply": "今日营业额为 12800 元，较昨日增长 8%。",
    "timestamp": "2026-08-01 14:00:00"
  }
}
```

- **错误码**：0 / 1001 / 5000

### 11.5.3 会话历史

- **方法**：GET
- **路径**：`/api/chat/history`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| sessionId | String | 是 | 会话 ID |
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "role": "user", "content": "今日营业额", "time": "2026-08-01 14:00:00" },
    { "role": "assistant", "content": "今日营业额为 12800 元", "time": "2026-08-01 14:00:01" }
  ]
}
```

- **错误码**：0 / 1001 / 2001

---

## 11.6 ApprovalController 审批流程

- **类映射前缀**：`/approval`
- **完整路径示例**：`/api/approval/submit`
- **说明**：通用审批流：提交、通过、驳回、待办、历史、模板。

### 11.6.1 提交审批

- **方法**：POST
- **路径**：`/api/approval/submit`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bizType | String | 是 | 业务类型：expense / payable / stock_loss / purchase 等 |
| bizId | Long | 是 | 业务单据 ID |
| templateId | Long | 否 | 审批模板 ID |
| submitComment | String | 否 | 提交说明 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "flowId": 100, "status": "pending", "currentNode": "店长审批" } }
```

- **错误码**：0 / 1001 / 4001 / 5000

### 11.6.2 审批通过

- **方法**：POST
- **路径**：`/api/approval/{flowId}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| flowId | Long | 是 | 审批流 ID（路径参数） |
| comment | String | 否 | 审批意见 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "flowId": 100, "status": "approved", "currentNode": "结束" } }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 11.6.3 审批驳回

- **方法**：POST
- **路径**：`/api/approval/{flowId}/reject`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| flowId | Long | 是 | 审批流 ID（路径参数） |
| comment | String | 是 | 驳回原因 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "flowId": 100, "status": "rejected" } }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 11.6.4 待审批列表

- **方法**：GET
- **路径**：`/api/approval/pending`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 店长仅本店，总经理全部 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "flowId": 100, "bizType": "expense", "bizId": 12, "applicant": "张三", "submitTime": "2026-08-01 09:00:00", "currentNode": "店长审批" }
  ]
}
```

- **错误码**：0 / 2001 / 3002

### 11.6.5 历史审批

- **方法**：GET
- **路径**：`/api/approval/history`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 店长仅本店，总经理全部 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "flowId": 99, "bizType": "purchase", "bizId": 1, "status": "approved", "finishTime": "2026-07-31 16:00:00" }
  ]
}
```

- **错误码**：0 / 2001

### 11.6.6 审批模板列表

- **方法**：GET
- **路径**：`/api/approval/templates`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 供下拉选择 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "templateId": 1, "templateName": "费用报销审批", "nodes": 2 }
  ]
}
```

- **错误码**：0 / 2001

### 11.6.7 审批流列表

- **方法**：GET
- **路径**：`/api/approval/flows`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 所有审批记录 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "flowId": 100, "bizType": "expense", "status": "pending", "submitTime": "2026-08-01 09:00:00" }
  ]
}
```

- **错误码**：0 / 2001

### 11.6.8 审批详情

- **方法**：GET
- **路径**：`/api/approval/{flowId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| flowId | Long | 是 | 审批流 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "flowId": 100,
    "bizType": "expense",
    "bizId": 12,
    "status": "pending",
    "nodes": [
      { "nodeId": 1, "nodeName": "店长审批", "approver": "店长", "status": "approved", "comment": "同意", "operateTime": "2026-08-01 10:00:00" },
      { "nodeId": 2, "nodeName": "总经理审批", "approver": "总经理", "status": "pending" }
    ]
  }
}
```

- **错误码**：0 / 4001 / 3002

---

## 11.7 ReimbursementController 报销管理

- **类映射前缀**：`/reimbursement`
- **完整路径示例**：`/api/reimbursement/list`
- **说明**：员工报销申请与审批；多表事务。

### 11.7.1 报销列表

- **方法**：GET
- **路径**：`/api/reimbursement/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待审 / 已通过 / 已驳回 |
| applicant | String | 否 | 报销人 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 15,
    "page": 1,
    "size": 10,
    "records": [
      {
        "reimbursementId": 1,
        "reimburseNo": "BX-2026-0001",
        "applicant": "张三",
        "amount": 500.00,
        "applyDate": "2026-07-30",
        "category": "差旅费",
        "status": "待审",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 11.7.2 报销详情

- **方法**：GET
- **路径**：`/api/reimbursement/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报销单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "reimbursementId": 1,
    "reimburseNo": "BX-2026-0001",
    "applicant": "张三",
    "amount": 500.00,
    "applyDate": "2026-07-30",
    "category": "差旅费",
    "annexUrl": "/upload/2026/bx-001.pdf",
    "remark": "外地采购交通费",
    "status": "待审"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 11.7.3 新增报销

- **方法**：POST
- **路径**：`/api/reimbursement`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| applicant | String | 是 | 报销人 |
| amount | BigDecimal | 是 | 金额 |
| category | String | 是 | 报销类别 |
| applyDate | Date | 是 | 申请日期 |
| annexUrl | String | 否 | 附件地址 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "reimbursementId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 11.7.4 修改报销

- **方法**：PUT
- **路径**：`/api/reimbursement/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报销单 ID（路径参数） |
| amount | BigDecimal | 否 | 金额 |
| category | String | 否 | 类别 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 5000

### 11.7.5 报销审批

- **方法**：POST
- **路径**：`/api/reimbursement/{id}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报销单 ID（路径参数） |
| approved | Boolean | 是 | true 通过 / false 驳回 |
| comment | String | 否 | 审批意见 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001 / 5000

### 11.7.6 删除报销

- **方法**：DELETE
- **路径**：`/api/reimbursement/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报销单 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已通过不可删）/ 5000

---

## 11.8 ScheduleMonthController 月排班

- **类映射前缀**：`/scheduleMonth`
- **完整路径示例**：`/api/scheduleMonth/list`
- **说明**：月度排班主表、每日明细、员工排班、生成、发布、确认。

### 11.8.1 排班主表列表

- **方法**：GET
- **路径**：`/api/scheduleMonth/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| month | String | 否 | 月份 YYYY-MM |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 3,
    "page": 1,
    "size": 10,
    "records": [
      {
        "scheduleId": 1,
        "month": "2026-08",
        "status": "已发布",
        "creator": "张三",
        "createTime": "2026-07-25 10:00:00",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 11.8.2 每日明细

- **方法**：GET
- **路径**：`/api/scheduleMonth/{scheduleId}/days`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| scheduleId | Long | 是 | 排班 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "dayId": 1, "scheduleDate": "2026-08-01", "staffId": 1001, "staffName": "张三", "shiftName": "早班" }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 11.8.3 员工月度排班

- **方法**：GET
- **路径**：`/api/scheduleMonth/staff/{staffId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| staffId | Long | 是 | 员工 ID（路径参数） |
| month | String | 是 | 月份 YYYY-MM |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "scheduleDate": "2026-08-01", "shiftName": "早班", "shiftStart": "08:00", "shiftEnd": "16:00" }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 11.8.4 生成月度排班

- **方法**：POST
- **路径**：`/api/scheduleMonth`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| month | String | 是 | 月份 YYYY-MM |
| templateId | Long | 否 | 排班模板 ID |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "scheduleId": 2, "month": "2026-09", "status": "草稿" } }
```

- **错误码**：0 / 1001 / 5000

### 11.8.5 发布排班

- **方法**：PUT
- **路径**：`/api/scheduleMonth/{scheduleId}/publish`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| scheduleId | Long | 是 | 排班 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "scheduleId": 1, "status": "已发布" } }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 11.8.6 确认排班

- **方法**：PUT
- **路径**：`/api/scheduleMonth/{scheduleId}/confirm`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| scheduleId | Long | 是 | 排班 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "scheduleId": 1, "status": "已确认" } }
```

- **错误码**：0 / 4001 / 4003

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| DictController | /dict/types | GET | 字典类型 |
| DictController | /dict/items/{dictCode} | GET | 字典项 |
| DictController | /dict/batch | GET | 批量取字典 |
| DictController | /dict/items | POST | 新增字典项 |
| DictController | /dict/items/{itemId} | PUT/DELETE | 修改/删除字典项 |
| DictController | /dict/staff | GET | 员工下拉 |
| DictController | /dict/customers | GET | 客户下拉 |
| DashboardController | /dashboard/today | GET | 今日看板 |
| DashboardController | /dashboard/report | GET | 看板报表 |
| OperationLogController | /operationLog/change-logs | GET/POST | 变更日志 |
| OperationLogController | /operationLog/audit-logs | GET/POST | 审计日志 |
| UploadController | /upload/image | POST | 上传图片 |
| UploadController | /upload/file | POST | 上传文件 |
| ChatController | /chat/greeting | GET | 欢迎语 |
| ChatController | /chat/send | POST | 发送消息 |
| ChatController | /chat/history | GET | 会话历史 |
| ApprovalController | /approval/submit | POST | 提交审批 |
| ApprovalController | /approval/{flowId}/approve | POST | 审批通过 |
| ApprovalController | /approval/{flowId}/reject | POST | 审批驳回 |
| ApprovalController | /approval/pending | GET | 待审批 |
| ApprovalController | /approval/history | GET | 历史审批 |
| ApprovalController | /approval/templates | GET | 审批模板 |
| ApprovalController | /approval/flows | GET | 审批流列表 |
| ApprovalController | /approval/{flowId} | GET | 审批详情 |
| ReimbursementController | /reimbursement/{id}/approve | POST | 报销审批 |
| ScheduleMonthController | /scheduleMonth/{scheduleId}/days | GET | 每日明细 |
| ScheduleMonthController | /scheduleMonth/staff/{staffId} | GET | 员工月度排班 |
| ScheduleMonthController | /scheduleMonth/{scheduleId}/publish | PUT | 发布排班 |
| ScheduleMonthController | /scheduleMonth/{scheduleId}/confirm | PUT | 确认排班 |
