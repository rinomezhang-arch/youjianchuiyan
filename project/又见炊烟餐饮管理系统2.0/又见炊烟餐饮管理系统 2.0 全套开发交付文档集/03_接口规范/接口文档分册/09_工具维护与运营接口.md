# 09 工具维护与运营接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 工具维护与运营 |
| Controller 数量 | 4 |
| 覆盖 Controller | ToolManagementController / MaintenanceController / EngineeringController / EnergyController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离 |
| Authorization | 是 | `Bearer <JWT>` |
| Content-Type | 否 | POST/PUT 为 application/json |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 资产新增仅总经理 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 主键查询为空 |
| 4003 | 状态不允许操作 | 维修单已完成不可派单 |
| 5000 | 业务异常 | BusinessException |
| 9000 | 系统异常 | 未捕获异常 |

---

## 9.1 ToolManagementController 工具管理

- **类映射前缀**：`/tool`
- **完整路径示例**：`/api/tool/list`
- **说明**：工具台账、分类、领用、归还、损坏、盘点；多表事务。

### 9.1.1 工具台账列表

- **方法**：GET
- **路径**：`/api/tool/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| keyword | String | 否 | 工具名称搜索 |
| categoryId | Long | 否 | 分类 ID |
| status | String | 否 | 状态：在用 / 闲置 / 报废 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 40,
    "page": 1,
    "size": 10,
    "records": [
      {
        "toolId": 1,
        "toolName": "不锈钢炒锅",
        "toolNo": "GJ-001",
        "categoryId": 1,
        "categoryName": "锅具",
        "qty": 10,
        "unit": "把",
        "status": "在用",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 9.1.2 工具详情

- **方法**：GET
- **路径**：`/api/tool/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 工具 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": { "toolId": 1, "toolName": "不锈钢炒锅", "toolNo": "GJ-001", "qty": 10, "status": "在用" }
}
```

- **错误码**：0 / 4001 / 3002

### 9.1.3 新增工具

- **方法**：POST
- **路径**：`/api/tool`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| toolName | String | 是 | 工具名称 |
| toolNo | String | 否 | 工具编号 |
| categoryId | Long | 是 | 分类 ID |
| qty | Integer | 是 | 数量 |
| unit | String | 是 | 单位 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "toolId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 9.1.4 修改工具

- **方法**：PUT
- **路径**：`/api/tool/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 工具 ID（路径参数） |
| toolName | String | 否 | 名称 |
| qty | Integer | 否 | 数量 |
| status | String | 否 | 状态 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5000

### 9.1.5 删除工具

- **方法**：DELETE
- **路径**：`/api/tool/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 工具 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5000

### 9.1.6 工具分类列表

- **方法**：GET
- **路径**：`/api/tool/categories`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| parentId | Long | 否 | 父分类 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "categoryId": 1, "categoryName": "锅具", "parentId": 0 }
  ]
}
```

- **错误码**：0 / 2001

### 9.1.7 新增工具分类

- **方法**：POST
- **路径**：`/api/tool/categories`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| categoryName | String | 是 | 分类名称 |
| parentId | Long | 否 | 父分类 ID，默认 0 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "categoryId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 9.1.8 删除工具分类

- **方法**：DELETE
- **路径**：`/api/tool/categories/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 分类 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5002

### 9.1.9 工具领用列表

- **方法**：GET
- **路径**：`/api/tool/issues`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "issueId": 1, "toolId": 1, "toolName": "不锈钢炒锅", "qty": 2, "receiver": "李四", "issueDate": "2026-08-01" }
  ]
}
```

- **错误码**：0 / 2001

### 9.1.10 工具领用

- **方法**：POST
- **路径**：`/api/tool/issues`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| toolId | Long | 是 | 工具 ID |
| qty | Integer | 是 | 领用数量 |
| receiver | String | 是 | 领用人 |
| issueDate | Date | 是 | 领用日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "issueId": 2 } }
```

- **错误码**：0 / 1001 / 5000（库存不足）

### 9.1.11 工具归还列表

- **方法**：GET
- **路径**：`/api/tool/returns`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| issueId | Long | 否 | 领用单 ID 过滤 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "returnId": 1, "issueId": 1, "toolName": "不锈钢炒锅", "qty": 2, "returnDate": "2026-08-01" }
  ]
}
```

- **错误码**：0 / 2001

### 9.1.12 工具归还

- **方法**：POST
- **路径**：`/api/tool/returns`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| issueId | Long | 是 | 领用单 ID |
| qty | Integer | 是 | 归还数量 |
| returnDate | Date | 是 | 归还日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "returnId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 9.1.13 工具损坏列表

- **方法**：GET
- **路径**：`/api/tool/damages`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "damageId": 1, "toolName": "不锈钢炒锅", "qty": 1, "reason": "底部穿孔", "status": "待处理" }
  ]
}
```

- **错误码**：0 / 2001

### 9.1.14 登记工具损坏

- **方法**：POST
- **路径**：`/api/tool/damages`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| toolId | Long | 是 | 工具 ID |
| qty | Integer | 是 | 损坏数量 |
| reason | String | 是 | 损坏原因 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "damageId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 9.1.15 处理损坏

- **方法**：POST
- **路径**：`/api/tool/damages/{id}/handle`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损坏单 ID（路径参数） |
| handleResult | String | 是 | 处理结果：维修 / 报废 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 5000

### 9.1.16 工具盘点列表

- **方法**：GET
- **路径**：`/api/tool/inventories`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "inventoryId": 1, "toolName": "不锈钢炒锅", "bookQty": 10, "actualQty": 9, "diffQty": -1 }
  ]
}
```

- **错误码**：0 / 2001

### 9.1.17 新增工具盘点

- **方法**：POST
- **路径**：`/api/tool/inventories`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| toolId | Long | 是 | 工具 ID |
| bookQty | Integer | 是 | 账面数量 |
| actualQty | Integer | 是 | 实盘数量 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "inventoryId": 2 } }
```

- **错误码**：0 / 1001 / 5000

---

## 9.2 MaintenanceController 维护管理

- **类映射前缀**：`/maintenance`
- **完整路径示例**：`/api/maintenance/requests`
- **说明**：设备报修、派单、完成；资产台账与盘点。

### 9.2.1 报修单列表

- **方法**：GET
- **路径**：`/api/maintenance/requests`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待派单 / 维修中 / 已完成 |
| keyword | String | 否 | 关键字 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 8,
    "page": 1,
    "size": 10,
    "records": [
      {
        "requestId": 1,
        "requestNo": "BX-2026-0001",
        "assetName": "中央空调",
        "faultDesc": "不制冷",
        "reporter": "张三",
        "reportDate": "2026-07-30",
        "status": "待派单"
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 9.2.2 提交报修

- **方法**：POST
- **路径**：`/api/maintenance/requests`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| assetId | Long | 否 | 资产 ID |
| assetName | String | 是 | 资产名称 |
| faultDesc | String | 是 | 故障描述 |
| reporter | String | 是 | 报修人 |
| reportDate | Date | 是 | 报修日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "requestId": 2, "requestNo": "BX-2026-0002" } }
```

- **错误码**：0 / 1001 / 5000

### 9.2.3 派单

- **方法**：PUT
- **路径**：`/api/maintenance/requests/{id}/dispatch`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报修单 ID（路径参数） |
| repairer | String | 是 | 维修人 |
| expectDate | Date | 否 | 预计完成日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 9.2.4 完成维修

- **方法**：PUT
- **路径**：`/api/maintenance/requests/{id}/complete`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 报修单 ID（路径参数） |
| repairResult | String | 是 | 维修结果 |
| cost | BigDecimal | 否 | 维修费用 |
| completeDate | Date | 是 | 完成日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

### 9.2.5 资产列表

- **方法**：GET
- **路径**：`/api/maintenance/assets`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| category | String | 否 | 资产分类 |
| status | String | 否 | 状态 |
| keyword | String | 否 | 关键字 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 25,
    "page": 1,
    "size": 10,
    "records": [
      { "assetId": 1, "assetName": "中央空调", "category": "空调", "purchaseDate": "2023-06-01", "status": "正常" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 9.2.6 新增资产

- **方法**：POST
- **路径**：`/api/maintenance/assets`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| assetName | String | 是 | 资产名称 |
| category | String | 是 | 分类 |
| purchaseDate | Date | 是 | 购置日期 |
| amount | BigDecimal | 否 | 购置金额 |
| status | String | 否 | 状态，默认正常 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "assetId": 2 } }
```

- **错误码**：0 / 1001 / 3001（仅总经理）

### 9.2.7 资产盘点

- **方法**：PUT
- **路径**：`/api/maintenance/assets/{id}/check`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 资产 ID（路径参数） |
| status | String | 是 | 盘点后状态 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 3002

---

## 9.3 EngineeringController 工程管理

- **类映射前缀**：`/engineering`
- **完整路径示例**：`/api/engineering/work-orders`
- **说明**：工程工单、巡检、备件。

### 9.3.1 工单列表

- **方法**：GET
- **路径**：`/api/engineering/work-orders`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待处理 / 进行中 / 已完成 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 6,
    "page": 1,
    "size": 10,
    "records": [
      { "workOrderId": 1, "orderNo": "GD-2026-0001", "title": "下水道疏通", "assignee": "王五", "status": "进行中" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 9.3.2 工单详情

- **方法**：GET
- **路径**：`/api/engineering/work-orders/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 工单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": { "workOrderId": 1, "orderNo": "GD-2026-0001", "title": "下水道疏通", "assignee": "王五", "status": "进行中", "remark": "后厨区域" }
}
```

- **错误码**：0 / 4001 / 3002

### 9.3.3 新增工单

- **方法**：POST
- **路径**：`/api/engineering/work-orders`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | String | 是 | 工单标题 |
| assignee | String | 否 | 负责人 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "workOrderId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 9.3.4 更新工单状态

- **方法**：PUT
- **路径**：`/api/engineering/work-orders/{id}/status`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 工单 ID（路径参数） |
| status | String | 是 | 目标状态 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003

### 9.3.5 巡检列表

- **方法**：GET
- **路径**：`/api/engineering/inspections`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "inspectionId": 1, "inspectDate": "2026-08-01", "inspector": "王五", "result": "正常", "items": 12 }
  ]
}
```

- **错误码**：0 / 2001

### 9.3.6 备件列表

- **方法**：GET
- **路径**：`/api/engineering/spare-parts`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "partId": 1, "partName": "灯管", "spec": "T8 36W", "stockQty": 20, "unit": "支" }
  ]
}
```

- **错误码**：0 / 2001

---

## 9.4 EnergyController 能源管理

- **类映射前缀**：`/energy`
- **完整路径示例**：`/api/energy/records`
- **说明**：水电气读数录入、月度汇总、趋势。

### 9.4.1 能源记录列表

- **方法**：GET
- **路径**：`/api/energy/records`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | String | 否 | 类型：water / elec / gas |
| startDate | Date | 否 | 起始日期 |
| endDate | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "recordId": 1,
      "type": "elec",
      "reading": 1200,
      "consumption": 200,
      "amount": 240.00,
      "recordDate": "2026-07-31"
    }
  ]
}
```

- **错误码**：0 / 2001

### 9.4.2 录入读数

- **方法**：POST
- **路径**：`/api/energy/records`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | String | 是 | 类型：water / elec / gas |
| reading | BigDecimal | 是 | 本次读数 |
| recordDate | Date | 是 | 记录日期 |
| amount | BigDecimal | 否 | 金额 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "recordId": 2, "consumption": 200 } }
```

- **错误码**：0 / 1001 / 5000

### 9.4.3 月度汇总

- **方法**：GET
- **路径**：`/api/energy/monthly-summary`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| month | String | 否 | 月份 YYYY-MM，默认当月 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "water": { "consumption": 300, "amount": 900.00 },
    "elec": { "consumption": 6000, "amount": 7200.00 },
    "gas": { "consumption": 500, "amount": 1500.00 },
    "totalAmount": 9600.00
  }
}
```

- **错误码**：0 / 2001

### 9.4.4 月度趋势

- **方法**：GET
- **路径**：`/api/energy/trend`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 返回近 6 月趋势 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "month": "2026-02", "water": 280, "elec": 5500, "gas": 480 },
    { "month": "2026-03", "water": 300, "elec": 6000, "gas": 500 }
  ]
}
```

- **错误码**：0 / 2001

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| ToolManagementController | /tool/categories | GET/POST | 工具分类 |
| ToolManagementController | /tool/issues | GET/POST | 工具领用 |
| ToolManagementController | /tool/returns | GET/POST | 工具归还 |
| ToolManagementController | /tool/damages | GET/POST | 工具损坏 |
| ToolManagementController | /tool/damages/{id}/handle | POST | 处理损坏 |
| ToolManagementController | /tool/inventories | GET/POST | 工具盘点 |
| MaintenanceController | /maintenance/requests/{id}/dispatch | PUT | 派单 |
| MaintenanceController | /maintenance/requests/{id}/complete | PUT | 完成维修 |
| MaintenanceController | /maintenance/assets | GET/POST | 资产台账 |
| MaintenanceController | /maintenance/assets/{id}/check | PUT | 资产盘点 |
| EngineeringController | /engineering/work-orders/{id}/status | PUT | 更新工单状态 |
| EngineeringController | /engineering/inspections | GET | 巡检列表 |
| EngineeringController | /engineering/spare-parts | GET | 备件列表 |
| EnergyController | /energy/records | GET/POST | 能源记录 |
| EnergyController | /energy/monthly-summary | GET | 月度汇总 |
| EnergyController | /energy/trend | GET | 月度趋势 |
