# 03 订单模块接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 订单模块 |
| Controller 数量 | 2 |
| 覆盖 Controller | BookingController / IpadOrderController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离 |
| Authorization | 是 | `Bearer <JWT>` |
| Content-Type | 否 | POST/PUT 为 application/json |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 1003 | 重复数据 | 预订单号重复 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 角色无权限 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 预订单 / 订单查询为空 |
| 4002 | 记录已被删除 | 命中逻辑删除标记 |
| 4003 | 状态不允许操作 | 预订已确认不可改 / 已结账不可退 |
| 5000 | 业务异常 | BusinessException |
| 5001 | 重复提交 | 桌台已被占用 |
| 9000 | 系统异常 | 未捕获异常 |

---

## 3.1 BookingController 宴会预订

- **类映射前缀**：`/booking`
- **完整路径示例**：`/api/booking/list`
- **说明**：宴会预订主单、桌台关联、菜品关联、复制 / 互换预订；涉及多表事务。

### 3.1.1 预订列表（含筛选）

- **方法**：GET
- **路径**：`/api/booking/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码，默认 1 |
| size | Integer | 否 | 每页条数，默认 10 |
| status | String | 否 | 预订状态：待确认 / 已确认 / 已完成 / 已取消 |
| startDate | Date | 否 | 宴会开始日期起 |
| endDate | Date | 否 | 宴会开始日期止 |
| keyword | String | 否 | 客户名 / 预订单号 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 36,
    "page": 1,
    "size": 10,
    "records": [
      {
        "bookingId": "B20260801001",
        "bookingNo": "YD-2026-0001",
        "customerName": "李四",
        "phone": "13900000002",
        "banquetDate": "2026-08-15",
        "banquetType": "婚宴",
        "tableCount": 20,
        "guestCount": 200,
        "status": "已确认",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001 / 3002

### 3.1.2 预订详情

- **方法**：GET
- **路径**：`/api/booking/{bookingId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "bookingId": "B20260801001",
    "bookingNo": "YD-2026-0001",
    "customerName": "李四",
    "phone": "13900000002",
    "banquetDate": "2026-08-15",
    "banquetType": "婚宴",
    "tableCount": 20,
    "guestCount": 200,
    "packageId": 5,
    "status": "已确认",
    "remark": "需要婚庆布置",
    "storeId": 1
  }
}
```

- **错误码**：0 / 4001 / 4002 / 3002

### 3.1.3 新增预订

- **方法**：POST
- **路径**：`/api/booking`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| customerName | String | 是 | 客户姓名 |
| phone | String | 是 | 联系电话 |
| banquetDate | Date | 是 | 宴会日期 |
| banquetType | String | 是 | 宴会类型 |
| tableCount | Integer | 是 | 桌数 |
| guestCount | Integer | 否 | 预计人数 |
| packageId | Long | 否 | 套餐 ID |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "bookingId": "B20260801002" } }
```

- **错误码**：0 / 1001 / 5001（桌台冲突）/ 5000（事务回滚）

### 3.1.4 修改预订

- **方法**：PUT
- **路径**：`/api/booking/{bookingId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |
| banquetDate | Date | 否 | 宴会日期 |
| tableCount | Integer | 否 | 桌数 |
| status | String | 否 | 状态 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已确认预订改期需取消重下）/ 5000

### 3.1.5 删除预订（逻辑删除）

- **方法**：DELETE
- **路径**：`/api/booking/{bookingId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已开席不可删）

### 3.1.6 宴会菜单

- **方法**：GET
- **路径**：`/api/booking/{bookingId}/banquet-menu`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "header": {
      "bookingId": "B20260801001",
      "customerName": "李四",
      "tableCount": 20,
      "packageAmount": 8888.00
    },
    "dishes": [
      { "dishId": 1001, "dishName": "宫保鸡丁", "qty": 20, "unitPrice": 38.00 }
    ]
  }
}
```

- **错误码**：0 / 4001 / 3002

### 3.1.7 预订搜索

- **方法**：GET
- **路径**：`/api/booking/search`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| keyword | String | 是 | 客户名 / 单号关键字 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "bookingId": "B20260801001", "customerName": "李四", "banquetDate": "2026-08-15" }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 3.1.8 按日期查询

- **方法**：GET
- **路径**：`/api/booking/date/{date}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| date | Date | 是 | 宴会日期（路径参数）yyyy-MM-dd |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "bookingId": "B20260801001", "customerName": "李四", "tableCount": 20 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 3.1.9 按日期范围查询

- **方法**：GET
- **路径**：`/api/booking/range`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| start | Date | 是 | 起始日期 |
| end | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "bookingId": "B20260801001", "banquetDate": "2026-08-15", "customerName": "李四" }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 3.1.10 按状态查询

- **方法**：GET
- **路径**：`/api/booking/status/{status}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | String | 是 | 状态（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "bookingId": "B20260801001", "status": "已确认", "customerName": "李四" }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 3.1.11 复制预订

- **方法**：POST
- **路径**：`/api/booking/copy`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| sourceBookingId | String | 是 | 源预订 ID |
| newBanquetDate | Date | 是 | 新宴会日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "bookingId": "B20260801003" } }
```

- **错误码**：0 / 1001 / 4001 / 5000（事务回滚）

### 3.1.12 互换预订

- **方法**：POST
- **路径**：`/api/booking/swap`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingIdA | String | 是 | 预订 A |
| bookingIdB | String | 是 | 预订 B |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 1001 / 4001 / 5000（事务回滚）

### 3.1.13 预订关联桌台-查询

- **方法**：GET
- **路径**：`/api/booking/{bookingId}/tables`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "tableBookingId": 1, "tableId": 11, "tableName": "A1 大厅", "seats": 10 }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 3.1.14 预订关联桌台-新增

- **方法**：POST
- **路径**：`/api/booking/{bookingId}/tables`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |
| tableIds | Array | 是 | 桌台 ID 数组 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "added": 5 } }
```

- **错误码**：0 / 1001 / 5001（桌台已被占用）/ 5000

### 3.1.15 预订关联桌台-删除

- **方法**：DELETE
- **路径**：`/api/booking/{bookingId}/tables/{tableBookingId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |
| tableBookingId | Long | 是 | 关联记录 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 5000

### 3.1.16 预订关联菜品-查询

- **方法**：GET
- **路径**：`/api/booking/{bookingId}/dishes`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "dishId": 1001, "dishName": "宫保鸡丁", "qty": 20, "unitPrice": 38.00, "subtotal": 760.00 }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 3.1.17 预订关联菜品-新增

- **方法**：POST
- **路径**：`/api/booking/{bookingId}/dishes`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |
| dishId | Long | 是 | 菜品 ID |
| qty | Integer | 是 | 数量 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 1001 / 4001 / 5000

### 3.1.18 预订关联菜品-批量新增

- **方法**：POST
- **路径**：`/api/booking/{bookingId}/dishes/batch`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bookingId | String | 是 | 预订 ID（路径参数） |
| items | Array | 是 | 菜品明细数组 |
| items[].dishId | Long | 是 | 菜品 ID |
| items[].qty | Integer | 是 | 数量 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "added": 12 } }
```

- **错误码**：0 / 1001 / 5000（事务回滚）

---

## 3.2 IpadOrderController iPad 订单

- **类映射前缀**：`/ipadOrder`
- **完整路径示例**：`/api/ipadOrder/order/current`
- **说明**：iPad 端点餐流程：当前订单、加菜、改菜、退菜、催菜、下发厨房。

### 3.2.1 当前订单

- **方法**：GET
- **路径**：`/api/ipadOrder/order/current`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "orderId": "O20260801001",
      "tableId": 11,
      "tableName": "A1 大厅",
      "dishId": 1001,
      "dishName": "宫保鸡丁",
      "qty": 1,
      "status": "已下单",
      "unitPrice": 38.00
    }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 3.2.2 加菜

- **方法**：POST
- **路径**：`/api/ipadOrder/order/dish/add`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| dishId | Long | 是 | 菜品 ID |
| qty | Integer | 是 | 数量 |
| remark | String | 否 | 备注（少辣等） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "orderId": "O20260801002", "subtotal": 38.00 } }
```

- **错误码**：0 / 1001 / 4001（菜品不存在或售罄）

### 3.2.3 改菜

- **方法**：PUT
- **路径**：`/api/ipadOrder/order/dish/edit`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| orderId | String | 是 | 订单明细 ID |
| qty | Integer | 是 | 修改后数量 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "orderId": "O20260801001", "qty": 2 } }
```

- **错误码**：0 / 1001 / 4003（已出菜不可改）

### 3.2.4 退菜

- **方法**：DELETE
- **路径**：`/api/ipadOrder/order/dish/remove`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| orderId | String | 是 | 订单明细 ID |
| reason | String | 是 | 退菜原因 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "removed": true } }
```

- **错误码**：0 / 1001 / 4003（已出菜不可退）

### 3.2.5 退菜退款

- **方法**：POST
- **路径**：`/api/ipadOrder/order/dish/refund`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| orderId | String | 是 | 订单明细 ID |
| reason | String | 是 | 退款原因 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "refundAmount": 38.00 } }
```

- **错误码**：0 / 1001 / 4003 / 5000

### 3.2.6 下发厨房

- **方法**：POST
- **路径**：`/api/ipadOrder/order/send-kitchen`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| orderIds | Array | 否 | 待下发明细 ID，不传则全部 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "sent": 5 } }
```

- **错误码**：0 / 1001 / 4003（已下发不可重复）

### 3.2.7 催菜

- **方法**：POST
- **路径**：`/api/ipadOrder/order/urgent`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| tableId | Long | 是 | 桌台 ID |
| orderId | String | 是 | 订单明细 ID |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "urgent": true } }
```

- **错误码**：0 / 1001 / 4001

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| BookingController | /booking/{bookingId}/banquet-menu | GET | 宴会菜单 |
| BookingController | /booking/search | GET | 预订搜索 |
| BookingController | /booking/date/{date} | GET | 按日期查询 |
| BookingController | /booking/range | GET | 日期范围查询 |
| BookingController | /booking/status/{status} | GET | 按状态查询 |
| BookingController | /booking/copy | POST | 复制预订 |
| BookingController | /booking/swap | POST | 互换预订 |
| BookingController | /booking/{bookingId}/tables | GET/POST/DELETE | 预订桌台关联 |
| BookingController | /booking/{bookingId}/dishes | GET/POST | 预订菜品关联 |
| BookingController | /booking/{bookingId}/dishes/batch | POST | 批量加菜 |
| IpadOrderController | /ipadOrder/order/current | GET | 当前订单 |
| IpadOrderController | /ipadOrder/order/dish/add | POST | 加菜 |
| IpadOrderController | /ipadOrder/order/dish/edit | PUT | 改菜 |
| IpadOrderController | /ipadOrder/order/dish/remove | DELETE | 退菜 |
| IpadOrderController | /ipadOrder/order/dish/refund | POST | 退菜退款 |
| IpadOrderController | /ipadOrder/order/send-kitchen | POST | 下发厨房 |
| IpadOrderController | /ipadOrder/order/urgent | POST | 催菜 |
