# 05 库存模块接口分册

| 项目 | 内容 |
|------|------|
| 模块名称 | 库存模块 |
| Controller 数量 | 4 |
| 覆盖 Controller | InventoryController / StockTakeController / StockTransferController / KitchenSupplyController |
| 维护人 | 又见炊烟研发组 |
| 文档版本 | 2.0 |
| 更新日期 | 2026-08-02 |
| 后端端口 | 8080 |
| 网关前缀 | 前端通过 Nginx 反代 /api/* 访问后端 |

---

## 通用约定

### 全局请求头

| 请求头 | 必填 | 说明 |
|--------|------|------|
| storeId | 是 | 门店 ID，多门店数据隔离 |
| Authorization | 是 | `Bearer <JWT>` |
| Content-Type | 否 | POST/PUT 为 application/json |

### 全局响应格式

```json
{ "code": 0, "message": "success", "data": {} }
```

### 通用错误码

| code | 含义 | 触发场景 |
|------|------|----------|
| 0 | success | 请求成功 |
| 1001 | 参数校验失败 | 必填参数缺失 |
| 1002 | 请求体格式错误 | JSON 解析失败 |
| 2001 | 未登录 / Token 失效 | 缺少 Authorization |
| 2002 | Token 过期 | JWT 超时 |
| 3001 | 无操作权限 | 角色无权限 |
| 3002 | 越权访问 | 跨门店访问 |
| 4001 | 记录不存在 | 主键查询为空 |
| 4002 | 记录已被删除 | 命中逻辑删除标记 |
| 4003 | 状态不允许操作 | 盘点已审核不可改 |
| 5000 | 业务异常 | BusinessException（库存不足等） |
| 5001 | 重复提交 | 盘点单号重复 |
| 9000 | 系统异常 | 未捕获异常 |

---

## 5.1 InventoryController 库存管理

- **类映射前缀**：`/inventory`
- **完整路径示例**：`/api/inventory/summary`
- **说明**：库存汇总、出入库流水、低库存预警、报损与调拨。

### 5.1.1 库存汇总

- **方法**：GET
- **路径**：`/api/inventory/summary`（别名 `/api/inventory/list`）
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "ingredientId": 201,
      "ingredientName": "鸡腿肉",
      "unit": "斤",
      "stockQty": 50.0,
      "totalValue": 1000.00,
      "lastInDate": "2026-07-28",
      "lastOutDate": "2026-08-01"
    }
  ]
}
```

- **错误码**：0 / 2001

### 5.1.2 出入库流水

- **方法**：GET
- **路径**：`/api/inventory/logs`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "logId": 1,
      "ingredientId": 201,
      "ingredientName": "鸡腿肉",
      "type": "in",
      "qty": 20.0,
      "unit": "斤",
      "operator": "张三",
      "createTime": "2026-07-28 10:00:00"
    }
  ]
}
```

- **错误码**：0 / 2001

### 5.1.3 按原料查询流水

- **方法**：GET
- **路径**：`/api/inventory/logs/{ingredientId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "logId": 1, "type": "in", "qty": 20.0, "operator": "张三", "createTime": "2026-07-28 10:00:00" }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 5.1.4 按日期范围查询流水

- **方法**：GET
- **路径**：`/api/inventory/logs/range`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | Date | 是 | 起始日期 |
| endDate | Date | 是 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "logId": 1, "ingredientName": "鸡腿肉", "type": "in", "qty": 20.0 }
  ]
}
```

- **错误码**：0 / 1001 / 2001

### 5.1.5 入库

- **方法**：POST
- **路径**：`/api/inventory/in`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| qty | BigDecimal | 是 | 入库数量 |
| unitPrice | BigDecimal | 是 | 单价 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 2, "stockQty": 70.0 } }
```

- **错误码**：0 / 1001 / 5000

### 5.1.6 出库

- **方法**：POST
- **路径**：`/api/inventory/out`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| qty | BigDecimal | 是 | 出库数量 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 3, "stockQty": 30.0 } }
```

- **错误码**：0 / 1001 / 5000（库存不足）

### 5.1.7 低库存预警

- **方法**：GET
- **路径**：`/api/inventory/alerts`（别名 `/api/inventory/warnings`）
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 无 | - | - | 由 storeId 隔离 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "ingredientId": 201, "ingredientName": "鸡腿肉", "stockQty": 5.0, "minStock": 10.0 }
  ]
}
```

- **错误码**：0 / 2001

### 5.1.8 提交报损

- **方法**：POST
- **路径**：`/api/inventory/loss`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| qty | BigDecimal | 是 | 报损数量 |
| reason | String | 是 | 报损原因 |
| details | Array | 否 | 损耗明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "flowId": 100, "status": "待审批" } }
```

- **错误码**：0 / 1001 / 5000

### 5.1.9 库存调拨

- **方法**：POST
- **路径**：`/api/inventory/transfer`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| qty | BigDecimal | 是 | 调拨数量 |
| fromStoreId | Long | 是 | 调出门店 |
| toStoreId | Long | 是 | 调入门店 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "logId": 4 } }
```

- **错误码**：0 / 1001 / 5000（库存不足）/ 3002

---

## 5.2 StockTakeController 盘点管理

- **类映射前缀**：`/stockTake`
- **完整路径示例**：`/api/stockTake/list`
- **说明**：盘点主单与明细、损耗主单与明细；多表事务。

### 5.2.1 盘点单列表

- **方法**：GET
- **路径**：`/api/stockTake/list`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：草稿 / 待审核 / 已审核 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 6,
    "page": 1,
    "size": 10,
    "records": [
      {
        "stockTakeId": 1,
        "stockTakeNo": "PD-2026-0001",
        "takeDate": "2026-07-31",
        "operator": "张三",
        "status": "已审核",
        "storeId": 1
      }
    ]
  }
}
```

- **错误码**：0 / 2001

### 5.2.2 盘点单详情

- **方法**：GET
- **路径**：`/api/stockTake/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 盘点单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "stockTakeId": 1,
    "stockTakeNo": "PD-2026-0001",
    "takeDate": "2026-07-31",
    "operator": "张三",
    "status": "已审核",
    "remark": "月末盘点"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 5.2.3 新增盘点单

- **方法**：POST
- **路径**：`/api/stockTake`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| takeDate | Date | 是 | 盘点日期 |
| remark | String | 否 | 备注 |
| details | Array | 否 | 盘点明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "stockTakeId": 2 } }
```

- **错误码**：0 / 1001 / 5001 / 5000（事务回滚）

### 5.2.4 修改盘点单

- **方法**：PUT
- **路径**：`/api/stockTake/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 盘点单 ID（路径参数） |
| takeDate | Date | 否 | 盘点日期 |
| status | String | 否 | 状态 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已审核不可改）/ 5000

### 5.2.5 删除盘点单

- **方法**：DELETE
- **路径**：`/api/stockTake/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 盘点单 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 5000

### 5.2.6 盘点明细列表

- **方法**：GET
- **路径**：`/api/stockTake/{id}/details`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 盘点单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "detailId": 1,
      "ingredientId": 201,
      "ingredientName": "鸡腿肉",
      "bookQty": 50.0,
      "actualQty": 48.0,
      "diffQty": -2.0
    }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 5.2.7 新增盘点明细

- **方法**：POST
- **路径**：`/api/stockTake/{id}/details`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 盘点单 ID（路径参数） |
| ingredientId | Long | 是 | 原料 ID |
| bookQty | BigDecimal | 是 | 账面数量 |
| actualQty | BigDecimal | 是 | 实盘数量 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "detailId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 5.2.8 损耗单列表

- **方法**：GET
- **路径**：`/api/stockTake/losses`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态：待审批 / 已通过 / 已驳回 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 4,
    "page": 1,
    "size": 10,
    "records": [
      { "lossId": 1, "lossNo": "BH-2026-0001", "lossDate": "2026-07-30", "amount": 120.00, "status": "已通过" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 5.2.9 损耗单详情

- **方法**：GET
- **路径**：`/api/stockTake/losses/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损耗单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": { "lossId": 1, "lossNo": "BH-2026-0001", "lossDate": "2026-07-30", "amount": 120.00, "status": "已通过" }
}
```

- **错误码**：0 / 4001 / 3002

### 5.2.10 新增损耗单

- **方法**：POST
- **路径**：`/api/stockTake/losses`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| lossDate | Date | 是 | 损耗日期 |
| reason | String | 是 | 损耗原因 |
| details | Array | 否 | 损耗明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "lossId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 5.2.11 损耗单审批

- **方法**：POST
- **路径**：`/api/stockTake/losses/{id}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损耗单 ID（路径参数） |
| approved | Boolean | 是 | true 通过 / false 驳回 |
| opinion | String | 否 | 审批意见 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 5.2.12 删除损耗单

- **方法**：DELETE
- **路径**：`/api/stockTake/losses/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损耗单 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已通过不可删）/ 5000

### 5.2.13 损耗明细

- **方法**：GET
- **路径**：`/api/stockTake/losses/{id}/details`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损耗单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "detailId": 1, "ingredientId": 201, "ingredientName": "鸡腿肉", "qty": 2.0, "amount": 40.00 }
  ]
}
```

- **错误码**：0 / 4001 / 3002

### 5.2.14 新增损耗明细

- **方法**：POST
- **路径**：`/api/stockTake/losses/{id}/details`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 损耗单 ID（路径参数） |
| ingredientId | Long | 是 | 原料 ID |
| qty | BigDecimal | 是 | 数量 |
| amount | BigDecimal | 是 | 金额 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "detailId": 2 } }
```

- **错误码**：0 / 1001 / 5000

---

## 5.3 StockTransferController 库存调拨

- **类映射前缀**：`/stockTransfer`
- **完整路径示例**：`/api/stockTransfer/list`
- **说明**：门店间 / 库位间库存调拨单。

### 5.3.1 调拨单查询

- **方法**：GET
- **路径**：`/api/stockTransfer`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| transferNo | String | 否 | 调拨单号 |
| status | String | 否 | 状态：待审核 / 已出库 / 已入库 / 已取消 |
| startDate | Date | 否 | 起始日期 |
| endDate | Date | 否 | 截止日期 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "transferId": 1,
      "transferNo": "DB-2026-0001",
      "fromStoreId": 1,
      "toStoreId": 2,
      "makeDate": "2026-07-30",
      "status": "已入库",
      "makerName": "张三"
    }
  ]
}
```

- **错误码**：0 / 2001

### 5.3.2 新增调拨单

- **方法**：POST
- **路径**：`/api/stockTransfer`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| fromStoreId | Long | 是 | 调出门店 |
| toStoreId | Long | 是 | 调入门店 |
| makerName | String | 否 | 制单人 |
| makeDate | Date | 是 | 制单日期 |
| remark | String | 否 | 备注 |
| details | Array | 否 | 调拨明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "transferId": 2, "transferNo": "DB-2026-0002" } }
```

- **错误码**：0 / 1001 / 5000（库存不足 / 事务回滚）

### 5.3.3 调拨单详情

- **方法**：GET
- **路径**：`/api/stockTransfer/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 调拨单 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "transferId": 1,
    "transferNo": "DB-2026-0001",
    "fromStoreId": 1,
    "toStoreId": 2,
    "status": "已入库",
    "remark": "支援二号店"
  }
}
```

- **错误码**：0 / 4001 / 3002

### 5.3.4 调拨单审核

- **方法**：PUT
- **路径**：`/api/stockTransfer/{id}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 调拨单 ID（路径参数） |
| approved | Boolean | 是 | true 通过 / false 驳回 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 5.3.5 删除调拨单

- **方法**：DELETE
- **路径**：`/api/stockTransfer/{id}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 调拨单 ID（路径参数） |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003（已出库不可删）

---

## 5.4 KitchenSupplyController 厨房用品

- **类映射前缀**：`/kitchenSupply`
- **完整路径示例**：`/api/kitchenSupply/purchase-requests`
- **说明**：厨房采购申请、收货、领用、预处理、成本卡、单位换算。

### 5.4.1 采购申请列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/purchase-requests`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |
| status | String | 否 | 状态 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 10,
    "page": 1,
    "size": 10,
    "records": [
      { "requestId": 1, "requestNo": "CG-2026-0001", "requestDate": "2026-07-30", "status": "待审批" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 5.4.2 新增采购申请

- **方法**：POST
- **路径**：`/api/kitchenSupply/purchase-requests`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| requestDate | Date | 是 | 申请日期 |
| items | Array | 是 | 申请明细 |
| remark | String | 否 | 备注 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "requestId": 2, "requestNo": "CG-2026-0002" } }
```

- **错误码**：0 / 1001 / 5000

### 5.4.3 采购申请审批

- **方法**：PUT
- **路径**：`/api/kitchenSupply/purchase-requests/{id}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 申请 ID（路径参数） |
| approved | Boolean | 是 | true 通过 / false 驳回 |
| opinion | String | 否 | 审批意见 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 5.4.4 收货单列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/goods-receipts`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 8,
    "page": 1,
    "size": 10,
    "records": [
      { "receiptId": 1, "receiptNo": "SH-2026-0001", "receiptDate": "2026-07-30", "supplierName": "鲜达食材" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 5.4.5 新增收货单

- **方法**：POST
- **路径**：`/api/kitchenSupply/goods-receipts`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| receiptDate | Date | 是 | 收货日期 |
| supplierId | Long | 否 | 供应商 ID |
| items | Array | 是 | 收货明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "receiptId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 5.4.6 领用单列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/requisitions`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 5,
    "page": 1,
    "size": 10,
    "records": [
      { "requisitionId": 1, "requisitionNo": "LY-2026-0001", "requisitionDate": "2026-08-01", "applicant": "李四" }
    ]
  }
}
```

- **错误码**：0 / 2001

### 5.4.7 新增领用单

- **方法**：POST
- **路径**：`/api/kitchenSupply/requisitions`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| requisitionDate | Date | 是 | 领用日期 |
| applicant | String | 是 | 领用人 |
| items | Array | 是 | 领用明细 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "requisitionId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 5.4.8 领用单审批

- **方法**：PUT
- **路径**：`/api/kitchenSupply/requisitions/{id}/approve`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 领用单 ID（路径参数） |
| approved | Boolean | 是 | true 通过 / false 驳回 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": null }
```

- **错误码**：0 / 4001 / 4003 / 3001

### 5.4.9 预处理记录列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/preprocessing`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "preprocessId": 1, "ingredientName": "土豆", "inQty": 10.0, "outQty": 8.5, "yieldRate": 0.85 }
  ]
}
```

- **错误码**：0 / 2001

### 5.4.10 新增预处理记录

- **方法**：POST
- **路径**：`/api/kitchenSupply/preprocessing`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| inQty | BigDecimal | 是 | 投入数量 |
| outQty | BigDecimal | 是 | 产出数量 |
| preprocessDate | Date | 是 | 预处理日期 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "preprocessId": 2 } }
```

- **错误码**：0 / 1001 / 5000

### 5.4.11 成本卡计算

- **方法**：POST
- **路径**：`/api/kitchenSupply/cost-cards/calculate`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dishId | Long | 是 | 菜品 ID |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "dishId": 1001, "totalCost": 18.00, "margin": 52.63 } }
```

- **错误码**：0 / 1001 / 4001

### 5.4.12 成本卡列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/cost-cards`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码 |
| size | Integer | 否 | 每页条数 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "dishId": 1001, "dishName": "宫保鸡丁", "totalCost": 18.00, "price": 38.00 }
  ]
}
```

- **错误码**：0 / 2001

### 5.4.13 菜品成本卡

- **方法**：GET
- **路径**：`/api/kitchenSupply/cost-cards/{dishId}`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dishId | Long | 是 | 菜品 ID（路径参数） |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "dishId": 1001,
    "dishName": "宫保鸡丁",
    "totalCost": 18.00,
    "items": [
      { "ingredientId": 201, "ingredientName": "鸡腿肉", "qty": 0.3, "unitPrice": 20.00, "cost": 6.00 }
    ]
  }
}
```

- **错误码**：0 / 4001 / 3002

### 5.4.14 新增单位换算

- **方法**：POST
- **路径**：`/api/kitchenSupply/unit-conversions`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 是 | 原料 ID |
| fromUnit | String | 是 | 源单位 |
| toUnit | String | 是 | 目标单位 |
| ratio | BigDecimal | 是 | 换算比例 |

- **响应示例**：

```json
{ "code": 0, "message": "success", "data": { "conversionId": 1 } }
```

- **错误码**：0 / 1001 / 1003

### 5.4.15 单位换算列表

- **方法**：GET
- **路径**：`/api/kitchenSupply/unit-conversions`
- **请求头**：storeId、Authorization

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ingredientId | Long | 否 | 原料 ID 过滤 |

- **响应示例**：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "conversionId": 1, "ingredientId": 201, "fromUnit": "斤", "toUnit": "克", "ratio": 500 }
  ]
}
```

- **错误码**：0 / 2001

---

## 附录：模块特有接口汇总

| Controller | 特有接口 | 方法 | 说明 |
|------------|----------|------|------|
| InventoryController | /inventory/summary | GET | 库存汇总 |
| InventoryController | /inventory/logs | GET | 出入库流水 |
| InventoryController | /inventory/logs/{ingredientId} | GET | 按原料查流水 |
| InventoryController | /inventory/logs/range | GET | 日期范围流水 |
| InventoryController | /inventory/in | POST | 入库 |
| InventoryController | /inventory/out | POST | 出库 |
| InventoryController | /inventory/alerts | GET | 低库存预警 |
| InventoryController | /inventory/loss | POST | 提交报损 |
| InventoryController | /inventory/transfer | POST | 库存调拨 |
| StockTakeController | /stockTake/{id}/details | GET/POST | 盘点明细 |
| StockTakeController | /stockTake/losses | GET/POST | 损耗单 |
| StockTakeController | /stockTake/losses/{id}/approve | POST | 损耗审批 |
| StockTakeController | /stockTake/losses/{id}/details | GET/POST | 损耗明细 |
| StockTransferController | /stockTransfer/{id}/approve | PUT | 调拨审批 |
| KitchenSupplyController | /kitchenSupply/purchase-requests | GET/POST | 采购申请 |
| KitchenSupplyController | /kitchenSupply/purchase-requests/{id}/approve | PUT | 采购审批 |
| KitchenSupplyController | /kitchenSupply/goods-receipts | GET/POST | 收货单 |
| KitchenSupplyController | /kitchenSupply/requisitions | GET/POST | 领用单 |
| KitchenSupplyController | /kitchenSupply/requisitions/{id}/approve | PUT | 领用审批 |
| KitchenSupplyController | /kitchenSupply/preprocessing | GET/POST | 预处理记录 |
| KitchenSupplyController | /kitchenSupply/cost-cards/calculate | POST | 成本卡计算 |
| KitchenSupplyController | /kitchenSupply/cost-cards/{dishId} | GET | 菜品成本卡 |
| KitchenSupplyController | /kitchenSupply/unit-conversions | GET/POST | 单位换算 |
