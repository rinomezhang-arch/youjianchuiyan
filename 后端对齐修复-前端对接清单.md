# 后端对齐修复 - 前端接口对接清单

> 地龙（DL-BOT）修复产出 | 2026-08-02
> 分支：feat-dl-backend-align-fix
> 用途：供 Trae 前端联调参考，可逐步迁移到简洁路径

---

## 一、本次后端修复总览

### P0 致命bug修复（4处404风险消除）

| 文件 | 修复内容 |
|---|---|
| [PurchaseController.java](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/banquet_project/src/main/java/com/youjian/banquet/controller/PurchaseController.java) | `@GetMapping("/api/procurement/requests")` → `@GetMapping("/procurement/requests")` （原路径会拼接成 `/api/menu-api/purchases/api/procurement/requests`） |
| 同上 | `@GetMapping("/api/procurement/receipts")` → `@GetMapping("/procurement/receipts")` |
| 同上 | `@PostMapping("/{purchaseId}/approve")` 加路径别名 `/audit` |
| [InventoryController.java](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/banquet_project/src/main/java/com/youjian/banquet/controller/InventoryController.java) | `@GetMapping("/summary")` 加路径别名 `/list` |
| 同上 | `@GetMapping("/alerts")` 加路径别名 `/warnings` |
| [StaffController.java](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/banquet_project/src/main/java/com/youjian/banquet/controller/StaffController.java) | 类级 `@RequestMapping` 加双路径 `{"/api/hr", "/api/menu-api"}`，使 `/api/menu-api/staff` 可访问 |

### P1 路径前缀统一（双路径兼容）

5 个 Controller 同时支持 `/api/xxx` 与 `/api/menu-api/xxx` 两种路径前缀：

| Controller | 新增标准路径 | 保留旧别名 |
|---|---|---|
| PurchaseController | `/api/purchases` | `/api/menu-api/purchases` |
| SupplierController | `/api/suppliers` | `/api/menu-api/suppliers` |
| IngredientController | `/api/ingredients` | `/api/menu-api/ingredients` |
| InventoryController | `/api/inventory` | `/api/menu-api/inventory` |
| StaffController | `/api/hr` | `/api/menu-api`（仅 /staff 端点） |

---

## 二、前端 booking.js 现状评估

[booking.js](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/frontend_v3/src/api/booking.js) 现有调用经后端兼容后**已可全部访问**（404风险已消除）。

但建议前端逐步迁移到标准路径（去掉 menu-api 中间前缀）。

### 端点路径映射表（前端调用 → 后端实际可访问路径）

| 模块 | 前端当前调用 | 后端兼容路径（可访问） | 建议迁移到 |
|---|---|---|---|
| 员工列表 | `/menu-api/staff` | ✅ `/api/menu-api/staff` | `/hr/staff` |
| 员工增删改 | `/staff` | ✅ `/api/hr/staff` (无需改) | 保持 |
| 供应商列表 | `/menu-api/suppliers` | ✅ `/api/menu-api/suppliers` 或 `/api/suppliers` | `/suppliers` |
| 供应商CRUD | `/menu-api/suppliers/{id}` | ✅ 同上 | `/suppliers/{id}` |
| 食材列表 | `/menu-api/ingredients` | ✅ `/api/menu-api/ingredients` 或 `/api/ingredients` | `/ingredients` |
| 食材新增 | `/menu-api/ingredients` | ✅ 同上 | `/ingredients` |
| 库存入库 | `/menu-api/inventory/in` | ✅ `/api/inventory/in` 或 `/api/menu-api/inventory/in` | `/inventory/in` |
| 库存出库 | `/menu-api/inventory/out` | ✅ 同上 | `/inventory/out` |
| 库存列表 | `/menu-api/inventory/list` | ✅ `/api/inventory/list` 或 `/api/menu-api/inventory/list` | `/inventory/list` |
| 库存日志 | `/menu-api/inventory/logs` | ✅ 同上 | `/inventory/logs` |
| 库存预警 | `/menu-api/inventory/warnings` | ✅ `/api/inventory/warnings` 或 `/api/menu-api/inventory/warnings` | `/inventory/warnings` |
| 采购列表 | `/menu-api/purchases` | ✅ `/api/purchases` 或 `/api/menu-api/purchases` | `/purchases` |
| 采购详情 | `/menu-api/purchases/${id}` | ✅ 同上 | `/purchases/${id}` |
| 采购新增 | `/menu-api/purchases` | ✅ 同上 | `/purchases` |
| 采购修改 | `/menu-api/purchases/${id}` | ✅ 同上 | `/purchases/${id}` |
| 采购删除 | `/menu-api/purchases/${id}` | ✅ 同上 | `/purchases/${id}` |
| 采购审批 | `/menu-api/purchases/${id}/audit` | ✅ `/api/purchases/${id}/audit` 或 `/api/menu-api/purchases/${id}/audit` | `/purchases/${id}/audit` |

---

## 三、前端迁移建议（可选优化）

### Step 1: 验证现有 booking.js 调用全部可用（无迁移成本）
后端兼容已生效，前端无任何修改即可联调。

### Step 2: 逐模块迁移到标准路径
建议迁移顺序（按风险从低到高）：

1. **采购模块**：`/menu-api/purchases` → `/purchases`（保留 `/audit` 别名）
2. **供应商模块**：`/menu-api/suppliers` → `/suppliers`
3. **食材模块**：`/menu-api/ingredients` → `/ingredients`
4. **库存模块**：`/menu-api/inventory/*` → `/inventory/*`
5. **员工模块**：`/menu-api/staff` → `/hr/staff`

### Step 3: 修复 staff 接口不一致问题
[booking.js:104](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/frontend_v3/src/api/booking.js) 调 `/menu-api/staff`，但 [booking.js:107](file:///f:/solo/project/又见炊烟餐饮管理系统2.0/frontend_v3/src/api/booking.js) 调 `/staff`（不一致）。建议统一为 `/hr/staff`。

---

## 四、验证清单

后端已通过 `mvn compile` 编译验证。前端联调时请重点验证：

- [ ] GET `/api/menu-api/staff?storeId=1` 返回员工列表
- [ ] GET `/api/menu-api/inventory/list?storeId=1` 返回库存汇总
- [ ] GET `/api/menu-api/inventory/warnings?storeId=1` 返回库存预警
- [ ] POST `/api/menu-api/purchases/1/audit` 触发采购审批
- [ ] GET `/api/purchases?storeId=1` 新路径返回采购列表（迁移验证）
- [ ] GET `/api/suppliers?storeId=1` 新路径返回供应商列表
- [ ] GET `/api/ingredients?storeId=1` 新路径返回食材列表
- [ ] GET `/api/inventory/summary?storeId=1` 新路径返回库存汇总

---

## 五、未涉及修复的内容（属于 Trae 职责）

- 前端 Vue 组件 / API 文件 / 路由的修改（需 Trae 处理）
- 实体命名重构（Java 实体类名与表名脱节，影响不大，暂不动）
- 数据库缺失表实体（约30张表无对应Java实体，按业务需要由地龙后续补）

---

## 六、后端路径前缀全表（修复后）

| 风格 | 路径前缀 | 涉及 Controller |
|---|---|---|
| 标准 | `/api/xxx` | 全部 Controller（已统一） |
| 双路径兼容 | `/api/xxx` + `/api/menu-api/xxx` | Purchase/Supplier/Ingredient/Inventory/Staff |
| 独立兼容 | `/api/dict` + `/menu-api/dict` | DictController（原有） |

---

**结论**：本次修复后，前端 booking.js 的 26 处 API 调用均可访问，无 404 风险。Trae 可在任意时间逐步迁移到标准路径，无需一次性重构。
