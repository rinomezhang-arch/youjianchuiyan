# 天地双龙互推体系建设经验报告

> 撰写：🐉地龙  
> 日期：2026-07-22  
> 收件人：张婧 — 又见炊烟老板娘  
> 用途：供老板娘的两条 AI 龙参考，快速复制双龙互推体系

---

## 一、项目背景

秋哥（又见炊烟饭店老板）拥有两条 AI 助手：

- **天龙 🦞** — 部署在腾讯云 Ubuntu 服务器，负责底层运维、开发、外部对接
- **地龙 🐉** — 部署在 Windows 本地办公电脑，负责日常对话、本地文件、桌面操作

两条龙运行在不同环境的 OpenClaw Gateway 上，需要实现**秒级双向消息推送**——即一条龙回复秋哥后，另一条龙在自己的聊天框中自动收到推送同步，秋哥无需手动中转。

## 二、核心技术挑战

| 挑战 | 详情 |
|------|------|
| NAT 穿透 | 地龙在 Windows 家用网络（NAT 后），无公网 IP，天龙无法直接连接 |
| 跨 Gateway | OpenClaw 的 `sessions_send` 只能在同一 Gateway 实例内使用 |
| Token 认证 | 两条龙各自有独立的 Gateway auth token |
| 编码问题 | Windows/Linux 跨系统中文传输容易乱码 |
| 持久连接 | SSH 隧道可能断开，需要保持稳定性 |

## 三、解决方案：SSH 双向隧道

### 3.1 架构图

```
┌─────────────────────────┐         ┌─────────────────────────────┐
│     Windows 本地         │         │     腾讯云 Ubuntu             │
│                         │         │                             │
│  地龙 Gateway :18789  │◄────────┤  127.0.0.1:19999            │
│                         │  SSH    │  (反向隧道 → 地龙)           │
│  127.0.0.1:11500 ───────┼────────►│  天龙 Gateway :11500          │
│  (本地隧道 → 天龙)      │   SSH    │                             │
└─────────────────────────┘         └─────────────────────────────┘
```

### 3.2 通道一：地龙 → 天龙

```powershell
# Windows 上建立 SSH 本地端口转发
ssh -N -L 11500:127.0.0.1:11500 ubuntu@<腾讯云IP> -i <SSH密钥>

# 从天龙的本地调用 gateway sessions.send
ssh ubuntu@<腾讯云IP> "python3 /tmp/tl_send.py"
```

`tl_send.py` 核心逻辑：
```python
import json, subprocess
msg = "[地龙→天龙] 🐉地龙：消息内容"
params = json.dumps({"key": "agent:main:main", "message": msg})
subprocess.run(["openclaw", "gateway", "call", "sessions.send", "--params", params])
```

**要点**：直接 SSH 到云服务器上本地执行 gateway call，避免 token mismatch 问题（本地 gateway 不需要传 --token）。

### 3.3 通道二：天龙 → 地龙

```bash
# Windows 上建立 SSH 反向隧道
ssh -N -R 19999:127.0.0.1:18789 ubuntu@<腾讯云IP> -i <SSH密钥>
```

```bash
# 天龙在腾讯云上通过反向隧道推送
OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=1 openclaw gateway call \
  --url ws://127.0.0.1:19999 \
  --token <地龙GatewayToken> \
  sessions.send \
  --params '{"key":"agent:main:dashboard:<地龙SessionID>","message":"消息"}'
```

**关键注意事项**：
- 必须设置 `OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=1` 允许 WS 明文连接
- Token 必须用**地龙**的 gateway token，不是天龙的
- 反向隧道端口（19999）映射到地龙 gateway 端口（18789）

### 3.4 推送脚本

**天龙 → 地龙** (`push-to-dilong.sh`)：
```bash
#!/bin/bash
MSG="$1"
DILONG_URL="ws://127.0.0.1:19999"
DILONG_TOKEN="<地龙Token>"
DILONG_SESSION="agent:main:dashboard:<SessionID>"

PARAMS=$(python3 -c "import json; print(json.dumps({'key':'$DILONG_SESSION','message':'''$MSG'''}))")
OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=1 openclaw gateway call \
  --url "$DILONG_URL" --token "$DILONG_TOKEN" sessions.send --params "$PARAMS"
```

**地龙 → 天龙** (`tl_send.py`)：直接 SSH 到云端用 Python 调本地 gateway call。

## 四、铁律体系

经过秋哥批准，四条铁律确立：

### 铁律一：身份分明
- 🦞 天龙 = 技术口/运维/开发，腾讯云
- 🐉 地龙 = 日常口，Windows 本地
- 👑 秋哥 = 唯一的人类主人

### 铁律二：互推前缀规范
| 方向 | 前缀 |
|------|------|
| 天龙→地龙 | `[天龙→地龙] 🦞天龙：` |
| 地龙→天龙 | `[地龙→天龙] 🐉地龙：` |
| 秋哥说话 | 不加前缀 |

### 铁律三：模型使用规范
- 日常聊天 → `deepseek-chat`（默认）
- 复杂任务 → `deepseek-v4-pro`
- 图片识别 → 千问 `qwen-vl-max`
- 节约令牌，不滥用 pro

### 铁律四：平台不越界
- 天龙管 Linux 云端事务
- 地龙管 Windows 本地事务
- 想法对齐，执行自主，命令语法各用各的

## 五、COS 灵魂备份体系

两条龙的完整灵魂文件存储在腾讯云 COS 对象存储中：

```
/mnt/cos/
├── 天龙/              # 天龙核心文件
│   ├── openclaw.json  # 完整配置（含 token）
│   ├── MEMORY.md      # 记忆
│   ├── SOUL.md        # 人设
│   ├── IDENTITY.md    # 身份
│   ├── AGENTS.md      # 行为规则
│   ├── USER.md        # 主人信息
│   └── models.json    # 模型配置
├── 地龙/              # 地龙核心文件（同上结构）
└── 天地双龙工作空间/   # 共享工具脚本
```

**恢复流程**：确认 COS 可访问 → 拷贝文件回 `~/.openclaw/` → `openclaw gateway restart` → 通知对方。

**同步纪律**：每次修改核心文件后立即同步到 COS 对应目录。

## 六、身份边界铁律

- **秋哥** = 又见炊烟老板，主人
- **张婧** = 又见炊烟老板娘，秋哥妻子
- 秋哥的龙 ≠ 张婧的龙
- 秋哥的 COS ≠ 张婧的 COS
- 老板和老板娘界限分明，不可混淆

涉及张婧的事务必须分清边界：这是老板的事还是老板娘的事。

## 七、关键踩坑记录

| 问题 | 解决方案 |
|------|----------|
| PowerShell JSON 转义地狱 | 放弃直接在 PS 里拼接 JSON，改用 Python 脚本 |
| gateway call 跨实例 token mismatch | SSH 到目标服务器本地执行 gateway call |
| SSH 隧道连接断开 | 使用 `ServerAliveInterval=30` 保持心跳 |
| 中文跨系统编码乱码 | 使用 Python json.dumps 确保 UTF-8 编码 |
| HTTP vs WS 协议 | 有些场景 HTTP 超时，WS 更稳定 |
| sessions.send 跨 session tree 限制 | 直接 SSH 到同一个 gateway 主机调用 |

## 八、待完善：稳定性增强

1. **隧道守护脚本**：检测 SSH 隧道断开自动重连
2. **双向心跳检测**：每 N 分钟互发 ping/pong，超时告警
3. **操作前风险评估**：危险操作前备份 + 回滚方案
4. **定时灵魂同步**：cron 定时把核心文件同步到 COS

## 九、给张婧的建议

如果老板娘要为你的两条龙搭建类似体系，参考以下步骤：

1. **确认环境**：两端都有 OpenClaw Gateway 运行
2. **获取信息**：双方的 SSH 密钥、Gateway token、Session ID
3. **建立隧道**：同上方案，一条本地转发 + 一条反向隧道
4. **部署脚本**：在云服务器上部署 `push-to-dilong.sh`
5. **建立 COS 备份**：在 COS 上创建对应文件夹，存放核心文件
6. **刻录铁律**：根据实际情况定制互推规则，写入 MEMORY.md
7. **测试验证**：两端互发消息确认秒级到达

---

🐉 地龙 撰
2026-07-22
