# 又见炊烟餐饮管理系统 — 三工具 Git 仓库完整约束规范

> 天龙 / 地龙 / Trae 共用仓库，机器强制执行，不靠 AI 自觉。

---

## 一、身份划分

| 工具 | Git 用户名 | 邮箱 | 职责 |
|------|-----------|------|------|
| 天龙 | TL-BOT | tl@project.local | 需求文档、业务清单、md 说明、测试用例、报表文字描述 |
| 地龙 | DL-BOT | dl@project.local | 后端 Python、数据库校验 SQL、存储过程、接口逻辑、系统体检脚本 |
| Trae | TRAE-BOT | trae@project.local | Vue 前端、路由、菜单、页面、看板 UI |

## 二、职责隔离（禁止越界）

### 天龙
- ✅ 只改：需求文档、业务清单、md 说明、测试用例、报表文字
- ❌ 禁止：改动 .py/.vue/.js/SQL 代码

### 地龙
- ✅ 只改：后端 Python、数据库校验 SQL、存储过程、接口逻辑、系统体检脚本
- ❌ 禁止：前端 Vue、路由菜单、页面样式

### Trae
- ✅ 只改：Vue 组件、路由 js、页面看板、CSS、前端接口调用
- ❌ 禁止：后端业务脚本、数据库校验逻辑

## 三、分支强制规则

- **main 主分支全局锁定**：三工具一律不允许直接 push main，仅人工审核 PR 后合入
- **禁止 `git push -f`** 强制覆盖
- **专属功能分支前缀**：
  - 天龙：`feat-tl-xxx`
  - 地龙：`feat-dl-xxx`
  - Trae：`feat-trae-xxx`
- 跨模块修改必须新建对应工具分支 + 提交人工审核

## 四、提交规范

### 提交文案强制模板
```
【工具名】模块-操作：简要改动内容
```
示例：
- `【地龙】系统体检-修复HTML按钮转义BUG`
- `【Trae】菜单管理-新增沽清管控页面路由`
- `【天龙】业务文档-补充16大业务闭环检测清单`

不标注工具标识的提交，钩子直接拦截。

### 提交内容限制
- 每次提交只能做单一改动，禁止几百文件一次性打包
- 删除类改动必须在备注写明删除原因
- 单次提交删除源码文件不得超过 5 个
- 禁止提交明文账号密码、密钥
- AI 生成代码必须删测试打印、无用注释、冗余调试代码再提交
- 禁止 AI 自动格式化全仓库代码

## 五、三大工具专属禁令

### 地龙禁令
1. 禁止在代码内写入 rm 批量删除逻辑
2. 仅允许操作后端目录，禁止改动 frontend_v3 前端文件夹
3. 批量数据库校验 SQL 必须附带异常兜底
4. 生成 py 文件后必须自检缩进、转义、闪退问题再提交
5. 大批量改动拆分多次提交

### Trae 禁令
1. 禁止擅自全局删除 .vue 组件
2. 仅操作前端目录，禁止修改后端 Python、SQL
3. 路由菜单改动必须同步更新 activeModule 匹配规则
4. JS 内禁止多层未转义引号
5. 页面批量重构拆分多次提交

### 天龙禁令
1. 完全禁止修改任何代码类文件
2. 不得篡改数据库/前后端逻辑描述
3. 梳理完检测清单后同步标注对应地龙后端校验编号

## 六、钩子强制拦截

### pre-commit（提交前）
- 检测批量删除命令（rm/rmdir/del/erase），直接阻断
- 单次删除源码超过 5 个文件，阻断
- 禁止提交明文账号密码密钥
- 地龙不能提交前端文件、Trae 不能提交后端脚本，越界拦截
- 代码语法错误禁止提交

### prepare-commit-msg（提交文案）
- 强制【工具名】前缀格式
- 不标注天龙/地龙/Trae 的提交直接拦截

### pre-push（推送前）
- 任何工具 push 到 main 分支，直接终止
- 只能推送自身 feat 分支

## 七、兜底恢复
- 本地 5 分钟自动生成临时 Git 快照
- 远程仓库永久保存全部提交，任意版本一键回滚
- 禁止 `git push -f`，历史不可覆盖
- 每周自动打包仓库完整备份

---

*最后更新：2026-07-31 · 秋哥钦定*
