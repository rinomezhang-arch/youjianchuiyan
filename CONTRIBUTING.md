# CONTRIBUTING.md — 三工具 Git 仓库完整约束规范

> ⚠️ 本文件为机器可读规则，天龙/地龙/Trae 三工具必须严格遵守。
> 违反规则的操作会被 Git Hook 直接阻断，无法提交。

---

## 一、身份划分

| 工具 | Git user.name | Git user.email | 职责范围 |
|------|--------------|----------------|---------|
| **天龙** (OpenClaw) | TL-BOT | tl@project.local | 需求文档、业务清单、md说明、测试用例、报表文字描述 |
| **地龙** (后端) | DL-BOT | dl@project.local | 后端Java、数据库校验SQL、接口逻辑、系统体检脚本 |
| **Trae** (前端) | TRAE-BOT | trae@project.local | Vue组件、路由JS、页面看板、CSS、前端接口调用 |

### 目录边界（铁律）

```
天龙 (TL-BOT) 只能修改：
  *.md                      — Markdown文档
  开发详情/                   — 开发规划手册
  体检/                      — 系统体检脚本（Python）

地龙 (DL-BOT) 只能修改：
  banquet_project/           — 后端Java项目（全部）

Trae (TRAE-BOT) 只能修改：
  frontend_v3/               — 前端Vue项目（全部）
```

---

## 二、分支强制规则

### main 分支全局锁定
- 三个工具**一律不允许**直接 push main
- 仅人工审核 PR 后合入
- 禁止 `git push -f` 强制覆盖提交

### 专属功能分支前缀
- 天龙：`feat-tl-*`
- 地龙：`feat-dl-*`
- Trae：`feat-trae-*`

所有代码改动，工具只能在自己前缀分支操作。

### 跨模块修改限制
若需修改对方负责文件，必须新建对应工具分支 + 提交人工审核，不允许私自覆盖。

---

## 三、Git Hook 强制拦截

### 3.1 pre-commit —— 提交前拦截
1. 检测代码内 `rm`/`rmdir`/`del`/`erase` 批量删除命令，直接阻断提交
2. 单次提交删除源码（.java/.vue/.py/.js）超过 **5** 个文件，阻断
3. 禁止提交明文账号、数据库密码、密钥
4. 地龙不能提交前端文件、Trae不能提交后端脚本，**越界直接拦截**
5. 代码语法错误直接禁止提交
6. 禁止一次性全仓库格式化、批量乱改缩进

### 3.2 prepare-commit-msg —— 提交文案强制模板
```
【工具名】模块-操作：简要改动内容
```
示例：
- `【天龙】业务文档-补充16大业务闭环检测清单`
- `【地龙】系统体检-修复HTML按钮转义BUG`
- `【Trae】菜单管理-新增沽清管控页面路由`

不标注【天龙】/【地龙】/【Trae】的提交，直接拦截。

### 3.3 pre-push —— 推送前拦截
任何工具尝试 push 到 main 分支，直接终止推送，只能推送自身 feat 分支。

---

## 四、专属禁令

### 天龙（TL-BOT）
1. ❌ 完全禁止修改任何代码类文件（.java/.vue/.py/.js/.sql等）
2. ✅ 只改：需求文档、业务清单、md说明、测试用例、报表文字描述
3. ❌ 不得篡改数据库/前后端逻辑描述
4. ✅ 梳理完检测清单后，同步标注对应地龙后端校验编号

### 地龙（DL-BOT）
1. ❌ 禁止在代码内写入 `rm` 批量删除逻辑
2. ✅ 本地执行删除文件必须先备份
3. ❌ 仅允许操作 banquet_project 后端目录，禁止改动 frontend_v3 前端文件夹
4. ❌ 批量数据库校验SQL必须附带异常兜底，禁止执行无限制 DROP/DELETE
5. ✅ 生成Java文件后必须自检编译再提交
6. ❌ 大批量业务闭环检测项新增，拆分多次提交，禁止一次性打包提交

### Trae（TRAE-BOT）
1. ❌ 禁止擅自全局删除 .vue 组件，删除页面单次不超3个
2. ❌ 仅操作 frontend_v3 前端目录，禁止修改后端Java、SQL
3. ✅ 路由菜单改动必须同步更新 activeModule 匹配规则
4. ❌ JS内禁止多层未转义引号
5. ✅ 页面批量重构拆分多次提交

---

## 五、提交内容硬性规范

1. 每次提交只能做单一改动
2. 删除类改动必须在提交备注写明删除原因
3. 批量删除源码必须人工确认后才放行
4. AI生成代码必须删除测试打印、无用注释、冗余调试代码再提交
5. 禁止AI自动格式化全仓库代码
6. 单次提交删除源码文件数量不得超过5个

---

## 六、远程仓库配置

- main 分支保护：关闭直接推送，所有变更走PR人工审核
- 禁用 `git push -f`
- 开启文件变更告警
- 永久保存全部提交记录
- 每周自动打包仓库完整备份

---

## 七、兜底恢复机制

- 本地5分钟自动生成临时Git快照
- 远程仓库全历史留存
- 所有删除操作提交记录永久留存，可追溯

---

## 八、落地执行清单

- [x] 仓库根目录新建 CONTRIBUTING.md
- [x] 配置三套 Git 本地钩子（pre-commit / prepare-commit-msg / pre-push）
- [x] 天龙配置专属 Git 用户名邮箱（TL-BOT / tl@project.local）
- [ ] 远程后台锁定 main 分支，关闭强制推送权限
- [ ] 地龙/ Trae 分别配置专属 Git 用户名邮箱
- [ ] 后续所有改动，仅允许创建自身前缀分支，提交必须带工具标识文案
