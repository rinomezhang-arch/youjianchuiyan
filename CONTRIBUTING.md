# 又见炊烟餐饮管理系统 - Git 协作规范

## 一、身份划分（提交日志一眼区分，永久溯源）

| 工具 | user.name | user.email | 职责范围 |
|------|-----------|------------|----------|
| 天龙 | TL-BOT | tl@project.local | 需求文档、业务清单、md说明、测试用例、报表文字描述 |
| 地龙 | DL-BOT | dl@project.local | 后端Python、数据库校验SQL、存储过程、接口逻辑、系统体检脚本 |
| Trae | TRAE-BOT | trae@project.local | Vue组件、路由js、页面看板、CSS、前端接口调用 |
| 人工 | rinomezhang-arch | rinomezhang@gmail.com | 代码审核、分支合并、主分支操作 |

## 二、职责隔离（禁止越界修改）

### 天龙（TL-BOT）
- **只改**：需求文档、业务清单、md说明、测试用例、报表文字描述
- **禁止**：改动 .py / .vue / .js / 库表 SQL 代码

### 地龙（DL-BOT）
- **只改**：后端Python、数据库校验SQL、存储过程、接口逻辑、系统体检脚本
- **禁止**：前端Vue、路由菜单、页面样式

### Trae（TRAE-BOT）
- **只改**：Vue组件、路由js、页面看板、CSS、前端接口调用
- **禁止**：后端业务脚本、数据库校验逻辑

## 三、分支强制规则

### main 主分支全局锁定
- 三个工具一律不允许直接 push main
- 仅人工审核 PR 后合入
- 禁止 `git push -f` 强制覆盖提交

### 专属功能分支前缀
- 天龙：`feat-tl-xxx`
- 地龙：`feat-dl-xxx`
- Trae：`feat-trae-xxx`
- 所有代码改动，工具只能在自己前缀分支操作

### 跨模块修改限制
- 若需修改对方负责文件，必须新建对应工具分支 + 提交人工审核
- 不允许私自覆盖

## 四、提交内容硬性规范

### 提交格式（强制）
```
【工具名】模块-操作：简要改动内容
```
示例：
- `【地龙】系统体检-修复HTML按钮转义BUG`
- `【Trae】菜单管理-新增沽清管控页面路由`
- `【天龙】业务文档-补充16大业务闭环检测清单`

不标注工具名的提交，直接拦截无法提交。

### 提交粒度
- 每次提交只能做单一改动：修复一个bug / 新增一个页面
- 禁止几百个文件一次性打包提交
- 删除类改动必须在提交备注写明删除原因
- 批量删除源码必须人工确认后才放行

### 代码质量
- AI生成代码必须删除测试打印、无用注释、冗余调试代码再提交
- 禁止AI自动格式化全仓库代码（只允许改动当前业务文件）

## 五、Git Hook 强制拦截（机器无法跳过）

### pre-commit 提交拦截
- 检测 `rm` / `rmdir` / `del` / `erase` 批量删除命令，直接阻断
- 单次提交删除源码（.vue/.py）超过5个文件，阻断
- 禁止提交明文账号、数据库密码、密钥
- 地龙不能提交前端文件、Trae不能提交后端脚本，越界直接拦截
- 代码语法错误（python缩进报错、vue标签未闭合）直接禁止提交
- 禁止一次性全仓库格式化、批量乱改缩进

### prepare-commit-msg 提交文案强制模板
- 统一格式，不达标直接阻断
- 必须带【工具名】标识

### pre-push 推送拦截
- 任何工具尝试 push 到 main 分支，直接终止推送
- 只能推送自身 feat 分支

## 六、远程仓库配套规则

- 保护 main 分支：关闭直接推送，所有变更走 PR 人工审核
- 禁用强制推送 `git push -f`
- 开启文件变更告警：任一工具一次性删除大量源码立刻推送通知
- 永久保存全部提交记录，任何误删可任意版本回滚
- 每周自动打包仓库完整备份

## 七、兜底恢复机制

- 本地定时自动生成临时 Git 快照
- 远程仓库全历史留存，一条命令 `git checkout <提交哈希>` 复原
- 所有工具删除操作提交记录永久留存，可追溯是谁、何时删的文件

## 八、专属禁令

### 地龙（DL-BOT）
1. 禁止在代码内写入 rm 批量删除逻辑；本地执行删除文件必须先备份
2. 仅允许操作后端目录，禁止改动 frontend_v3 前端文件夹
3. 批量数据库校验SQL必须附带异常兜底，禁止执行无限制 DROP/DELETE
4. 生成py文件后必须自检缩进、转义、闪退问题再提交
5. 大批量业务闭环检测项新增，拆分多次提交

### Trae（TRAE-BOT）
1. 禁止擅自全局删除 .vue 组件，删除页面单次不超3个
2. 仅操作前端目录，禁止修改后端体检Python、库校验SQL
3. 路由菜单改动必须同步更新 activeModule 匹配规则，避免页面无高亮
4. JS内禁止多层未转义引号，生成页面自带打印适配逻辑
5. 页面批量重构拆分多次提交，防止PR冲突

### 天龙（TL-BOT）
1. 完全禁止修改任何代码类文件
2. 业务清单、流程文档更新，不得篡改数据库/前后端逻辑描述
3. 梳理完检测清单后，同步标注对应地龙后端校验编号

## 九、落地执行步骤

1. 仓库根目录新建 CONTRIBUTING.md（本文档）
2. 配置三套 git 本地钩子放入 .git/hooks
3. 远程后台锁定 main 分支，关闭强制推送权限
4. 天龙/地龙/Trae 分别配置专属 Git 用户名邮箱
5. 后续所有改动，三个工具仅允许创建自身前缀分支，提交必须带工具标识文案
