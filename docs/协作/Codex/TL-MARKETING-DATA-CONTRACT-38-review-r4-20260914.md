# TL-MARKETING-DATA-CONTRACT-38 R4 技术验收

结论：REVIEWED。

- 固定验收头：`ad31f715726d693a4d1324e8829d0182dca5842e`；R4 实现提交：`1bcd78d92a4df74b9b70a000072515d02152ded1`；远端分支回读一致。
- 触发器预检已按名字扫描全 schema，并将目标表、事件和时机纳入拒绝条件；同名触发器落在错误表时会在第一条 DDL 前失败。
- 更新触发器预检已核对 18 个快照字段，并确认 `status`、`paused_by`、`paused_at` 不在快照保护条件中；保护字段缺失的同文案触发器会在第一条 DDL 前失败。
- R4 定向证据为 5 PASS、0 FAIL：全新 schema 首次迁移和重放成功；错误表同名触发器、保护字段不全触发器均被拒绝且结构前后一致。
- `git diff --check c6794bb5..ad31f715` 通过；改动位于迁移、TL38 验证脚本和天龙报告路径，未涉及 Java、Vue、配置、部署或法务。

限制：预检依赖 MySQL 8.0 当前 `ACTION_STATEMENT` 文本格式，未来数据库版本或触发器体格式变化时可能保守拒绝并要求人工核对；这是失败关闭，不会静默放行。部署执行器必须使用 `//` 分隔，命令行为 `mysql --delimiter=//`，Spring ScriptUtils 为 `separator="//"`。

本结论只接受数据库迁移候选及隔离库证据，不代表营销真实后端接口、生产迁移或部署已完成。
