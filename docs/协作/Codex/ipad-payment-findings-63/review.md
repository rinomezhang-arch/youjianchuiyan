# CO60 技术退回与 CL61 复核验收

- 执行人：Codex；时间：2026-09-15（北京时间，继承20260914工作轮次）。
- 固定候选：e226289f0bc0e77fc6fbbfd74cddc159d2cdb532。
- CL61：复核工作 reviewed；两项业务缺口已由统筹运行实际组件逻辑确认。此状态不代表CO60产品通过。
- CO60：CHANGES_REQUESTED，候选不允许并入发布包。

## 真实执行结果

命令：node docs/协作/Codex/ipad-payment-findings-63/reproduce.mjs F:/solo/artifacts/team-worktrees/codex-ipad-account-recovery-60

证据：同目录 reproduce.mjs、before.json；0 PASS / 3 FAIL，退出码1。加载实际候选Vue脚本和既有夹具，延迟返回合成支付响应；未连接真实支付、后端或数据库，不算端到端验收。

1. 旧店支付成功晚到：当前二店所选账户31变为null，执行clearCart和跳转/ipad/home。应当均不发生。
2. 页面卸载后旧支付成功：仍执行clearCart和跳转。应当均不发生。
3. 应付100，提交现金实收200，响应前将现金输入改300：提交仍为200，但弹窗找零为200；应为100。

## 定位与方法

- PaySelect.vue:244-309：confirmPay成功、失败、catch、finally均需支付请求所属门店/预订及组件生命周期的版本守卫，不能仅比较门店ID，须覆盖A-B-A和卸载。
- PaySelect.vue:67、129、279-283：所有可变支付控件在请求过程中锁定；方法、现金实收、应付和找零必须使用同一次不可变提交快照。弹窗显示确认快照，不能读取实时输入。
- 原15项已有断言保留；增加真实延迟响应下的成功/失败/卸载、A-B-A以及现金快照检查，修复后针对性运行一次并构建一次。
- 网络结果未知后的幂等重试须遵守实际后端契约；不得通过换新key绕过未知支付结果。后端核对另行给出。

## 历史归因更正

ccf24b31基线已存在可修改现金输入和读取实时changeAmount的找零弹窗。CL61说此现金问题不是基线已有，归因不准确；本次新候选延续该问题，仍必须修复。不能据此宣称后端已错误扣款。原11 PASS/4 FAIL到15 PASS/0 FAIL的账户加载回归与构建证据仍有效，仅未覆盖支付响应及现金快照。

## 边界

未改业务代码、生产或法务。CO60工作树冻结，由后续Trae修复卡独占PaySelect.vue与相关测试。当前只完成复现和退回，不声称已修复。

## 追加勘误与执行调整（2026-09-14 21:38）
- 首条人工日期误写为2026-09-15，实际为2026-09-14，时间以任务板事件UTC为准。保留原记录，不改写历史。
- Trae精确会话当前不可达，不再新增TR63卡；CO60由内部执行代理按原归属续修，Trae仅保留TR62整合任务。前文未来Trae修复卡设想取消。
