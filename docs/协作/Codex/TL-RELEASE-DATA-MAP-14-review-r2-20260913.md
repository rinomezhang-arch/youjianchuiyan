# TL-RELEASE-DATA-MAP-14 技术验收 R2

结论：`changes_requested`。固定基线已经取得，但报告的行号、缺口计数和证据时效仍不足以供发布门禁直接使用。

## 已核证

- 固定基线 `24c3c611f1bf48796dfb2558dbc01758fcd260c6` 可复现，且位于目标集成分支历史中。
- 报告未修改业务代码、生产、配置或法务内容。

## 必须修正

1. 工资审批实际调用位于 `PayrollController.java` 247 行；246 行只是 `try`。迁移脚本第 44 行是 `month_salary` 指向发放台账的外键，不属于 `payroll_payout_record` 自身关系。修正映射和行号。
2. 第 4 项已确认在固定基线不存在，不能继续写成七个缺口。第 2 项只是分层观察，第 7 项只是环境限制；没有业务反例时不得列作已证生产缺口。
3. 应付来源以实体 52、55 行及迁移 6 至 11 行为直接依据；旧证据显示 `source_receipt_id` 与 `source_receipt_no` 均缺失，不能在两处各漏写一列。
4. `stock_take_detail=0` 时关联检查必须标 `NOT_COVERED`。只能陈述旧快照存在两张 completed 空主单，不能由空表推断当前明细落盘链未走通。
5. 所有旧生产证据标明 9 月 8 日或 9 月 9 日采集时间；没有本轮生产复核就明确写“当前未复核”，不得描述成 9 月 13 日现状。

只修文档和只读证据，不重新全盘扫描，不连接生产，不碰法务。完成后报告精确提交与六项或更少的可复核结论。
