# 生产交付决策与执行分工

当前决定：不可发布。工资、盘点、收款和候选与线上比对仍有待验收项；不是只补截图即可交付全系统。

Codex 负责场景、数据链、范围裁决、候选整合决定和最终验收；Trae 负责实现、构建、证据整理、候选准备和获准后的发布执行；Claude 负责关键语义复核；天地龙执行限定的真实链验证。一个文件同时一个负责人。

## 经营场景与必须证明的结果

| 场景 | 实际操作与结果 | 数据与失败约束 | 当前推进 |
| --- | --- | --- | --- |
| 每日买货 | 采购、验收入库、库存数量金额、供应商应付和付款能按单据追踪 | 页面状态与单据、明细、库存及资金流水一致；不能用采购创建代替验收入库 | TR62盘清已验收证据和真实缺口，由Codex拆最小修复 |
| 门店盘点 | 页面录入实盘、提交、刷新、查询/打印能找到同一盘点单 | stock_take与stock_take_detail主从完整；同店原料；HALF_UP金额与主单合计一致；禁止空完成单 | DL53待真实页面提交成功证据，旧HTTP代理证据不能替代 |
| 发工资 | 核算保存、批复、付款、刷新后状态金额一致 | 工资、审批、付款及资金流水可勾稽；重复付款零新增流水 | TL59为Linux专属环境续作，任务未认领，不算在办 |
| 收款 | 选择本店账户收款，切店、失败重试、现金找零行为正确 | 账户白名单；旧请求不能回写；幂等键与支付成功状态一致；真实资金流水需后续复验 | CO60候选e226289f的15项仅覆盖账户加载；CL61复核完成，统筹新增3场景全部复现失败，CO60已退回续修。CL04/CL05账户后端漏整合，CO63选择性接入；二者须同版本联测 |
| 客人看活动与咨询 | 公开活动进入、提交咨询、拿到咨询号、回查与刷新 | 同requestId同载荷返回同号且两表零新增；错误条件不泄漏客户信息 | TR58重放已Codex实测200/INQ3及delta=0；Trae只补截图和最终报告 |

## 发布闸门

1. 形成当前精确SHA的单一manifest，每条需求写已执行证据或未覆盖，不把历史版本成功算作当前候选成功。
2. 真实登录与批复身份必须核实；张律师原权限与法务内容冻结，不向其数据写测试记录。
3. 只读获取当前生产源码/JAR基线，逐文件忽略CRLF差异比对，保留线上较新代码。不得仅凭本地构建覆盖生产。
4. 明确迁移预检、备份恢复和回退入口；受保护内容用hash证明保留。
5. Codex核定缺口关闭后才给实际部署指令，Trae执行发布并回报部署hash、健康和真实业务回读。此前所有“候选成功”不等于生产交付。

禁止重复消耗：不复跑TR58整套浏览器和API链，不复跑CO60的15项测试；只有独立复核发现新问题或候选变化影响既有证明时才追加定向验证。阻断立即上板，由统筹裁决，不空转排查模型或账户余额。

## 2026-09-14 21:45 收款具体修复决定

1. 收银等待响应时切店、切预订或离开页面，旧响应不得清理当前购物车/账户、跳转或刷新当前账户列表；采用上下文递增版本，同时约束成功、失败和finally。只比较门店ID不能覆盖A-B-A。
2. 应付100、现金实收200的该笔找零固定为100。所有金额、方式、混合明细按提交快照处理；请求在飞或待确认时锁定变更，确认弹窗不能读实时输入。
3. 当前后端幂等重放只按key和booking返回旧结果，不校验完整payload。网络结果未知时仅可用原key和原payload重试；明确零写入的账户拒绝才允许重选。前端不能用新key绕开未知结果。
4. 后端现有CL04/CL05已reviewed，分别e792256f/aab6d59a；只合账户逻辑。当前related_id必须保留，pay方法Transactional不能随插入账户列表方法丢失。禁止整文件复制旧控制器。
5. 新组件回归和新后端单元测试证明各自逻辑，不能替代同候选真实HTTP、资金流水账户及订单关联回读。

执行事实：CO60由内部代理修前端，CO63由内部代理整合后端；Trae指定会话入口当前不可达，TR62仍ready，未宣称已开工。CL61复核任务reviewed仅代表复核交付完成，CO60候选仍需重新技术验收。
