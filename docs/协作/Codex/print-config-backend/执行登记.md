# PRINT-CONFIG-BACKEND 意图

当前收口状态：父代理已精确提交四个打印文件，全部冻结，不再修改。最新打印结果7项真实MySQL通过，同行Ipad4项通过。最新7项证据同时归入evidence.json；旧6项证据另存evidence-initial-6.json，不抹除历史。以下旧6项段落仅是早期轮次记录，以此状态和evidence-store-fk.json为准。此次仅收口文档/证据，无Maven。

父代理复核修订意图：补门店存在与FK。只读现有生产元数据夹具及StoreInfo/PublicStoreController确认真实表为store_info（非store_master），主键store_id为有符号BIGINT AUTO_INCREMENT；不新造store_master。测试抽取既有restaurant-production-schema中完整store_info DDL，仅种合成必填列，禁止用简化假表绕过真实约束。服务scope在授权后统一校验存在；新打印机/规则表均引用store_info，新增GM不存在门店及直接DB孤儿拒绝用例。旧隔离库保留，不改生产或共享实体。

FK首跑结果：7项均在store_info建表因其manager_id引用staff_master未创建而失败，非业务断言失败。补读取原restaurant-production-schema自带staff_master外部主键stub（无任何员工数据），不移除store_info真实外键、不简化store_info。maven-store-fk.log及失败库保留。父代理授权下一轮合跑RestaurantPrintMysqlTest,IpadOrderScopeTest，单Maven顺序执行。

FK修订最终：22:18:47单Maven合跑成功，打印7项＋父代理Ipad4项＝11项、0失败/错误/跳过，1分10秒、退出0，已通知释放。maven-store-fk-ipad.log及evidence-store-fk.json为最新证据，旧6项evidence.json保留历史但哈希不再代表修订版。

门店关联修复：授权后的常规scope统一查store_info存在；GM正整数不存在及普通账号不存在门店返回400“门店不存在”，查询/保存均拒绝。两张新表store_id均以有符号BIGINT外键引用实际store_info主键，并保留规则→打印机同店复合FK。迁移可重复运行并尝试为旧无门店FK的候选表补约束；旧孤儿会让DDL失败，不自动删/改旧数据。该旧候选升级分支尚未单独做已有数据升级演练，新库连续执行两次已经验证。

fixture来源：直接提取现有restaurant-production-schema-20260906.sql完整store_info DDL，包含真实列类型、必填、索引及manager_id外键；其staff_master只有原夹具外部主键stub，无员工数据，不声称全staff生产结构。仅插合成store_code/name及ID1/2，未查询生产门店数据。新增第7项验证GM不存在门店POST/GET均拒绝、规则不写、直接JDBC写孤儿被FK拒绝、打印机/规则0行及孤儿0、两条store_info FK实际存在。

四文件仍在本任务ownership内：Controller未变，Service/迁移/测试修订；无共享实体或生产修改，无stage/commit。契约已补门店必须存在。等待父代理读diff验收。

2026-09-07 Codex子代理。只新增RestaurantPrintController、RestaurantPrintService、restaurant_print_config_v1.sql、RestaurantPrintMysqlTest及本目录。持久配置与真实复合外键；所有范围由真实UserContext校验；不修改现有类、JWT/系统配置/前端/法务，不连接打印设备、不生产迁移、不stage/commit。

先固定JSON契约，Jdbc事务写配置与归档。未知字段拒绝，打印机被未归档规则引用时不得归档；规则只能引用同店未归档打印机。响应设备始终unverified、规则effectiveEnabled=false。独立MySQL迁移+事务测试，每例保留，不物理删除。Maven等待父代理明确释放，当前未运行。

父代理释放Maven后首跑5项4过1失败：中文回读断言测试使用MockHttpServletResponse默认字符解码导致乱码。只修改测试用ObjectMapper解析响应原始字节（JSON UTF8），不改业务或编码配置；补非法规则及重复规则更新用例，复跑新日志。首跑maven-first.log和库保留。

最终：22:11:36复跑RestaurantPrintMysqlTest 6项全过、0失败/错误/跳过，Maven45.587秒、退出0，已通知父代理释放Maven。只改测试解码后中文原名称回读成功，证实首跑为测试解码问题。日志maven-rerun.log；evidence.json包含用例、保留库、四个交付文件SHA256。

实现：新迁移仅创建restaurant_print_printer和restaurant_print_rule，真实同店复合FK及类型/纸宽/份数CHECK；Jdbc参数绑定、同店ID更新、事务保存、归档而非删除。打印机活跃引用阻止归档，归档规则保留历史引用，已归档记录禁止普通更新恢复。未知字段、非法规则、伪造服务端状态字段拒绝。GM显式有效门店，无身份/门店及他店fail-closed。异常本地映射不泄漏SQL。

验证链：真实JwtAuthInterceptor＋StoreDataScopeAspect/AuditLogAspect＋Controller＋事务Service＋本机隔离MySQL，每例新库、实际迁移每例执行两遍且保留。测试覆盖重建Spring服务上下文后回读；打印机/规则重复更新不增行；同店复合FK直接DB拒跨店；先规则后打印机归档且行数保留；未知字段/非法type/纸宽/份数/名称/规则类型/布尔值及跨店/缺身份/GM缺门店拒绝；合成AFTER UPDATE失败触发器验证事务回滚旧中文名称不变。不是重启真实网关或整机，也未做并发归档压力测试。

响应固定connectionStatus=unverified，effectiveEnabled=false/inactiveReason=device adapter not connected。未提供测试打印成功、未访问任何网络/USB/蓝牙设备。address仅存储定位字符串；receipt/kitchen/refund/daily_report是有限配置枚举，执行器映射待后续合同。完整WebMvcConfig其他拦截器/旧前端/打印任务调度/设备回执/真实纸张出单未验收。新表未在生产执行，业务规则修改历史由现有审计记录，此阶段不提供版本历史查询API。

交付：四个新代码/迁移/测试文件及本目录；API契约.md可给Trae。无stage/commit，无旧类、核心配置、权限、生产、法务或身份候选修改。父代理精确复核后合入，本子代理等待。
