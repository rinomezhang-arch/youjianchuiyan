# PAYABLE-HTTP 意图

2026-09-07 Codex子代理，基线190c1878。只新增PayableHttpMysqlFlowTest.java和本目录。真实JWT拦截器及GET/POST身份切面、真实FinancePayableController/Service/JPA事务，每例独立MySQL库保留，仅两实体DDL及合成audit_logs。仓储夹具创建原应付，不测试尚未合入的HTTP创建合同。不手工UserContext，不改业务/配置/生产、不stage/commit。父代理让出Maven，本轮仅跑新类。

验证范围：结算→HTTPGET流水→同key重试、已付+待付=总额且流水和已付一致、同店关联、跨店/未登录拒绝、两HTTP请求并发同key及明确冲突重放，验证请求后线程身份清理。不是银行付款、完整生产DDL或浏览器验收。

结果：22:04首跑3项全过，0失败/错误/跳过；Maven45.629秒已结束。命令`mvn -B -o -f banquet_project/pom.xml -Dtest=PayableHttpMysqlFlowTest test`；maven-first.log保留，evidence.json记录用例对应的三个保留schema和实际并发状态。不跑旧用例、没有删除表或库。

正向证据：原应付合成1000（JPA仓储独立事务创建，不是HTTP创建），第一次HTTP结算300，HTTPGET流水返回相同结算号及storeId=1，同key重试replayed=true；同key改金额301返回实际HTTP409，余额/流水不变；新key结算700后状态paid、两笔合计1000、待付0，操作人合成身份落流水。

并发证据：两个线程经CountDownLatch同时起跑、各自实际MockMvc POST同一单同key金额400；断言恰好一个200非重放，其余仅允许200重放或409；随后同key重试必须200重放，最终流水一条、已付400。实际状态见evidence.json，不声称本轮一定触发锁冲突409（同key改金额的明确409已独立验证）。每个线程请求返回后断言UserContext为空。

负向：无JWT的POST及GET流水实际HTTP401，审计0写入；他店JWT访问单据结算及流水均实际HTTP403，已付0、流水0。全部场景检查每条流水以payable_id和store_id关联原单且无孤儿、paid+pending=total、流水sum=paid。JPA实体生成DDL不一定具有生产完整复合FK，本断言证明本次数据关联，不冒称全生产结构验收。

真实装配：FinancePayableController、FinancePayableService事务代理、两仓储、JpaTransactionManager、JwtAuthInterceptor、StoreDataScopeAspect及AuditLogAspect均实际类；未手工设置UserContext。JWT每例用内存随机秘密签发合成store_manager，未打印凭证。未装完整WebMvcConfig其余拦截器、未测真实登录/浏览器/银行付款；审计最小合成表和两个实体仅当前链路。只新测试及日志，无业务或配置变更，无stage/commit，等待父代理合入。
