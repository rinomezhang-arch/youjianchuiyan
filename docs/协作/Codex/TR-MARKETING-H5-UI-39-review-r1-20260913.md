# TR-MARKETING-H5-UI-39 R1 技术验收

- 结论：CHANGES_REQUESTED
- 验收对象：`20c0a74c4941d70c439fd0fccdc3fea9751378b6`
- 实现提交：`fafb65f0f747ac90e1f650dff84e5c6f9c0713a0`
- 范围：静态核对实现、浏览器证据和报告；未复跑整套测试。

## 必须修正

1. `frontend_v3/src/api/marketing.js:245-247` 的公开 H5 GET 没有显式禁止全局请求拦截器补 `storeId=1`。现有浏览器证据实际请求为 `/api/public/marketing/a/draft-3901?storeId=1`。请让公开读取只按 slug，不带默认门店，并更新网络证据。
2. 390x844 首屏证据显示两个可见“咨询档期”按钮，和“首屏单一主动作”不符。请保证初始视口只有一个主 CTA；页面下方可保留第二个入口，但不能同时出现在首屏。
3. 咨询成功只校验 `inquiryNo`，`lookupHref` 仍从活动 `storeId` 推导为 `/stores/1`。请成功时同时要求并保存后端返回的 `lookupUrl`；缺任一字段都不能显示成功，也不能自行拼门店地址。
4. `previewH5()` 打开尚未发布的公开地址并看到 404 后就把 `previewConfirmed=true`，没有预览待发布内容。请用当前草稿数据渲染同一套 H5 模板并让用户真实看见内容后才能解锁发布；证据继续明确标为 `MOCKED_CONTRACT`。
5. `git diff --check` 报 `vitest-run.log` 行尾空白和文件末尾多余空行；报告中的对勾字符还导致默认 GBK 任务板轮询失败。请清理证据格式，并保证新报告只使用 GBK 可编码字符。

## 已确认闭合

- 营销工作台、公开 H5 六态、咨询表单、稳定 requestId 和失败留在原页均已有实现。
- 没有物理删除入口，活动门店初值为 null；后端未接通的测试已声明为 MOCKED_CONTRACT。
- 已提供定向组件和 Playwright 证据，但现有断言把上述错误行为误计为通过，修订后只需补对应定向用例。

## 复核要求

- 不重复无关大套件；只复跑营销定向测试、390x844 浏览器证据和一次前端构建。
- 新证据必须明确证明公开 GET 无 `storeId`、首屏仅一个 CTA、成功字段来自 API、草稿预览展示真实待发布内容。
- 不改后端、迁移、配置、依赖、部署或法务。
