# PAYROLL-HTTP-MYSQL 意图

2026-09-07 Codex子代理。仅新增PayrollHttpMysqlFlowTest.java及本目录。复用payroll-metadata-fixture和实际工资迁移，每例独立随机MySQL库保留；补充HTTP读取及审计所需最小合成列/表，明确非完整生产DDL。真实PayrollController、PayrollService事务代理、Jdbc、JwtAuthInterceptor、StoreDataScopeAspect、AuditLogAspect；每例随机内存JWT密钥，实际签发合成身份token，不打印密钥/token。不手工设置UserContext，不改业务、配置或现场。

只运行新类，父代理已让出Maven；既有测试不执行。保存→HTTP回读→合成授权人审批→记账→重试，数据库台账/明细守恒；未登录及跨店拒绝。无删除夹具、无生产、无stage/commit。

首跑结果：4项中3通过，非白名单审批测试预期403但实际Result.code=400失败。代码核查PayrollRejectedException由现有控制器映射400，属于测试预期不符；真实拒绝路径已到达且状态未变。仅将该断言修正为现有400合同，明确HTTP仍200；不宣称403权限合同已实现。首跑日志maven-first.log及四个隔离库保留。复跑maven-rerun.log，仍只跑新类。

最终结果：21:56复跑4项全过，0失败/错误/跳过，Maven47.501秒。evidence.json保存用例名、耗时、对应保留合成库；不复制整个Surefire系统属性。Maven已结束，可由父代理接用。

链路实证：真实JwtAuthInterceptor验签；真实StoreDataScopeAspect处理GET身份、AuditLogAspect处理POST身份及审计；真实代理PayrollService使用DataSourceTransactionManager提交/回滚。未在测试中调用UserContext.set，且每次HTTP后验证上下文清理。

正向输入：本店合成员工2，基础1000、岗位200、出勤300、奖金40、加班50、补贴5，月2026-08；保存后数据库和HTTPGET均为实发1595，读取不重复叠加考勤/KPI，GET不含他店员工3。合成审批人synthetic_approver经白名单审批，approved_by落库；payout状态3、批次一条，重试返回alreadyRecorded且批次和明细完整快照不变。检查人数/总净额与每条payout_id关联守恒，审计行有实际操作身份。

负向：无token四个入口HTTP401且工资/审计零写入；混合本店2＋他店3保存整批回滚工资0行；签发但非白名单身份审批被业务400拒绝，工资仍状态1、无记账批次。

局限：没有跑登录接口、浏览器或完整WebMvcConfig中其他拦截器；JWT签名与白名单仅测试运行时合成，不验证真实账号。month_salary使用已有元数据夹具和真实迁移；staff_master、读取辅助表和audit_logs只是满足此链路的合成最小结构，非完整生产DDL。现有业务拒绝HTTP200＋Result.code400沿用并记录。记账不代表银行付款；未测试并发、生产部署或真实付款。

交付仅新增PayrollHttpMysqlFlowTest.java与本目录；未改业务、身份守护候选或已冻结报表文件，未stage/commit。待父代理复核合入。
