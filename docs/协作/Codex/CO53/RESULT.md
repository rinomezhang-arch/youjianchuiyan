# CO53 盘点金额精确计算

状态：针对性回归通过，可供统筹合并。基线71cd01d6b9e778213dbcf6dbf13470497549ba54，独立分支codex/stocktake-money-53。

## 最终改动

- StockTake.vue：实盘输入改为保留原文的decimal文本输入，空值不转0、超精度不被控件自动舍入；每行错误提示、汇总错误提示和提交阻断。
- utils/stockTakeMoney.js：数量3位/价格8位字符串解析、BigInt乘法，正负HALF_UP到整数分，逐行分值累加。支持JSON Number 1e-8及字符串科学计数；非法、缺失、超过DECIMAL存储范围或Number安全精度的值明确拒绝。大精度价格可传精确十进制字符串，不把金额转Number，也不使用epsilon。
- 两位金额显示统一覆盖原5处toFixed调用；预览、确认、CSV使用同一计算结果，历史/明细的无效金额显示“金额无效”，不伪装0。差异数量固定三位；提交数量规范为三位十进制字符串，字段及接口不变（后端接受字符串/数字）。
- 金额上下限按盘点物理DECIMAL校验，含系统金额、实盘金额、差额与总额；确认期间输入变化或换店须重新确认，避免显示与提交内容不同。
- 不修改后端、法务、CO51或其他协作者树；不增加依赖、不运行Java或全量Vite构建。

## 真实验证

运行：`node scripts/stocktake-money-53/test.mjs`。
使用已有安装的Vue及compiler-sfc，无安装动作。脚本默认在本独立worktree查找F:/solo/frontend_v3既有依赖；其他检出位置可将CO53_VUE_ROOT设为已有frontend_v3依赖的仓库根目录。

最终：53个针对性测试通过、0失败。DL49原三组由1通过2失败变为3通过：+1*0.015=+0.02、-1*0.015=-0.02、+1*0.335=+0.34。

覆盖：纯函数、直接执行实际StockTake.vue script setup函数（真实Vue ref/computed，HTTP/消息/浏览器下载IO桩）、实际SFC script/template编译。包括0/空/undefined、三位数量、八位价格、科学计数、正负半分、逐行合计、整数分大数、缺失/非法/超范围拒绝、提交/CSV阻断、预填清除旧金额、显示/确认/CSV一致、确认期间编辑/换店。

最终证据：test-result-1788921978357.json，含53项结果及两源码SHA256。
首轮50通过1失败test-result-1788921955665.json原样保留：测试夹具默认参数吞掉显式undefined；修正夹具后重跑通过，未伪装产品失败或隐去历史。
git diff --check通过。

## 边界

没有运行浏览器、真实HTTP/DB、全量Vite构建；本次结果不等于端到端业务验收。由父任务统一构建并交CO55继续验收。
未以Trae身份认领/落板/对外发消息，父任务管理统筹回执。
