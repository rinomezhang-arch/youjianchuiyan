# TR-MARKETING-INQUIRY-REAL-E2E-58 技术验收

## 结论

CHANGES_REQUESTED。

真实 H5、真实 Spring Boot 和隔离 MySQL 的主链证据有价值，保留，不重跑整套。当前交付还不能验收，原因如下。

## 必须修正

1. 没有固定候选。工作树仍停在基线 `e5adb15a82e762e3646ca742e3986432428b1432`，任务脚本和报告全部未跟踪，reported 也写明“无 commit”。请在独立任务分支提交允许路径内的脚本、脱敏证据和报告，推送后给出精确提交及远端最终 HEAD。
2. 没有完成“完全相同载荷重放”。`test-replay.sh:14-17` 明确把原备注改成“重放测试”，实际证明的是不同载荷返回 409。请保留该反例，另用与 INQ3 首次请求完全相同的姓名、手机号、日期、人数、备注、sourceCode 和 requestId 重放一次，断言业务 200、返回同一 inquiryNo，咨询表和事件表行数均不增。只补这一条，不重跑主链。
3. 违反禁止删除边界。`clean2.sh:4-6` 和 `cleanup.sh:5-8` 含 DELETE，reported 又写“DROP/DELETE/TRUNCATE：零（只 DELETE 自身测试数据）”，前后矛盾。禁止再次执行删除；将删除脚本移出候选并改为只读核对。新报告必须如实写明已经执行过哪些 DELETE、各自影响行数或“影响行数未知”，不得再写零 DELETE。保留隔离 schema，不做补偿性删除。
4. 证据未脱敏。报告及脚本含完整测试手机号、私网地址、客户字段和 requestId；`db-dump.sh` 还会输出完整姓名、手机号和备注。改为运行时环境变量，证据只保留脱敏值、业务 ID、计数和关系断言；私网地址使用“测试前端地址”表述。不要把口令、JWT、连接串或完整手机号写进提交。
5. 越出允许路径。`frontend_v3/.vite.e2e.config.mjs` 仍为未跟踪文件，与报告中的“测试完已删”不符。不要真删；将需要保留的配置移到 `scripts/trae-marketing-inquiry-real-e2e-58/`，最终工作树不得在允许路径外留下本任务文件。
6. 截图只有口头声明。允许目录内没有 reported 所称的成功态、空态和系统错误态截图，也没有可核对的网络与 DOM 摘要。请提交三态证据；截图不得显示完整手机号或私网地址。输入保留用脱敏 DOM 断言证明，例如 `input_retained=true`，不记录原值。

## 返工范围

- 只改 `scripts/trae-marketing-inquiry-real-e2e-58/**` 与 `docs/协作/Trae/marketing-inquiry-real-e2e-58/**`。
- 保留已经完成的真实主链、错误手机号、非法咨询号和后端不可用证据，不重复浏览器全流程，不重复 57 或 56 测试。
- 只补完全相同载荷重放、脱敏、提交固定和历史 DELETE 如实登记。
- 不改产品、迁移、配置、生产、共享进程、旧测试 schema 或法务。

