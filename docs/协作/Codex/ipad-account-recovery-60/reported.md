# CO-IPAD-ACCOUNT-RECOVERY-60 候选报告

执行人：Codex。分支：codex/ipad-account-recovery-60。
基线：ccf24b31（3191e642 上仅追加统筹验收文档）；原功能候选：f361cd5b。

## 修改与证据

- 从原候选只移植 PaySelect.vue、api/ipad.js 和专属测试三个文件，git apply 无冲突。未带入旧提交链、账号夹具、完整幂等键或旧截图。
- 新增请求序号及请求时门店比较，旧请求成功、失败都不能回写新门店账户/错误/选择；切店同步清空账户，防止同一事件循环支付旧账户；卸载时作废未完成请求。
- 专属组件测试：修复前 11 PASS / 4 FAIL；修复后 15 PASS / 0 FAIL / 0 ERROR / 0 SKIP。before-tests.txt 与 after-tests.txt 为原始输出。
- 前端构建一次成功，耗时54.19秒，退出码0；既有大 chunk 提醒仍存在。日志 build.txt。只复用既有依赖 Junction，没有安装或更改依赖。
- 旧 TR06 的30项浏览器测试属于原候选历史证据，本次没有重跑，也不将其算作新提交端到端通过。

## 未覆盖与下一步

- 当前候选真实设备/浏览器及后端复验：未执行。待独立复核后进入统一发布验收。
- Claude 独立核查切店、乱序和幂等/现金找零路径，判断是否还存在会造成误收款的缺口；不得将构建通过等同于生产可用。
- 无后端、数据库、生产、法务或配置改动。未部署。
