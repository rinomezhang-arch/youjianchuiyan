# CO-COLLAB-WATCHDOG-FIX-18 变更记录

## started

- 执行人：Codex 助手
- 基线：`318fb40549b8aba24a18e2e404215adb8b3edad8`
- 意图：修复 watchdog 验证调度、pending 状态、unknown 误激活、原子单实例与陈旧锁，并补充从主循环入口执行的夹具测试。
- 范围：仅 `scripts/agent_bridge/liveness/**` 与本目录。
- 验证：只运行 Node 夹具测试；不连接真实网关、不发送 agent 消息、不启动真实 watchdog。
- 状态：started。

## reported

- 最终提交 SHA 随任务板 reported 事件回报，避免 amend 后文档自引用失效。
- 实际改动：
  - 将 `accepted_pending_verify` 接入主循环的去重冷却定时器，验真后原位结算为 `verified` 或 `verify_failed`。
  - 同一时刻复用一个检查循环；`unknown` 只记录，不调用 `agent`。
  - 锁改为 `O_EXCL` 原子创建；活 PID 拒绝第二实例，陈旧或损坏锁改名留证后接管。
  - 恢复限流按每成员最近 30 分钟的全部 activation 记录计数；验证异常也结算失败并参与阻断。
  - 临时客户端配置移除未知 `auth.cooldowns` 且不写入 token；token 仅通过 OpenClaw 支持的 `OPENCLAW_GATEWAY_TOKEN` 子进程环境变量传递，不进入 argv。
  - 恢复验真只认本轮 `[任务标识][RESUME]` 用户消息之后的助手或工具活动。
- 测试：`node --test scripts/agent_bridge/liveness/liveness.test.mjs`，13 passed、0 failed、0 skipped。
- 静态检查：所有 liveness `.mjs` 均通过 `node --check`；`git diff --check` 通过。
- 未执行：未连接真实网关、未发送真实 `agent` 消息、未启动 watchdog、未运行生产命令。
- 遗留：需要统筹独立审查并决定是否部署；本执行方不标记 reviewed。

## round 2 reported

- 处理第二轮 `changes_requested` 的两项阻断：
  - 客户端配置不再克隆 `openclaw.json`。每个网关只生成 `{gateway:{mode:"remote",remote:{url}}}` 最小非敏感配置；省略 CLI `--url/--token`，token 继续走官方 `OPENCLAW_GATEWAY_TOKEN`。合成夹具证明 `models.providers.*.apiKey`、`channels.*.token`、`gateway.auth.token` 均未复制，原文件不变。
  - 锁先写完整随机候选文件，再用 hard link 原子发布 canonical lock，杜绝可见半写窗口。空白/损坏锁安全拒绝自动接管；有效死 PID 锁只在原子 reclaim guard 内复核并改名留证，多个回收者不能移动后来者的新有效锁。
- 官方版本静态兼容依据：OpenClaw 2026.9.2 的 CLI 仅在 CLI `--url` override 时要求 argv 显式凭据；现在 URL 来源为 `gateway.remote.url`，token 来源为官方环境变量。最小配置经该版本 `openclaw config validate` 验证通过，未连接网关。
- 测试：`node --test scripts/agent_bridge/liveness/liveness.test.mjs`，14 passed、0 failed、0 skipped；包含嵌套凭据、半写锁和双回收者确定性交错。
- 静态检查：所有 liveness `.mjs` 通过 `node --check`；`git diff --check` 通过。
- 安全边界：未读取凭证明文，未连接真实网关，未发送消息，未启动或恢复 watchdog，未修改原配置。

## round 3 reported

- `inspectMember` 的工具活动判定只读取结构化角色/类型；恢复标识后的用户正文即使含 `exec/tool/command` 也不贡献真实活动。
- `classify` 仅在当前调用失败时解析权限和额度错误；成功的历史正文含 `quota/额度` 时仍按标识返回 `delivered`。
- 新增两个精确反例并保留原有真实失败分类夹具。
- 测试：`node --test scripts/agent_bridge/liveness/liveness.test.mjs`，16 passed、0 failed、0 skipped；两个修改文件 `node --check` 通过；`git diff --check` 通过。
- 未重新检查配置或锁，未连接网关，未启动或恢复 watchdog。

## CO-COLLAB-WATCHDOG-DEPLOY-19 started

- 已确认 CO18 本体由统筹独立复跑并 reviewed。本跟进只把天龙固定恢复目标从已过时的 TL14 改为 `TL-OPS-PAYROLL-MIGRATION-CANONICAL-13` 当前三处 changes_requested。
- 准备更新天龙 marker、resumeIdempotencyKey 与固定 RESUME 文案，并补一项 agent 参数夹具，断言 task ID、marker、幂等键一致且不含旧 TL14。
- 不改调度、锁、配置或其他实现；不发送消息，不启动或恢复 watchdog。

## CO-COLLAB-WATCHDOG-DEPLOY-19 reported

- 天龙固定 marker 更新为 `CX-TL-PAYROLL13-R3-0909-01`，resumeIdempotencyKey 使用新唯一键；固定恢复文案只指向 `TL-OPS-PAYROLL-MIGRATION-CANONICAL-13` 的三处当前反例，不含旧 TL14。
- 新夹具通过实际 `activateMember` 捕获 `agent` 参数，断言消息 task ID、marker、幂等键一致。
- 测试：17 passed、0 failed、0 skipped；`git diff --check` 通过。
- 未更改 CO18 任务状态，未部署、未连接网关、未发送真实消息、未恢复 watchdog。
