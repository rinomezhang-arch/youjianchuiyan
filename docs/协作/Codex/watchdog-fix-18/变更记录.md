# CO-COLLAB-WATCHDOG-FIX-18 变更记录

## started

- 执行人：Codex 助手
- 基线：`318fb40549b8aba24a18e2e404215adb8b3edad8`
- 意图：修复 watchdog 验证调度、pending 状态、unknown 误激活、原子单实例与陈旧锁，并补充从主循环入口执行的夹具测试。
- 范围：仅 `scripts/agent_bridge/liveness/**` 与本目录。
- 验证：只运行 Node 夹具测试；不连接真实网关、不发送 agent 消息、不启动真实 watchdog。
- 状态：started。

## reported

- 最终提交 SHA 随任务板 reported 事件回报，避免 amend 后文档自引用失效。
- 实际改动：
  - 将 `accepted_pending_verify` 接入主循环的去重冷却定时器，验真后原位结算为 `verified` 或 `verify_failed`。
  - 同一时刻复用一个检查循环；`unknown` 只记录，不调用 `agent`。
  - 锁改为 `O_EXCL` 原子创建；活 PID 拒绝第二实例，陈旧或损坏锁改名留证后接管。
  - 恢复限流按每成员最近 30 分钟的全部 activation 记录计数；验证异常也结算失败并参与阻断。
  - 临时客户端配置移除未知 `auth.cooldowns` 且不写入 token；token 仅通过 OpenClaw 支持的 `OPENCLAW_GATEWAY_TOKEN` 子进程环境变量传递，不进入 argv。
  - 恢复验真只认本轮 `[任务标识][RESUME]` 用户消息之后的助手或工具活动。
- 测试：`node --test scripts/agent_bridge/liveness/liveness.test.mjs`，13 passed、0 failed、0 skipped。
- 静态检查：所有 liveness `.mjs` 均通过 `node --check`；`git diff --check` 通过。
- 未执行：未连接真实网关、未发送真实 `agent` 消息、未启动 watchdog、未运行生产命令。
- 遗留：需要统筹独立审查并决定是否部署；本执行方不标记 reviewed。
