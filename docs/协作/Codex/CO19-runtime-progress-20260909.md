# CO19 运行恢复阶段

助手只完成源码部分并reported。统筹独立检查5415bef54b66a982895fd179411dfcb210e70956，17/17通过、0失败/跳过；天龙恢复消息及幂等键对应工资13，旧数据图14已退出映射。

整体任务尚缺部署运行证据，因此先changes_requested再由统筹started完成余项，不把助手源码报告直接当整个任务已完成。

运行意图：git archive提取该精确SHA到artifacts/coordination-r3/runtime/watchdog-5415bef54b66；保留共享旧脚本、旧锁和日志。先确认没有已在运行的watchdog，只启动单个隐藏Node辅助进程。使用已有liveness证据目录及新runtime内不含凭据的客户端临时配置。核实锁PID、至少两轮只读探测、正常时不发送agent消息；若失败只处理这个辅助进程，不动网关或餐饮生产。该运行不是系统服务安装，不承诺电脑关机后仍运行。
