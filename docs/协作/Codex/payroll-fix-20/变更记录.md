# CO-PAYROLL-MIGRATION-FIX-20 变更记录

## started

- 执行人：Codex 助手；基线：`9dc186c2b11d111dd2206405ecdb5e4253dad8aa`。
- 意图：只修 `month_salary` 已有错误列定义时重复 ADD、同名 `idx_month_salary_payout` 错误结构重建、`idx_payout_month` 唯一性漏检三项。
- 测试：补三类异常夹具；只使用本机隔离 MySQL 13317，schema 全部以 `co_payroll20` 开头并保留。
- 边界：不碰生产、法务、天龙工作树、其他业务规则或他人 schema；不修改原天龙任务事件。
- 状态：started。

## reported 前验证结果

- 迁移修复：七个 `month_salary` 目标列先区分“缺失”和“已存在但定义错误”；缺失时 `ADD`，错误时在阶段 0 完成类型、长度、精度及 `payout_id` 有符号范围预检后 `MODIFY`。
- 索引修复：`idx_month_salary_payout` 会删除同名错误列序或错误唯一性后重建为非唯一 `(payout_id)`；`idx_payout_month` 同时核对列序和 `NON_UNIQUE=1`。
- 异常夹具：8 类反例共 16 个断言，最终 `PASS=16 FAIL=0`。新增三类分别覆盖七个既有错误列原位修正且历史值保留、错误同名 `idx_month_salary_payout`、列序正确但错误为唯一索引的 `idx_payout_month`。
- 额外安全边界：保留 schema `co_payroll20_unsigned_1788889097_5343` 验证超出有符号 BIGINT 的 `payout_id` 会在首条 DDL 前失败；迁移退出 1，原列仍为 `bigint unsigned`，新增列数 0，原值保持不变。
- 三态演练：全新迁移、同库第二/第三次幂等执行、旧候选表自愈均退出 0；`态1=态2`、`态1=态3` 两组结构比较一致，共 6/6 检查通过。保留 schema 为 `co_payroll20_s1_1788889233_9612` 与 `co_payroll20_s3_1788889236_2597`。
- 静态校验：`git diff --check` 通过；`anomaly_test.sh` 与 `verify_migration.sh` 的 `bash -n` 为 2/2 通过。
- Java 25 项未运行：`PayrollMysqlIntegrationTest.java:44` 硬编码创建 `payroll_it_*`，违反本任务“所有 schema 必须以 `co_payroll20` 开头”的隔离边界；该文件不在允许修改范围内，因此没有越权改写或执行。后续由统筹另开任务把该测试 schema 参数化或改为合规前缀，再运行 `mvn -B -q test -Dtest=PayrollMysqlIntegrationTest`。
- MySQL 13317 未停止、未重启；未删除任何 schema。当前保留的 34 个 `co_payroll20_*` schema 完整清单见 `retained-schemas.txt`，包含前次并发受扰运行及最终验证证据。
- 未触碰法务、生产、他人 schema、天龙工作树或原天龙任务事件。

## changes_requested 返工意图

- 统筹复现 `DATETIME(6)` 向 `DATETIME` 转换时微秒被截断或四舍五入；本轮只在阶段 0 增加 `approved_at`、`paid_at` 非零微秒拒绝，并补一组零 DDL、原定义和原值保持的合成反例。
- 按统筹明确授权，顺序复跑异常脚本与单一 `PayrollMysqlIntegrationTest` 进程；该轮允许测试自行创建并保留 `payroll_it_*` 时间戳 schema，不访问或删除既有库。

## changes_requested 返工结果

- 阶段 0 已增加 `approved_at`、`paid_at` 的 `MICROSECOND(...)<>0` 检查；非零微秒在任何 DDL 前明确失败。
- 新增反例 9：两个字段均为 `DATETIME(6)`，合成值分别为 `10:20:30.123456` 与 `10:20:30.654321`。异常脚本最终 `PASS=19 FAIL=0`，返工三项断言为 3/3：迁移非零退出、其他目标列新增数 0、两个原定义和原值不变。
- `PayrollMysqlIntegrationTest` 首次完整顺序运行：Tests run 25, Failures 0, Errors 2, Skipped 0；错误均为 MySQL 13317 执行 metadata 查询时返回 Out of memory。
- 首次错误的两个方法随后用一个进程定点复跑：2/2 通过。
- 第二次完整顺序运行：Tests run 25, Failures 0, Errors 5, Skipped 0；先出现 MySQL Out of memory，随后连接被重置并拒绝，说明共享 13317 在测试中退出。未再重复运行。
- 全程没有停止、重启、刷新、修改 MySQL 配置或删除任何 schema；发现 13317 退出后交由统筹恢复。所有本轮创建的 `co_payroll20_*` 与获授权的 `payroll_it_*` 测试库均保留。
- 静态检查待提交前复核；法务、生产和业务 Java 均未修改。
