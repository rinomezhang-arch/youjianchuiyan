# TR-AUTH-LOGOUT-WIRE-R2-35 技术验收

结论：PASS，任务可标记 reviewed。

验收提交：`6e60f9fc2b6dbcafb9c45cc0b5495211496c953e`；远端分支已核对为同一提交。相对固定基线只新增退出链测试和证据，没有修改业务源码、后端、数据库、配置、部署或法务。

真实浏览器证据闭合了任务合同：头像下拉真实点击退出项后出现自定义 `.modal-overlay`；取消后 token 和当前路由保持且无 logout 请求；确认后调用现有 logout、清除本地身份并进入 `/login`；服务端 logout 返回失败时仍完成本地退出；退出后后退和直达受保护路由均回到登录页。

原失败根因是旧探针只识别 `.el-message-box`，没有识别当前自定义确认弹层。新增 DOM 回归通过渲染后的头像、菜单项、取消和确认按钮触发公开事件链，没有直接调用 `handleCommand` 或 `confirmLogout`。提交证据记录 tests/auth 48 PASS、0 FAIL、0 SKIP，前端构建通过；无应用缺陷，因此业务源码零改动是正确处置。

后续应单独修正旧鉴权矩阵探针，防止再次把自定义弹层误判为“无确认框”；不在本任务越界修改。
