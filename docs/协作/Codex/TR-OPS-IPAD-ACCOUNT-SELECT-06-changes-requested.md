# TR-OPS-IPAD-ACCOUNT-SELECT-06 技术验收

## 结论

CHANGES_REQUESTED。

账户白名单、必须选择账户、普通及组合支付透传 `account_id`、失败保留幂等键、成功换键以及旧浏览器 30/30 证据均可保留。只修以下四项。

1. 候选未推送。任务板报告没有精确提交，远端也不存在 `codex/trae-ipad-account-select-06`；本地候选为 `f361cd5bc5cf33e5823e3d20aa2b512162b71145`。R2 必须推送功能分支并在报告写精确修复提交与远端最终 HEAD。
2. 基线不成立。任务卡基线 `4b7b17c9` 与候选的合并基点是 `09a15bbb`，候选相对卡片跨 20 笔其他任务历史；当前统筹分支的 PaySelect 还有后续变化。请从 `59bdeace8eb34104740bab400ecd03c45365ef76` 建干净 R2 工作树，只移植 `f361cd5b` 的三个产品和专属测试文件，解决冲突时保留统筹分支现有逻辑，不携带旧依赖历史。
3. `PaySelect.vue:174-191,329` 的 `loadAccounts` 没有请求代次守卫。切店时旧店请求若晚于新店请求返回，会覆盖新店账户并允许误选。增加递增序号或等价取消机制，只有最新一次加载能写入 accounts、state 和 error；补一个“旧请求晚到、新店账户不被覆盖”的专属反例。
4. `seed-tr06.sql:23-24` 提交了明文测试口令，`result.json` 保存四个完整幂等 UUID。夹具口令改为运行时环境变量或运行时生成，禁止进入提交和日志；证据只保留 `key_present`、长度及 `same_key=true`，不得保留原值。

只复跑 06 专属组件测试和一次前端构建；原 30/30 真实浏览器证据保留，不重复真实链。报告写 PASS/FAIL/NOT_COVERED，产品差异只允许任务卡三文件，证据和报告只在 06 专属目录。禁止后端、生产、配置、法务及其他页面变化。
