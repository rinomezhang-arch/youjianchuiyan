# TL-MARKETING-INQUIRY-API-55 R1 技术验收

结论：CHANGES_REQUESTED。

提交 `7db5d3b286b47c4b401b7b328a05b8c4909a210a` 已完成来源码反查门店、咨询与归因同事务、并发唯一键、稳定咨询号和手机号双因子回查；现有 11/11 专属证据可保留。还剩两个生产语义缺口：

1. `MarketingInquiryService.java:90-97` 接受并落盘 `remark`，但 `:122-152` 的幂等重放只比较姓名、手机号、日期、人数和部分事件字段。同一个 requestId 改备注会被当作相同请求并返回原回执，违背“相同载荷重放、不同载荷 409”。同时事件回查没有核对 `business_type=booking_inquiry` 与 `business_no=INQ{id}`，不足以证明回查的是同一类业务结果。请把备注和这两个固定业务语义纳入完整比对，并补同 requestId 改备注的 409、零新增定向反例。
2. `BookingInquiryController.java:128-134` 把回查中的所有异常都吞成 `success(null)`。查无、电话不符和非法输入应统一为空，但数据库断连、SQL/结构错误属于系统故障，不能伪装成“没有这条咨询”。请只对服务已返回的空结果响应 `success(null)`，让非业务异常进入现有 5xx 处理，并补一个服务异常不返回成功空结果的定向控制器断言。

只修改上述两个点和 55 专属测试、报告；只跑 55 专属测试，不重跑迁移整套，不扩前端、审批、转预订、配置、生产或法务。

