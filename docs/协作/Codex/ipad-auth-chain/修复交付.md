# IPAD-AUTH-CHAIN-02 实际交付

2026-09-07 Codex子代理。6个业务/测试文件待父精确审阅，未stage/commit：

1. banquet_project/src/main/java/com/youjian/banquet/config/IpadInterceptor.java：设备独立入口与客人菜单精确许可；不再将空绑定员工头提升；共享设备login/verify/batch不依赖旧员工头。
2. banquet_project/src/main/java/com/youjian/banquet/controller/IpadOrderController.java：仅新增专用service注入与verify/batch身份代码；原厨房/订单锁/菜品写入算法未动。
3. banquet_project/src/main/java/com/youjian/banquet/service/IpadBatchAuthorizationService.java：专用一次性票据，设备/店/员工/订单/用途/120秒绑定，容量控制，原子消费、同店在职复核。
4. frontend_v3/src/views/ipad/order/GuestOrder.vue：verify携订单、batch携票据而非自报员工、重复点击门控、清密码/票据、未知结果提示核对。
5. banquet_project/src/test/java/com/youjian/banquet/controller/IpadBatchAuthorizationTest.java：11项新合法/失败回归。
6. banquet_project/src/test/java/com/youjian/banquet/controller/IpadAuthChainIsolationTest.java：原6项从漏洞存在刻画改为拒绝与无副作用，注入真实新service；原源码CHAIN01-original-test.java.snapshot和原maven-isolation.log保留。

api/ipad.js透传data可直接承载新契约，未修改。契约详情见修复契约.md（含之前未运行阶段记录，实际最新状态以本文为准）。

## 实测

integration原始命令：

```powershell
mvn -f banquet_project/pom.xml "-Dtest=IpadAuthChainIsolationTest,IpadBatchAuthorizationTest" test
node --test docs/协作/Codex/ipad-auth-chain/guest-auth.test.mjs
```

maven-auth2.log：22:56:30结束，17测试/0失败/0错误/0跳过，BUILD SUCCESS、exit0，Maven已释放。node-auth2-final.log：4/4通过、exit0。diff --check通过。

Java验证真实MockMvc拦截器/控制器、BCrypt密码检查、真实专用票据及设备/员工查询；换班实际更新独立库设备staff绑定。其余订单/JPA及通知是出口替身，断言拒绝零业务访问及合法请求真实controller的save/通知，不声称订单全事务E2E。17例各独立schema保留，标识在日志；无真实员工数据。

合法：空绑定设备真实店员授权加菜；绑定设备由另一同店员工当次授权；真实login换班；客人菜单不需员工头；同票据并发2请求结果200/403且只save一次。

拒绝：空绑定伪造头员工；body自报员工；缺票据；错密码；他店员工；他店订单；设备/订单/店范围错；篡改票据；120秒边界过期；员工离职；票据重复；真实PC JWT拦截器拒此不透明票据。全部身份失败路径不写订单/通知。

## 所有非测试调用方核查

从integration项目范围检索`ipadAuthVerify|/auth/verify|order/add-dishes`，覆盖vue/js/ts/html/java并排除node_modules/dist/target/测试/个人证据。结果仅frontend_v3/src/api/ipad.js定义两个API、GuestOrder.vue唯一调用verify并提交batch、后端路由和拦截器。本轮已更新唯一页面；未发现其他生产页面沿旧verify参数。另读views/ipad/login/Login.vue:103仍调用原ipadLogin(phone,password)，参数无变化。此结论限当前检出源码，不含独立旧版本部署/外部调用者。

## 已知限制与未改范围

- 票据进程内且一次性：跨节点/重启失效，消费后业务失败也需再授权。不可宣称恢复幂等；未知提交应先核对订单。发布前确认单实例/路由，未改共享缓存或配置。
- 已有绑定员工的其他API仍依赖设备记录与员工头匹配；SN可复制，不是完整iPad强会话鉴权。本轮仅修空绑定身份提升和当次批量授权，不改变经理/退款角色制度。
- 前端现有order/detail调用没有对应控制器映射，原订单查看缺口未修；浏览菜单已验证但不宣称整个GuestOrder订单流程已打通。
- 旧设备迁移FK引用id与捕获结构不符，沿CHAIN01夹具显式适配；员工fixture新增最小认证列，不能当完整元数据迁移验收。原生产迁移未改。
- 初次独立javac路径编码及依赖读取失败已保留诊断，最终以本次Maven17项成功为准。
- 未部署/未启动全应用/未改应付冻结文件、Kitchen方法、盘点、财报、法务或全局权限配置。

下一步父审以上6文件并整合；应付浏览器v2先按同目录应付v2浏览器隔离联调方案.md准备，需实际隔离应用运行阶段另明确排期。
