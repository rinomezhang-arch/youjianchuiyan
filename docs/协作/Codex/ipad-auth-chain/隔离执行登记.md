# IPAD-AUTH-CHAIN-01 隔离复现登记

2026-09-07 Codex子代理，基线e9a7f1e8。意图：仅新增banquet_project/src/test/java/com/youjian/banquet/controller/IpadAuthChainIsolationTest.java和本目录个人证据。真实IpadInterceptor+真实IpadOrderController经standalone MockMvc执行；真实本机隔离MySQL仅设备绑定表，读取原迁移，每例独立保留schema，无删除。订单/菜品仓储与通知为出口替身，不mock授权拦截器或授权方法，不手填request身份。测试不等同订单事务落库。先核无Maven进程，仅运行新类；不启动全应用，不修改应付4文件、业务/auth/config/法务/Kitchen文件。测试输出合成设备/员工/数据库名，不输出凭据。
追加夹具边界：原ipad_device_binding迁移外键目标为store_info(id)/staff_master(id)，与捕获结构不符。仅新测试中读取真实store_info及外部staff主键夹具，将设备DDL外键指向store_id/staff_id且staff_id改INT兼容原主键；不修改迁移文件。此为授权查询隔离夹具，不宣称原迁移可上线。

结果：新专属测试6项执行通过，maven-isolation.log exit0，22:44:26已释放Maven。真实拦截器/JDBC/控制器复现两项身份来源缺口；另证明batch缺body员工仍成功及三个设备绑定负例拒绝。全部合成schema保留，业务持久化为出口替身不算E2E；原迁移外键不匹配仅测试适配，未修业务。详见隔离复现交付.md。等待父代理审阅，未stage/commit。
