# IPAD-AUTH-CHAIN-02 意图与最小方案

2026-09-07 Codex子代理。授权路径：IpadInterceptor.java；IpadOrderController.java仅verify/batch身份部分；GuestOrder.vue授权提交；新service/IpadBatchAuthorizationService.java、新test/controller/IpadBatchAuthorizationTest.java、本目录专属证据。必要ipad.js契约注释。保留CHAIN01原证据与测试不改。

核实：IpadAuthController.login实际验证同店在职员工密码后更新设备绑定staff_id，未签发通用token。现拦截器绑定员工比对会阻止以0/新员工头换班。GuestOrder先verify后body.staff_id加菜，verify未发凭据。

方案：精确允许同店active设备访问POST login/verify/batch和客人菜单GET（不是全部GET）；这些入口不提升头员工，login真实密码核验仍保留。其他业务无绑定员工拒绝；有绑定仍原比对，不改退款/经理制度。verify同店员工密码成功且订单存在可操作后，新专属服务发32字节随机不透明一次性票据，绑定设备/店/员工/订单/用途，120秒有效；仅内存保存，不是JWT、不共享PC/法务密钥，不改配置/数据库。batch先验证并原子消费票据及复核员工仍在职，再进入原订单锁/加菜逻辑，拒绝body.staff_id。服务重启/不同实例票据失效需重新授权，明确限制；容量上限与过期回收，失败关闭。当前部署能力需父核实多实例路由，不能声称跨节点可用。

前端verify携booking_id，随后batch仅携authorization_token不传staff_id；重复点击门控，授权结束清密码/票据，网络未知不自动重发。全应用不启动；仅专属测试。旧迁移FK不在本轮范围。

阶段结果：已写候选5个Java/前端文件（拦截器、控制器verify/batch、新service、新test、GuestOrder），未Maven。父Kitchen运行期间冻结Java。前端专属Node4通过；首次测试路径错误日志保留，修复后node-auth2-final.log exit0。授权契约见修复契约.md。无修改Kitchen方法/应付文件/全局配置。

可编译检查点：使用现有Surefire classpath，仅javac --release 17 -proc:none编译3个变更业务类及专属新测试，输出个人isolated-classes，JAVAC_EXIT=0。javac-auth2.args为精确参数，javac-auth2.log保留原始诊断。包装Python打印诊断时遇GBK UnicodeEncodeError退出1，不是javac编译失败；后续核查4个主class均存在。不写target，不Maven，不代表Java运行测试通过。此刻Java与前端候选冻结，待父57回归结束再安排auth运行。

更正紧前检查点：首次javac返回0但参数文件中的中文输出路径被本机默认编码误读，目标目录没有class，不能按首次退出码认定预期产物到位。改用证据目录作为cwd、ASCII相对输出路径isolated-classes重新编译；见javac-auth2-ascii.args/log。本轮exit=1，核到class数量=0。未删除首次文件。

父57回归完成释放。先保存CHAIN01-original-test.java.snapshot，原maven-isolation.log保留不覆盖；按授权更新原6项拒绝断言并给旧装配注入真实票据service，随后仅跑两专属类。非测试调用方扫描已执行，结果另存证据。

完成：原6拒绝回归+新11合法/失败=17全过，maven-auth2.log exit0，22:56:30释放Maven。前端4通过。非测试verify唯一GuestOrder调用已同步；6文件清单及边界见修复交付.md。旧快照日志保留。新增应付v2浏览器隔离联调方案，尚未启动服务。未stage/commit，等待父精确审阅。
阶段意图：父授权PAYABLE-BROWSER-01，从75b067b新建专属worktree F:/solo/artifacts/team-worktrees/payable-browser，分支codex/payable-browser-20260907。新树专属harness/test/个人证据，真实嵌入Servlet白名单，无业务mock/手填身份；独立保留schema，随机本地端口。integration auth6冻结。本条先登记工作树创建意图，新树后续登记实际环境。
