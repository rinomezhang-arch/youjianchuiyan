# TL-OPS-PAYROLL-MIGRATION-CANONICAL-13 第二轮技术验收退回

- 状态：changes_requested，不可合并、不可部署。
- 候选：`7a0d726051666d58b29f064eebadce6fa91b9889`。
- 通过：全新库、同库重复执行、指定旧空表三态通过；`PayrollMysqlIntegrationTest` 25通过、0失败、0错误、0跳过；旧V文件以Git重命名保留为 `.sql.retired`；动态SQL无外部输入。
- 阻断：独立异常结构反例0/5通过。同名错误复合外键被当作正确外键；错误 store 外键可能令脚本中途失败；带历史数据的字段收窄失败并留下部分DDL；已有错误列类型与同名错误索引不自愈；提交内三态证据文本仍写有“不一致”，与报告冲突；任务远端分支仍未指向本候选。
- 整改：按列完整定义、索引列序/唯一性、外键子父列序及更新删除规则逐项比对；任何不可安全收窄的数据先做只读预检并整体失败，避免开始DDL后才失败；补历史数据和错误结构反例；重新生成一致证据；最终提交推到任务声明分支。
- 执行边界：只允许隔离 MySQL `127.0.0.1:13317`。禁止 SSH、sudo、systemctl、nginx、生产3306、生产服务、部署、法务及真实工资明细。

