# 本轮执行入口与证据复用

Codex统筹。Trae负责候选合并、构建、真实联调以及经精确候选验收后的发布执行；天地龙做任务板指定独立任务。用户亲自监管的Claude法务线不属于本轮范围。

先读各自任务JSON，任务板claim/event是状态事实源。使用 F:/solo/scripts/backup/cos_taskboard.py claim --actor trae --task TR-RELEASE-RC-15；事件使用 event --actor trae --task TR-RELEASE-RC-15 --type started 或 reported --file 报告文件。天龙改为自己的actor和任务ID。不要使用poll --task假定只查单任务，该实现会读全板。

Trae先结束并提交TR-AUTH-SHELL-14，再领取TR-RELEASE-RC-15。已有前端工具版本为Vite5.4.21、Vitest2.1.9，沿用已有node_modules，先检查版本再决定是否确需安装。工资候选9dc186c2先独立复核；其他输入以任务中给定工作树和对应任务最新reported的精确SHA为准，不复制未提交文件。新候选分支独立试验，最终只有reviewed补丁可以上线。

缓存索引按“源码SHA或相关文件哈希、完整命令、依赖/运行时版本、测试库结构及fixture状态”记录结果与日志路径。同键证据可以引用；键变化时只重跑受影响检查。不要复述长日志，不重复调查已给定路径，不无目的循环调用。模型prompt前缀保持稳定，变量放末尾；能否命中服务端缓存取决于实际运行时，不能保证或虚报命中率。

合并后的真实集成回归、发布前现状比对和发布后验证必须真实执行。报告区分mock组件、真实HTTP、浏览器、数据库四类证据。HTTP200、空列表和编译成功不替代业务验收。

后台消息带任务ID和精确路径，一次投递后核验原会话或带消息ID回执；queued/accepted不能写成已送达。不用鼠标，不新开任务窗口，不轮番催同一活跃成员。问题与完成经已验证通道回复codex/current，额度预警时先交接和推进真实状态。

秋哥追加授权：天龙或地龙确实掉线时，使用其现有启动入口恢复协作进程。先分别验证真实网关和执行会话，CLI配置校验失败不等于离线，在线空闲不等于崩溃。恢复前查现存进程，避免重复启动；恢复后核验main原会话收信及回执。禁止借此重启餐饮生产、数据库或修改网关配置；不运行doctor --fix。此条是恢复授权与操作规则，实际自动监管是否运行必须另有运行证据，不能只写本条就声称已常驻。

秋哥进一步指定：上述恢复和拉起后假死激活由Trae承担，任务TR-COLLAB-LIVENESS-17。Codex接收摘要、验收效果，不重复做同一通信诊断。

天地龙允许参加生产操作，不能按成员身份一律禁止。生产动作必须另有具体任务，锁定目标、允许写入范围、脚本或制品哈希、备份、回退、前后验收；Codex审核，执行端全程留痕。出现与预期不符、并发发布或法务交叉立即停止回报。既有数据映射/协作恢复任务的只读或禁生产范围继续适用于这些任务；这不是对天地龙永久禁止生产。涉及系统瘫痪风险仍由秋哥裁决，法务边界不变。
