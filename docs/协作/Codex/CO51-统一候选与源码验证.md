# 统一候选源码与验证

本提交增加餐饮会话实时复核两类和单测，并修复应收收款后账户余额没有增加的问题。
原事务中只对首次收款原子累加对应门店有效账户余额；幂等重放不重复更新，
账户停用、跨店、余额异常或更新失败回滚收款、应收和账户。
旧失败账户保留，没有回补历史金额。

会话复核使用现员工档案，停用、调店、角色变更后旧JWT要求重新登录。
独立注册在原JWT/角色/设备拦截器之后，法务路径与律师既有访问继续由原拦截器处理。
这两类不是替代或修改法务共享鉴权类。

## 已执行证据

- CO46单测：18/0/0/0；真实登录、HTTP、DB：64通过/0失败，31次请求。
- CO47真实Spring事务/MySQL测试：9通过/0失败，含并发累加和失败回滚。
- CO47实际登录HTTP：17断言通过/0失败，8次请求，收40再60余额100、重放仍100、
  停用和跨店拒绝且三张资金表没有新增或金额变化。
- CO43盘点实际登录HTTP/DB：41通过/0失败，主明细持久化、精度、刷新和跨店拒绝。
  该盘点代码此前已在候选中，不在本提交重复改写。

原始结果分别保留在 F:/solo/artifacts/release-candidates/co43-stocktake-20260909、
co46-restaurant-revocation-20260909、co47-account-balance-20260909。
CO47的MySQL测试是有隔离库及类来源约束的专项驱动，不纳入普通无数据库单测默认运行。
这些数字不能充当统一候选全部业务通过。

## 发布输入

实际统一候选：
F:/solo/artifacts/release-candidates/co51-converged-20260909/co51-converged-candidate.jar

SHA256: c7365d08a4ca0efb338e75b1d9668bdddcfb52cb20c1d3bee5056d8f3f07a59e

该JAR由当前生产d5b29714基底和精确源码白名单组装，324Java离线编译成功；
保留现生产7冻结类、131资源和101依赖。来源清单、复制前后SHA、
class覆盖计划和所有非白名单ZIP内容/压缩记录证明在同目录。

**不能直接拿本Git树中的旧共享法务源码或旧部署脚本覆盖生产。**
统一候选以生产d5b基底及manifest为准，工资真实联调和统一浏览器联调尚在继续。
未做生产DDL/DML、部署或重启；三个真实用户实际密码登录和实体打印仍未由本轮证明。
