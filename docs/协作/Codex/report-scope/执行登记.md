# REPORT-SCOPE 意图

2026-09-07 Codex子代理，基线6f8560bd。只修改ReportController、FinanceReportController、新ReportQueryScope、新ReportScopeHttpContractTest及本目录。统一门店查询边界：无身份/非GM缺门店403，非法参数400，非GM他店/all拒绝，GM空/all保持全门店。局部异常映射和脱敏，SQL定义不变。不改UserContext/JWT/global/法务、配置、生产；不stage/commit。

验证：standalone真实MockMvc加载两个控制器，mock Jdbc/Service并验证拒绝路径零调用，成功路径门店参数正确。此为HTTP控制器合同，不是认证链或业务E2E。仅跑ReportScopeHttpContractTest，日志留本目录；Maven编译输出是测试运行生成物。

结果：2026-09-07 21:51，`mvn -B -o -f banquet_project/pom.xml -Dtest=ReportScopeHttpContractTest test`通过，19项、0失败/错误/跳过，53.941秒。日志maven-first.log。全部8入口验证无身份（含残留全局scope标志）、非GM缺门店、非法上下文门店、非法参数0/负数/非数字/小数/溢出/加号、非GM他店/all拒绝，拒绝时Jdbc与Service零调用；合法本店参数绑定、GMall/空/未传仍允许。两业务异常路径返回HTTP500固定文字，不泄漏模拟SQL。

修改明细：新ReportQueryScope统一解析及400/403异常；ReportController范围解析移到业务try外，局部处理器保留状态，旧catch改为固定查询失败；FinanceReportController同用范围解析及局部处理器。对两个文件HEAD与工作副本SELECT/AND/GROUP BY/ORDER BY字符串序列逐项比较相同，未改SQL定义。未改全局身份、JWT、权限、法务或其他业务文件。

边界：沿用UserContext.isGeneralManager现有GM判定（包括storeId=0的既有全局约定），未在此任务重定义角色或修复上游身份构造。trim后空参数沿用默认范围；非GM未传参数仍限定自身合法门店。MockMvc直接设置合成UserContext，不验证JWT/切面/生产认证链，也不验证SQL业务结果。无数据库连接、无生产写入、无数据删除。

交付：仅授权四个Java文件及本目录，未stage/commit；待父代理独立复核合入。本子代理停止编辑，身份守护候选保持封存。
