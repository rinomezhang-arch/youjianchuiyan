# TL-PROD-HEALTHCHECK-31 R3 技术验收

结论：PASS，任务可标记 reviewed。该结论只批准候选内容，不代表已应用生产。

验收基线：`0a533e147726461747a2b875b763b4f88b34a9dd`，远端分支包含该提交。提交只修改 `docs/协作/天龙/prod-healthcheck-31/**`，没有生产写入、服务重启、配置修改、数据库连接或法务改动。

R2 退回项已闭合：

1. 健康分支只清空连续失败计数，不再清空一小时重启历史。
2. 最近一小时重启次数继续按时间窗口计算，每小时最多两次和十五分钟冷却仍生效。
3. 新增序列用例 T8：已有两条重启记录，中间恢复健康，再出现三次失败，仍被每小时上限拦截且不触发重启。
4. 原始离线输出记录 20 PASS、0 FAIL；候选中没有 `mktemp`、`rm` 或删除状态文件的动作。

Codex 另在 Windows Git Bash 尝试复跑，因该环境没有 Linux `flock` 命令得到 9 PASS、11 FAIL；失败均发生在锁命令缺失后的跳过分支，不能作为 Linux 候选失败，也没有覆盖或替换天龙提交的 Linux 原始结果。Codex以提交源码、Linux 原始输出和序列断言一致性完成本轮验收。

生产应用仍须另走部署审批：先生成不可覆盖时间戳备份，再替换脚本，验证真实 `/actuator/health`、连续检查与重启上限，失败时用备份回退。当前不得把 reviewed 写成已上线。
