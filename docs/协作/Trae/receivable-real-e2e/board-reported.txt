TR-OPS-RECEIVABLE-REAL-E2E-03 三次 reported（2026-09-14，按 Codex R2 changes_requested 窄修，未重跑 E2E、未改产品代码）

R2 退回属实并已修复：上一提交 f5601cf0 的 redactSecrets 仅定义未接入，call() 仍把原始 body/json 写入 log 并直接 REQ/RESP 打印，EVIDENCE_JSON 随之含口令/JWT。

本轮改动（3 个交付文件，均在 allowed_paths）：
1. api-probe-03.mjs：
   - 真实 HTTP fetch 继续发送原始值，联调能力不变；
   - 进入 log/REQ/RESP 前分别生成 redactSecrets 脱敏副本（safeBody/safeJson），只输出副本；EVIDENCE_JSON 只由已脱敏的 log 构成；
   - 口令必填环境变量检查移入 main（无默认、缺失 exit 2 且零网络）；main 运行门控（import.meta.url === argv[1]），被 import 时不执行；导出 redactSecrets。
2. redact.test.mjs（新增，零网络定向断言）：fetch 守护计数必须为 0；嵌套及数组内 password/passwd/token/authorization/jwt/secret 全部 [REDACTED]；普通业务字段（storeId、金额、业务单号 RVABCDEF0123456789、业务ID 1788852951448）原值保持；原始入参不被修改；脱敏输出不含任何明文密标。
3. e2e-results.md / reported.md 追加 R3 说明；业务证据与数字（API 17/17、浏览器 6/6、DB 守恒）不重跑、不变更；业务单号中形似数字未改动。

实测（仅三项，无网络、无 E2E）：
- node --check api-probe-03.mjs / redact.test.mjs：均 exit 0；
- node redact.test.mjs：PASS（敏感键全脱敏、业务字段原值、入参不变、networkCalls=0）；
- 无 E2E_LOGIN_PASSWORD 直跑探针：exit=2，输出仅 ABORT 行，无任何请求/RESP/EVIDENCE 输出。

任务路径模式扫描（只报文件与分类，不输出值）：JWT 字面量 0；未打码 password 值 0；Bearer 长 token 0。敏感键名命中分类：api-probe-03.mjs（键名集合/脱敏函数/请求头构造，值运行时获取不打印）、redact.test.mjs（断言键名与假密标）、contract/flow.test.mjs（请求体禁含敏感键、错误文案禁含 token/Bearer 的负向断言）、api-probe-03.log（仅 [REDACTED] 占位）、backend-e2e03.log（法务 COS 未配置属性名，无值）、download_cos.py/list_cos.py（运行时读 ~/.cos.conf，无硬编码，未跟踪未提交）、文档为说明文字。LEAK 模式总数=0。

生产、法务、后端源码、产品代码均未触碰。
