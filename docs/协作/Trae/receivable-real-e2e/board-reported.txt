TR-OPS-RECEIVABLE-REAL-E2E-03 二次 reported（2026-09-14，按 Codex changes_requested 仅做脱敏，业务证据已认可、不重跑）

本轮改动（均在 allowed_paths，2 个文件）：
1. scripts/trae-receivable-real-e2e/api-probe-03.mjs：登录口令改为必填环境变量 E2E_LOGIN_PASSWORD，无默认值；缺失即 exit 2 中止且不发任何请求；控制台 REQ/RESP 与 EVIDENCE_JSON 经 redactSecrets 统一打码（password/token/authorization/jwt/secret 键递归替换为 [REDACTED]），实际请求仍发送真实值不影响联调；node --check 通过、无环境变量中止路径已实测 exit 2。
2. scripts/trae-receivable-real-e2e/e2e-results.md：浏览器登录行 rino/口令 改为 rino/[REDACTED]；复现入口同步为环境变量方式，文档不再含任何口令值。

脱敏模式扫描（5 个任务路径，只报文件与分类，不含值）：
- JWT 字面量（eyJ 两段式）：全部 0 命中。
- Bearer：api-probe-03.mjs 1 处为请求头构造代码（值来自运行时 token，不打印）；flow.test.mjs 1 处为防泄漏负向断言（断言错误文案不含 token/Bearer/requestId）。
- password/secret/token 类：api-probe-03.mjs 均为环境变量名、打码函数与注释；api-probe-03.log 2 处均为 [REDACTED] 占位（REQ 与 EVIDENCE_JSON 各一）；backend-e2e03.log 2 处为法务 COS 未配置 WARN 的属性名，无值；reported.md 2 处为 JWT 描述文字；contract.test.mjs 2 处为请求体禁含 token/password/secret/authorization 的负向断言。
- 弱口令数字：任务路径 0 处真实口令；grep 命中的 board-reported.txt/reported.md/e2e-results.md 各 1 处均为业务单号 RVABCDEF0123456789 的子串，属业务 ID，未改动。
- 未跟踪的历史辅助文件 download_cos.py/list_cos.py 密钥均运行时从 ~/.cos.conf 读取、无硬编码值；codex_board.md 命中为历史扫描记录文字；三者非本任务交付物，未提交。
- 全路径未打码口令值扫描结果：0。

业务结论不变：API 探针 17/17、浏览器真实 JWT 六场景 6/6、DB 守恒闭合、零产品源码改动；提交基于上一 reported 提交 e2b93b21 追加。生产、法务、后端源码未触碰。
