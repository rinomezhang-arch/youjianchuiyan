# TL-PAYROLL-EVIDENCE-GUARDS-71 报告（prepared 与未执行 runtime 分列）

- 任务ID：TL-PAYROLL-EVIDENCE-GUARDS-71
- 基线 base_sha：b5f7cc34caa0d02a4c5e164bc143cc4c8be382d5
- 分支：codex/tianlong-payroll-evidence-guards-71
- 范围：只改两个既有脚本的闸门/流式脱敏/制品证据入口，加本地合成 fixture；未启服务、未连库、未跑 HTTP

## 一、改动文件清单
| 文件 | 说明 |
|---|---|
| scripts/tianlong-payroll-real-e2e-59/env-precheck.sh | 挂载解析器改为临时文件 + 精确异常捕获；JSON 错误/结构不符/空输入一律非 0 退出（fail-closed） |
| scripts/tianlong-payroll-real-e2e-59/run-http-e2e.sh | 新增 redact_stream 流式脱敏（启动日志先脱敏后落盘）；失败分支 tail 也过脱敏；新增 SHA/JAR sha256 记录入口 |
| scripts/tianlong-payroll-evidence-guards-71/fixture-reject-test.sh | 本地合成 fixture：抽取真实解析器与真实 redact_stream 做拒绝/脱敏断言 |
| scripts/tianlong-payroll-evidence-guards-71/verify-jar-sha256.sh | SHA 校验入口（一致 0 / 不一致 1 / 参数问题 2） |
| scripts/tianlong-payroll-evidence-guards-71/fixture-output.txt | fixture 原始输出 |
| docs/协作/天龙/payroll-evidence-guards-71/报告.md | 本报告 |

## 二、验收对应（1-5）
1. env-precheck.sh 挂载段：裸 `except: sys.exit(0)` 已删除。改为 `except (json.JSONDecodeError, ValueError): sys.exit(2)`，另空输入 exit 3、非数组 exit 2、含 bind 挂载 exit 1、匿名 volume exit 0；**不再 except 后 pass**，任何不确定一律非 0。
2. run-http-e2e.sh 启动段：原先 `> "$APP_LOG" 2>&1`（原始 stdout/stderr 先落盘）改为 `> >(redact_stream >> "$APP_LOG")`，日志**先脱敏再持久化**；失败分支 `tail -n 60 "$APP_LOG"` 也改为 `| redact_stream >>`。成功/失败/异常路径均覆盖。
3. 制品证据入口：新增 `构建输入SHA`（git rev-parse）、`运行文件sha256`（sha256sum JAR）、`runtime一致性核对: 未执行（prepared only）` 三项写入 SUMMARY；另提供 verify-jar-sha256.sh 作为可校验入口。**本卡未启动 JAR，不虚报 runtime 哈希绑定已验。**
4. 本地合成 fixture（无真实服务/账号/DB）：坏 JSON、空输入、非数组、含 bind 挂载全部被拒；匿名 volume 放行；三类 canary（JWT 样式、口令、JWT_SECRET 值）在脱敏输出中均不出现，正常诊断「Tomcat started on port 18081」保留。结果 **PASS=15 FAIL=0**（原始输出见 fixture-output.txt）。
5. 本卡未解决/未执行（不包装）：
   - 59 独占环境缺口（审批/付款无权反例、独占随机口令隔离环境）**仍未执行**，待统筹准备统一候选后再验；
   - HTTP 24 项**未复跑**；
   - JAR 未启动，runtime 哈希绑定**未验**（仅 prepared）。

## 三、额度
- unknown（bl usage summary 无 console token；复用记录，未查 key、未做控制台登录/付费探测）。

## 四、边界
- 未启停服务/容器、未建库连库、未灌 seed、未跑真实 HTTP；未碰生产/法务/网关；未启子代理、未 LLM 轮询。
- 一次 fixture 失败（null 用例期望写错）只定向修一次：确认解析器对 Mounts=null 采取 fail-closed 更合规，故修正 fixture 期望而非放宽解析器。
