# TL-PAYROLL-DENIAL-REGRESSION-76 报告（R2：green，reported）

- 任务ID：TL-PAYROLL-DENIAL-REGRESSION-76
- 基线 base_sha：6cf5e6d15fc908a159d4aa29b64d1e3bdb82a652
- 分支：codex/tianlong-payroll-denial-76
- 结果：**reported**（仅测试侧修复，未改产品代码）
- 零写入：未连库、未写表、未起服务、未跑真实 HTTP、未跑旧 6/24 矩阵

## 一、本轮修复（按统筹批准，仅测试侧）
`getResponse().getContentAsString()` -> `getResponse().getContentAsString(StandardCharsets.UTF_8)`（3 处调用点），
并新增 `import java.nio.charset.StandardCharsets;`。**未删除任何断言、未降标**：403 断言与服务零调用断言原样保留。

## 二、结果（原始证据 surefire-PayrollDeniedActionRegressionTest-r2.txt）
Tests run: **4**, Failures: **0**, Errors: 0, Skipped: 0 —— BUILD SUCCESS
- PASS `nonWhitelistedEmployeeCannotApproveAndServiceStaysUntouched`：非白名单员工 POST `/api/hr/payroll/approve`
  -> 真实 `ApprovalAuthorityInterceptor` 拒绝（403 + 拦截器文案），且 `PayrollService` 与 `JdbcTemplate` **零交互**。
- PASS `loggedInEmployeeWithoutPayrollPermissionCannotPayAndServiceStaysUntouched`：**已登录**合成员工（staffId=2、can_manage_hr=0）
  POST `/api/hr/payroll/pay` -> 403「无权访问薪酬数据」，`PayrollService` **零调用**。
- PASS `loggedInEmployeeWithoutPayrollPermissionCannotPayoutAndServiceStaysUntouched`：同 `/payout`。
- PASS `whitelistedApproverReachesControllerAndCallsApproveOnce`：白名单合成角色可达控制器并真实调用 `approve` 一次（正向对照）。

## 三、覆盖语义（分开陈述，不混为一谈）
1. `/approve` 的拒绝 = **审批批复权（张婧/张晓秋白名单拦截器）**。
2. `/pay`、`/payout` 的拒绝 = **Controller RBAC**（`checkPayrollAccess`，can_manage_hr≠1 -> 403）。
   `ApprovalAuthorityInterceptor` 的动作正则只认 `approve|reject`，**不覆盖 payment 路由**。

## 四、不宣称（不降标）
- 不宣称付款已纳入白名单；不宣称真实 JWT 全链或登录员工 RBAC 全矩阵（本轮为 MockMvc + mock `JdbcTemplate`，零数据库）。
- 未跑旧 6/24 矩阵；真实 MySQL 下 pay 的角色矩阵未验。
- 旧 TL71 保持停止，未改未跑。

## 五、额度与边界
- 额度 unknown（bl 无 console token，未查 key、未做付费探测）；未启子代理；未改生产/法务/配置。
