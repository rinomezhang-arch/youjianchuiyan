# TL-PAYROLL-SCRIPT-FIXTURE-79 R1 报告（补桩后聚焦重跑，仍 blocked）

- 任务ID：TL-PAYROLL-SCRIPT-FIXTURE-79；基线 f63634d03ec8502048c013ac68004a25e4b00d67
- 分支：codex/tianlong-payroll-script-fixture-79
- 本轮：按统筹批准做「一次补桩后聚焦重跑」；结果 **PASS=12 FAIL=5**，仍 **blocked**（已达本轮配额，按令不继续尝试）
- 零真实外部调用：全程 `PATH=<fixture bin>`；未真实调用 mysql/java/curl/docker/ss/netstat；未连网络/DB、未加载真实配置或凭证

## 一、本轮已解决（相对上一轮）
1. **原 precheck 离线 PASS**：`env-precheck.sh` 单独运行退出 0、打印 `PRECHECK_RESULT=PASS`；且**期间 java 未被调用**（无 `java-called-PRE` 标记）。
2. 桩升级为**按参数分派**：docker 按 `--format` 返回 image=mysql:8.0 / status=running / PortBindings 含 `127.0.0.1:13318` / Binds=null / Mounts=[{"Type":"volume","Name":"fixture-only","Destination":"/var/lib/mysql"}]；mysql 按 SQL 返回 3306 / /var/lib/mysql/ / 8.0.46 / SELECT 1=1 / SCHEMATA COUNT=0；**ss、netstat 均落桩为空**（不读真实监听）。
3. 漏桩检查 13/13 全部落在 fixture；未知 docker format 与未知 SQL 会**立即非零失败**（不是通用返回值）。
4. 入口与 precheck 均**整文件同哈希**调用（非片段抽取）；三场景均 `timeout 90`，无挂死；**A/B/C 持久证据与输出均不含 canary**。

## 二、仍 FAIL 的 5 项（同一根因）
- caseA/B/C：**未命中 Java 启动路径**（3 项）、caseB `app-startup.log` 空、caseC 未检出脱敏器故障。
- 现场证据（out-A/B/C.txt 相同）：
```
[INFO] 运行环境前置断言...
[PASS] 环境前置断言通过（机器/容器/端口/挂载/datadir/唯一性/端口空闲）
[INFO] 创建唯一 schema tlpay59_http_<ts> 并建表...
FIXTURE-UNKNOWN-SQL:
[FAIL] CREATE DATABASE 失败
```
- 根因：夹具的 mysql 桩目前只认 **SELECT 类**语句；入口在启动 java 之前还要执行 **CREATE DATABASE / 建表 DDL / 灌种子**等非 SELECT 语句，被严格分派判为「未知 SQL」并立即失败，于是流程再次停在 java 之前。

## 三、阻断依赖（请裁决二选一）
- **A**：授权我把该入口在 java 启动**之前**所需的非 SELECT 语句面（CREATE DATABASE、payroll-http-schema.sql 内 DDL、必要种子 DML）逐类列为「已知语句」并各给固定桩响应，再跑一次同三场景（仍为纯桩、不碰真库）。范围仅限「抵达 Java 启动点」所需，不含业务断言值。
- **B**：接受本卡当前覆盖并收口——已证「precheck 离线可 PASS + 13/13 落桩 + 未知输入立即失败 + 零真实调用 + 无 canary 泄漏」；java/脱敏器路径留待 A 或改派。
- 我已达本卡配额（一次聚焦运行 + 一次定向修复），**不再自行尝试**。

## 四、证据
`scripts/tianlong-payroll-script-fixture-79/artifacts-r1/`：precheck-out.txt、out-A/B/C.txt、env-precheck-http.txt、http-summary.txt、http-e2e-output.txt、http-request-log.txt、http-db-assertions.txt
