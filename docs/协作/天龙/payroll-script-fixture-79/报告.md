# TL-PAYROLL-SCRIPT-FIXTURE-79 报告（blocked：具体依赖）

- 任务ID：TL-PAYROLL-SCRIPT-FIXTURE-79
- 基线 base_sha：f63634d03ec8502048c013ac68004a25e4b00d67（提交信息：fix(payroll-scripts): 脱敏子进程失败改为哨兵文件感知，不再静默放过）
- 分支：codex/tianlong-payroll-script-fixture-79
- 收口状态：**blocked**（已用尽「一次聚焦运行 + 一次定向修复」配额，按要求报告具体依赖而非继续环境探索）
- 零真实外部调用：全程 `PATH=<fixture bin>`，未调用真实 mysql/java/curl/docker，未连网络、未读凭证、未碰真实 DB/服务/配置

## 一、做了什么（真实调用原入口，非片段抽取）
- 入口整文件复制后调用：`scripts/tianlong-payroll-real-e2e-59/run-http-e2e.sh`，**sha256 一致 `5cd8847852cd62e6bed8c87e68b2f2e2f61cfde9eb04e2d3e271cf7d3a06b729`**（与源文件逐字节相同，未用 awk 抽片段、未复制算法）。
- 专属临时 PATH 落桩 **11/11**，`command -v` 全部指向 fixture：hostname、docker、mysql、java、curl、openssl、sha256sum、nohup、python3、sed、sleep。
- 真实执行入口 **3 次**（Case A/B/C），每次 `timeout 90`，均快速退出、无挂死。
- 覆盖设计：A = java 启动失败；B = java stderr 含 JSON canary 密码 + JWT 样式串；C = sed 桩强制非零（模拟脱敏器故障）。

## 二、结果：PASS=8 FAIL=2
PASS：
1. 入口同哈希（整文件、非片段）
2. 漏桩检查 11/11
3. Case A 上游非零退出（rc=1）
4. Case B 上游非零退出（rc=1）
5. Case C 上游非零退出（rc=1）
6. A/B/C 持久证据与输出**均不含 canary**（`Canary-Json-Pw-9x`、`eyJ…CanarySignaturePart` 全无命中）
7. 无真实外部调用（PATH 证据）
8. 未使用真实环境配置

FAIL：
1. Case B：`app-startup.log` 空/不存在 → 脱敏落盘路径未被走到
2. Case C：未检出「脱敏器故障」→ 哨兵路径未被走到

## 三、根因（一次明确定位，非产品缺陷）
原入口第一条闸门是**环境前置断言**，桩返回的通用值不满足它，入口在**启动 java 之前**就以「环境前置断言未通过，中止（零写入）」退出：
```
[INFO] 运行环境前置断言...
[FAIL] 环境前置断言未通过，中止（零写入）
```
因此 java/脱敏器/哨兵三条路径在本次夹具下**不可达**；A/B/C 全部停在闸门，rc=1 来自闸门而非脱敏路径。

## 四、阻断依赖（请统筹裁决，我不继续探索）
要让 72 入口走到 java/脱敏/哨兵路径，夹具必须先把**环境前置断言**喂过，需要补以下桩响应（仍为纯桩，不碰真环境）：
1. `docker inspect <容器> --format` 系列：容器名/镜像 mysql:8.0/状态 running/端口映射 `127.0.0.1:13318→3306`/无宿主机 bind 挂载；
2. `mysql -N -e "SELECT @@port / @@datadir / @@version"` 返回 `3306` / `/var/lib/mysql/` / `8.0.46`；
3. `hostname` 需与断言期望一致（卡片允许夹具模拟，报告已声明**非真实机身验收**）。
- 说明：这属于**夹具覆盖面**问题（我完成了一次定向修复即修正自身 bug，未继续叠加尝试）；是否授权我按上述补桩再跑一次，或改派他人，请裁决。

## 五、未执行清单
- 未验证：java 真实启动失败路径、stderr JSON canary 是否被脱敏、脱敏器非零退出是否被哨兵捕获（三者均被前置断言挡住）。
- 未连真实 DB/容器/网络；未修改 72 产品脚本；未删除任何文件；未启子代理。
- 59/71 按令不返工。

## 六、证据
- `scripts/tianlong-payroll-script-fixture-79/stub-verify.sh`（夹具本体）
- `scripts/tianlong-payroll-script-fixture-79/artifacts/`：out-A/B/C.txt（三次真实执行输出）、env-precheck-http.txt、http-summary.txt、http-request-log.txt、http-db-assertions.txt
