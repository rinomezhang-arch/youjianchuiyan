# TL-MARKETING-DATA-CONTRACT-38 营销发布与客人来源码数据库契约候选（天龙）

- 对应 Codex 卡片：TL-MARKETING-DATA-CONTRACT-38
- 固定基线：c6794bb56dd440742da3b298ef7b6375cde609b4
- 分支：codex/tianlong-marketing-data-contract-38
- 采集时间：2026-09-13 CST
- 范围：数据库迁移候选 + 隔离库验证 + 精简报告；不改 Java/Vue/其它迁移/配置/部署/生产对象

## 一、交付物路径

| 类型 | 路径 |
|---|---|
| 迁移 SQL（唯一候选） | scripts/migrations/marketing_publication_v1.sql |
| 验证脚本（一次跑完） | scripts/tianlong-marketing-data-contract-38/verify.sh |
| 验证基座 SQL | scripts/tianlong-marketing-data-contract-38/base_schema.sql |
| 机器 JSON | scripts/tianlong-marketing-data-contract-38/machine_output.json |
| 原始精简输出 | scripts/tianlong-marketing-data-contract-38/raw_output.txt |

TL38 隔离 schema（本次创建并保留，仅连接 127.0.0.1:13317）：

- tl38_20260913_193956_main（首次迁移 + 重放 + 旧值保留 + 反例拒绝）
- tl38_20260913_193956_badcol / _baduk / _badfk（异常结构前置失败三组）

## 二、环境闸门（写入前实测）

- 主机：VM-0-14-ubuntu
- 容器：youjian-mysql-test-13317，镜像 mysql:8.0，状态 Up
- 端口映射：0.0.0.0:13317 -> 3306（宿主 13317，容器 3306）；无宿主目录挂载（Mounts=[]）
- 容器内：@@port=3306，@@datadir=/var/lib/mysql/，版本 8.0.46
- 仅连接 127.0.0.1:13317；未连宿主 3306、未连生产库、未连 Windows 13317
- 凭据：未在任何文件写入口令/连接串/环境变量值（隔离库 root 空密码属既有环境事实，未写入证据文本）

## 三、10 条验收逐条结论

1. 只读四类输入，不扫全仓：PASS。仅按需读设计文档第三节、banquet_full_schema.sql 第 1562 行起 marketing_activity 定义、create_booking_inquiry_v1.sql、payroll_approval_payout_v1.sql（同类迁移规范），使用定向 grep/sed，未全仓扫描。
2. 写入前核实机器/容器/端口映射/数据目录：PASS（gate-01/02/03）。端口 13317->3306、无宿主挂载、@@datadir=/var/lib/mysql/，均已实测留证。
3. 只建 TL38 前缀新 schema，不 DROP/DELETE/TRUNCATE/清理：PASS（schema-01）。迁移与验证脚本均无 DROP/DELETE/TRUNCATE，仅 CREATE/ALTER/INSERT。
4. marketing_activity 增列并移除 store_id 默认 1，旧活动只迁 draft：PASS（main-05/06/07）。新增 status/document_version/row_version/created_by/updated_by/submitted_at/approved_at；store_id 默认值已去除（COLUMN_DEFAULT 为 NULL）；2 条旧活动全部为 draft（非 draft/cancelled 计数=0），无 approved/published 伪造。
5. 不可变 marketing_publication 与 marketing_attribution_event；public_slug/source_code/request_id 唯一；复合键同店；索引覆盖：PASS（main-02 + SHOW CREATE TABLE + 反例）。唯一键 uk_publication_activity_version_channel / uk_publication_slug / uk_publication_source_code / uk_publication_request_id / uk_publication_id_store；索引 idx_publication_store_status_valid(store_id,status,valid_from,valid_to)；归因索引 idx_attribution_pub_type_occurred / idx_attribution_store_type_occurred / idx_attribution_business。发布/归因通过复合外键 (activity_id,store_id) 与 (publication_id,store_id) 强制同店。
6. booking_inquiry 增列并移除默认 1，带来源咨询同店关系：PASS（main-07）。新增可空 marketing_publication_id/source_code/source_channel（3 列）；store_id 默认去除；复合外键 fk_bi_marketing_publication(marketing_publication_id,store_id)->marketing_publication(publication_id,store_id)；三者全空时含 NULL 外键行不校验，兼容历史咨询。
7. 首次与重放成功，旧值不变，反例全拒绝：PASS（main-02/03/04 + neg-01..13）。首次迁移退出码 0、重放退出码 0；2 条旧活动 + 1 条旧咨询业务值逐字段一致（snap_before==snap_after）；13 组反例均被数据库拒绝。
8. 同名错误列/唯一键/外键在 DDL 前失败，零部分迁移：PASS（ab-badcol/baduk/badfk refuse + zeropartial）。三组异常结构均在阶段 0 预检触发哨兵 1146 报错整体拒绝，结构转储前后一致，证明无任何 DDL 落地。
9. 报告列出 PASS/FAIL/NOT_COVERED/INFO、提交 SQL/脚本/JSON/原始输出、提交 SHA 与远端核对：PASS（本文 + machine_output.json + 第九节）。
10. 不写口令/连接串/环境变量值：PASS。证据文件仅含主机/端口/容器事实与错误码脱敏原文，无口令与连接串凭据。

## 四、反例逐条错误码与脱敏原文（13 组全 PASS）

| 反例 | 错误码 | 约束 | 脱敏原文（关键行） |
|---|---|---|---|
| 同 public_slug 重复(发布) | 1062 | uk_publication_slug | Duplicate entry 'slug-a' for key 'marketing_publication.uk_publication_slug' |
| 同 source_code 重复(发布) | 1062 | uk_publication_source_code | Duplicate entry ... for key ... uk_publication_source_code |
| 同 request_id 重复(发布) | 1062 | uk_publication_request_id | Duplicate entry ... for key ... uk_publication_request_id |
| 同 request_id 重复(归因) | 1062 | uk_attribution_request_id | Duplicate entry ... for key ... uk_attribution_request_id |
| 孤儿发布(activity_id 不存在) | 1452 | fk_publication_activity | Cannot add or update a child row: a foreign key constraint fails ... fk_publication_activity ... REFERENCES marketing_activity(activity_id, store_id) |
| 孤儿归因(publication_id 不存在) | 1452 | fk_attribution_publication | Cannot add or update a child row ... fk_attribution_publication |
| 跨店发布(activity_id=1 但 store_id=2) | 1452 | fk_publication_activity(复合) | Cannot add or update a child row ... fk_publication_activity |
| 跨店归因(publication_id=1 但 store_id=2) | 1452 | fk_attribution_publication(复合) | Cannot add or update a child row ... fk_attribution_publication |
| 发布状态非法(bogus) | 3819 | chk_publication_status | Check constraint 'chk_publication_status' is violated. |
| 活动状态非法(bogus) | 3819 | chk_activity_status | Check constraint 'chk_activity_status' is violated. |
| 负结算金额(settlement amount<0) | 3819 | chk_attribution_amount | Check constraint 'chk_attribution_amount' is violated. |
| 结算事件缺失 amount | 3819 | chk_attribution_amount | Check constraint 'chk_attribution_amount' is violated. |
| 非结算事件携带 amount(view amount=100) | 3819 | chk_attribution_amount | Check constraint 'chk_attribution_amount' is violated. |

## 五、异常结构三组"零部分迁移"断言结果（全 PASS）

三组异常结构均在阶段 0 预检（纯只读 information_schema）触发哨兵 SELECT 报错 1146 整体拒绝，任何 DDL 均未执行。

| 组 | 异常结构 | 哨兵（脱敏） | 前置失败 | 结构前后一致 |
|---|---|---|---|---|
| badcol | status 同名错误列类型 varchar(50)（应为 varchar(24)） | __refuse_marketing_activity_column_definition_mismatch__ | 拒绝(1146) | 一致(零部分迁移) |
| baduk | uk_publication_slug 同名错误唯一键（非唯一） | __refuse_marketing_publication_structure_mismatch__ | 拒绝(1146) | 一致(零部分迁移) |
| badfk | fk_publication_activity 同名错误外键（指向 store_info） | __refuse_marketing_publication_structure_mismatch__ | 拒绝(1146) | 一致(零部分迁移) |

断言方式：迁移前后对 schema 做结构转储（表/列/索引/外键），diff 一致即证明无部分迁移。预检按列类型+可空+默认、唯一键列序+唯一性、外键子父列序+规则逐项比对，不只看对象名是否存在。

## 六、机器 JSON 与关键计数

- 路径：scripts/tianlong-marketing-data-contract-38/machine_output.json
- 汇总：PASS=29，FAIL=0，NOT_COVERED=3，INFO=2，检查项总数=34
- NOT_COVERED：H5 公开接口/页面/浏览器端到端验收、HTTP 接口契约、物理渠道投放/企微发送（均不在 DB 迁移范围）
- INFO：TL38 schema 清单、隔离库连接范围说明

## 七、证据文件清单（路径 + 字节数）

| 文件 | 字节 |
|---|---|
| scripts/migrations/marketing_publication_v1.sql | 20906 |
| scripts/tianlong-marketing-data-contract-38/verify.sh | 20541 |
| scripts/tianlong-marketing-data-contract-38/base_schema.sql | 2892 |
| scripts/tianlong-marketing-data-contract-38/machine_output.json | 5628 |
| scripts/tianlong-marketing-data-contract-38/raw_output.txt | 1132 |
| scripts/tianlong-marketing-data-contract-38/snap_before.txt | 365 |
| scripts/tianlong-marketing-data-contract-38/snap_after.txt | 365 |
| scripts/tianlong-marketing-data-contract-38/mig1.txt | 62 |
| scripts/tianlong-marketing-data-contract-38/mig2.txt | 62 |
| scripts/tianlong-marketing-data-contract-38/ab_badcol_before.txt | 2527 |
| scripts/tianlong-marketing-data-contract-38/ab_badcol_after.txt | 2527 |
| scripts/tianlong-marketing-data-contract-38/ab_badcol_mig.txt | 138 |
| scripts/tianlong-marketing-data-contract-38/ab_baduk_before.txt | 4348 |
| scripts/tianlong-marketing-data-contract-38/ab_baduk_after.txt | 4348 |
| scripts/tianlong-marketing-data-contract-38/ab_baduk_mig.txt | 132 |
| scripts/tianlong-marketing-data-contract-38/ab_badfk_before.txt | 4187 |
| scripts/tianlong-marketing-data-contract-38/ab_badfk_after.txt | 4187 |
| scripts/tianlong-marketing-data-contract-38/ab_badfk_mig.txt | 132 |

## 八、提交与远端核对

- 提交 SHA：92895c0cb4cd5269133f8b1c8e65b797de713eea
- 远端核对：git ls-remote origin codex/tianlong-marketing-data-contract-38 -> 92895c0cb4cd5269133f8b1c8e65b797de713eea（与本地 HEAD 一致）

## 九、未完成/不确定项（如实列出，不包装）

1. 未执行 H5 公开接口/页面/浏览器的端到端验收，未执行真实渠道投放（NOT_COVERED）。
2. 迁移为 marketing_activity.status 增加 CHECK 约束（chk_activity_status），属设计文档"唯一权威状态"的数据库侧加固，超出卡片第 4 条最小列清单，但方向一致；如需严格最小化可移除该约束。
3. valid_from/valid_to 使用 TIMESTAMP（对齐设计文档"带时区"表述）；published_at/paused_at 使用 DATETIME。两者语义差异已在实现中显式区分。
4. booking_inquiry 复合外键含可空列，MySQL 对含 NULL 的外键行不校验，历史咨询（三者全空）因此兼容；带来源咨询仅当 marketing_publication_id 非空时强制同店。
5. 旧活动统一迁为 draft（未按 is_active 区分 cancelled），符合"只能迁 draft 或 cancelled"下限，未伪造 cancelled 语义。
6. 未连接生产库、未改业务源码/配置、未部署、未碰法务、未动 COS 任务板（父代理负责上板）。
