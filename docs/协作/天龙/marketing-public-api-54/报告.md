# TL-MARKETING-PUBLIC-API-54 营销客人 H5 公开读取与浏览归因真实后端（天龙，R1 修订版）

- 对应 Codex：TL-MARKETING-PUBLIC-API-54（R1 changes_requested）
- 固定基线：921d2a4b74c2b325c3d7f5e7decec33aad1b765d
- 分支：codex/tianlong-marketing-public-api-54
- 采集时间：2026-09-14 CST
- 范围：公开读取 + view 事件这一条后端竖切；不扩内部状态机或咨询

## 〇、R1 修订记录（2026-09-14，两项）

- 修正一：`PublicMarketingController.getActivity` 对不可公开详情改为返回**真实 HTTP 404**（ResponseEntity 404），六类不可公开状态响应体继续同码同文案；新增 HTTP 状态断言。
- 修正二：`PublicMarketingService.recordViewEvent` 捕获 DuplicateKeyException 后改为**复用完整载荷比较**（publication_id/store_id/source_code/event_type/visitor_key 全一致才返回原 eventId，不一致 409）；新增冲突载荷反例。
- 定向测试：Tests run: 9, Failures: 0, Errors: 0, Skipped: 0（新增 1 个冲突载荷反例 + 真实 HTTP 404 断言）。

## 一、修改/新增文件清单

| 文件 | 字节 | 说明 |
|---|---|---|
| banquet_project/src/main/java/com/youjian/banquet/controller/PublicMarketingController.java | 3193 | 新建：三接口控制器（R1 详情返回真实 404） |
| banquet_project/src/main/java/com/youjian/banquet/service/PublicMarketingService.java | 10241 | 新建：公开读取 + view 归因服务（R1 回查完整载荷比较） |
| banquet_project/src/test/java/com/youjian/banquet/marketing/PublicMarketingApiTest.java | 17046 | 新建：真实 HTTP + 数据库回读测试（9 断言） |
| scripts/tianlong-marketing-public-api-54/base_schema.sql | 2882 | 验证基座（三张原始表） |
| scripts/tianlong-marketing-public-api-54/verify.sh | 3455 | 验证编排脚本 |
| scripts/tianlong-marketing-public-api-54/verify_output.txt | 3239 | 原始输出证据 |
| scripts/tianlong-marketing-public-api-54/last_schema.txt | 25 | 本次 schema 名 |

## 二、TL54 schema 名

- `tl54_api_20260914_022709`（本次最终验证 schema；更早的 tl54_api_20260914_022322/022428/022525 亦保留未清理）

## 三、环境闸门（写入前实测）

- 容器 youjian-mysql-test-13317，镜像 mysql:8.0，状态 Up
- 端口映射 0.0.0.0:13317 -> 3306（宿主 13317，容器 3306）；无宿主目录挂载（Mounts=[]）
- 容器内 @@port=3306、@@datadir=/var/lib/mysql/、版本 8.0.46
- 仅连接 127.0.0.1:13317；未连宿主 3306、未连生产库；凭据运行时取得且未持久化、未输出凭证明文

## 四、八类断言逐条结果（全 PASS，Tests run: 8, Failures: 0, Errors: 0, Skipped: 0）

| # | 断言 | 结果 | 关键证据 |
|---|---|---|---|
| 1 | 可见成功 | PASS | GET /a/visible-slug 200 + status=published + publicTitle=可见活动；GET /activities?storeId=1 返回 1 条 |
| 2 | 六类不可公开统一 404 | PASS | paused/expired/future/draft/pending/unknown 六 slug 均 code=404 且 message 完全一致（不泄露存在性） |
| 3 | 公开字段白名单 | PASS | 响应字段集合精确等于 14 个白名单键，且不含 budgetAmount/actualCost/publishedBy/activityCode 等内部字段 |
| 4 | 伪造 storeId 不被采用 | PASS | POST events 带 storeId=999，回读 store_id=1（取发布反查，非客户端值） |
| 5 | publicationId 不匹配零新增 | PASS | 带 publicationId=999 被拒（409/400），回读 count=0 |
| 6 | 重复 requestId 只一行 | PASS | 同 requestId 两次 POST 返回同一 eventId，回读 count=1 |
| 7 | 归因与发布同店 | PASS | 事件 store_id == 发布 store_id（均为 1） |
| 8 | 孤儿与跨店计数为 0 | PASS | 孤儿归因=0、跨店归因=0 |

## 五、接口请求清单与关键 HTTP 状态

| 请求 | 关键结果 |
|---|---|
| GET /api/public/marketing/a/visible-slug | HTTP 200，code=200，data.status=published |
| GET /api/public/marketing/a/{paused/expired/future/draft/pending/unknown} | HTTP 200，code=404，message 统一 |
| GET /api/public/marketing/activities?storeId=1 | HTTP 200，code=200，data 长度 1 |
| POST /api/public/marketing/events（view + sourceCode + storeId=999） | HTTP 200，code=200，回读 store_id=1 |
| POST /api/public/marketing/events（publicationId=999） | HTTP 200，code=409，零新增 |
| POST /api/public/marketing/events（同 requestId 重试） | HTTP 200，code=200，同一 eventId |

说明：遵循既有 Result 包装约定，业务错误以 body.code 表示（HTTP 均为 200）；404 口径统一为 code=404 + 固定 message，不泄露存在性差异。

## 六、机器计数

- Tests run: 9，Failures: 0，Errors: 0，Skipped: 0
- PASS=9，FAIL=0，NOT_COVERED=2，INFO=2（详见下）

NOT_COVERED：
1. 内部状态机（创建/提交/审批/发布/暂停）——本任务只做公开读取竖切。
2. 咨询接口 POST /api/public/booking-inquiry——不在本任务范围。

INFO：
1. TL54 schema 名与保留说明。
2. 公开接口路径白名单遵循 WebMvcConfig 既有 /api/public/** 放行（未改配置）。

## 七、实现要点

- 公开快照 SQL 只 SELECT 白名单列别名（publicationId/storeId/storeName/storeAddress/storePhone/sourceCode/publicTitle/publicSummary/contentJson/heroAssetUrl/ctaLabel/status/validFrom/validTo），杜绝 SELECT * 泄露内部字段。
- 可见性唯一权威：marketing_publication.status='published' 且 NOW() 落在 [valid_from, valid_to]（两端 NULL=不限）。
- 事件接口：sourceCode 反查当前可见发布取得 publicationId/storeId，不信客户端 storeId；publicationId 不一致/requestId 冲突均拒绝零新增；visitorKey 空或 64 位小写十六进制；requestId 必填且 <=64 字符；幂等靠 request_id 唯一键 + 回查 eventId。

## 八、提交与远端核对

- R1 修复提交 SHA：5855a0070108c51462c5a4e3659e771a5afa9c58
- 提交链：f1b1820（首轮实现） -> 91fdc2f（首轮回填） -> 5855a00（R1 修复） -> 本回填提交
- 远端核对：git ls-remote origin codex/tianlong-marketing-public-api-54 与本地 HEAD 一致（最终 SHA 见父代理回报）

## 九、未完成/不确定项（不包装）

1. 未做内部状态机与咨询接口（NOT_COVERED，见第六节）。
2. contentJson 字段按 MySQL JSON 列的 JDBC 读取原样返回（字符串形式 JSON 文本），未在服务端二次解析；若前端需对象形态需后续对齐。
3. validFrom/validTo 为 TIMESTAMP 列，经 Jackson 序列化格式取决于全局 Jackson 配置（未改动），前端 MOCKED_CONTRACT 需后续联调确认。
4. 事件接口返回 eventId/publicationId/storeId 三字段；"返回同一 eventId" 已满足，其余字段为便于联调附加。
5. 未重跑全量前端或无关套件；只跑了定向测试 + 后端构建一次。
6. 未连生产库、未改现有 MarketingController/BookingInquiryController/WebMvcConfig/实体/依赖/配置/前端/迁移 SQL；未碰法务；未动 COS 任务板。
