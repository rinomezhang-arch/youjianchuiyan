# 开发记录：天龙硬化测试误触生产服务

- 记录时间：2026-09-08 19:22（Asia/Shanghai）
- 执行人：天龙；发现与独立核验：Codex
- 关联候选：`e869c96`，生产硬化脚本，明确要求仅制作和合成测试、不得执行生产

## 已确认事实

天龙的合成测试遗漏 `sudo` 替身，故障注入场景调用了真实 `sudo systemctl restart banquet.service` 和 `sudo systemctl reload nginx`。系统日志确认 2026-09-08 19:18:37 至 19:20:07 之间，`banquet.service` 被停止并启动六次，Nginx 被 reload 六次。Codex 已发送紧急停止令，禁止继续脚本、测试、sudo、systemctl 和 Nginx 操作，只允许只读取证和写事故报告。

## 当前恢复状态

- `banquet.service`：active，当前启动时间 19:20:07。
- `nginx`：active；`sudo nginx -t` 通过。
- 本机健康、生产 HTTPS 健康、首页、公开门店接口：4/4 返回 HTTP 200。
- 当前运行 JAR SHA256：`18c2c01ed0016d819086fb783d0792482e99c5595630c150d566de05157cf4bf`，与法务窄部署后的新基线一致。
- `/home/ubuntu/.banquet_env.sh`：3451 字节、mtime 04:21:58、mode 600；无 `SERVER_ADDRESS`。
- `/etc/nginx/sites-enabled/default`：6139 字节、mtime 2026-09-06 00:45:56、mode 644；五个拟加安全头均不存在。
- 8080 仍监听全部接口，说明硬化未生效；此次核验未修改配置、数据或制品。

## 影响与未决

已确认发生六轮短暂服务中断。现有证据未发现配置或 JAR 漂移；尚待天龙提供命令级事故报告，交叉核对每次测试场景、退出码及是否存在其他副作用。`e869c96` 已拒绝，禁止生产使用。

## 下一步

1. 接收并核对天龙不可覆盖事故报告。
2. 新版脚本必须在完全隔离的容器或无 sudo 的模拟执行器中测试，禁止通过 PATH 替身模拟生产命令后在生产主机运行。
3. 在独立复核通过前，不再执行任何生产硬化。

