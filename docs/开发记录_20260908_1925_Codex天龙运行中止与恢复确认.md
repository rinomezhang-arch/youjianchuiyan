# 开发记录：天龙运行中止与恢复确认

- 记录时间：2026-09-08 19:25（Asia/Shanghai）
- 关联事故：`INCIDENT-TL-HARDENING-01`

两次网关消息只进入排队，未能打断天龙正在运行的测试回合。Codex 从已安装 OpenClaw 网关能力确认 `sessions.abort` 是具有 `operator.write` 权限的会话中止方法，随后对天龙标准会话键 `agent:main:main` 调用。网关返回 `ok=true`、`status=aborted` 并给出被中止运行 ID，未修改网关配置、未杀系统进程、未操作鼠标键盘。

中止后只读核验：生产主机无 `harden.sh`、`rollback.sh` 或硬化测试残留进程；`banquet.service` 与 Nginx 均 active；后端启动时间仍为 19:20:07，没有再次重启；本机与生产 HTTPS 健康接口均为 HTTP 200。

天龙保持停止状态，只允许整理不可覆盖事故报告。候选 `e869c96` 及其后续未审查修改均禁止生产执行。

