# 变更登记簿

## 2026-09-08 PROD-PAYMENT-9901-QUARANTINE

- 执行人/工具：Codex。
- 意图：为生产遗留收款 `payment_id=9901` 准备默认只预检的隔离与回滚工具；本任务不连接、不修改生产。
- 准备改动：新增专属隔离脚本、回滚脚本和隔离测试，所有输出脱敏。
- 原因：该记录缺少应收、预订和客户父记录，并被财务月趋势聚合计入 16000.00；生产数据处置属于秋哥最终决定的红线。
- 范围：仅 `scripts/production/`、`scripts/tests/`、`docs/协作/Codex/payment-9901-quarantine/` 和本登记簿。
- 验证：静态语法检查；隔离 MySQL 或等价事务测试覆盖默认预检、显式应用、断言失败回滚、隔离表前镜像、影响行数、回滚父引用保护与置空恢复。
- 下一步：实现后复跑测试，追加实际结果、提交号、通过/失败/跳过数字和未解决项。

### 结果事件

- 实际改动：新增默认预检的隔离脚本、严格/置空两模式回滚脚本、隔离MySQL测试和交付报告。
- 安全行为：只有 `APPLY=YES` 才可能写库；先核对固定主键与业务指纹、三个父记录、十类下游、16000.00聚合差额和完整列契约。数据移动采用事务，归档插入、哈希验证、原行删除任一步异常都会回滚；归档表和恢复表均不输出原行正文。
- 验证：Bash语法3/3通过；隔离MySQL测试33通过、0失败、0跳过；`git diff --check`通过。测试库与临时证据保留，未执行物理删除。
- 过程中失败：第一次回滚实测暴露JSON时间字段在 `NULLIF` 中被MySQL错误转成整数；已改为按 `JSON_TYPE` 分支解析。第二轮发现失败的存储过程会残留；已增加无论成功失败都清理临时过程，并补回归测试。
- 未执行：未连接生产，未创建生产垃圾桶表，未隔离或恢复任何生产记录，未触碰法务、配置和服务。
- 遗留边界：MySQL永久DDL会隐式提交，因此显式应用时先创建空的时间戳垃圾桶表，再在单独事务中执行前镜像写入与原行移出；事务失败可能留下空表，但不会留下半份归档或删除原行。
- 下一步：只在秋哥决定生产数据红线并复核执行窗口后，先运行默认预检；保存脱敏输出，再单独授权 `APPLY=YES`。
