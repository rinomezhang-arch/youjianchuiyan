# Codex 交付检查点：工资验收与候选收尾

时间：2026-09-09；执行人：Codex当值统筹。目标仍是餐饮生产交付。本轮未部署生产，未修改法务或律师权限。

## 意图与实际结果

- DL-IPAD-BATCH-AUTH-16已经reviewed。独立16/16授权与幂等测试、四个隔离库双表回读通过，候选ecf98b7a已被Trae合入8f0da145。
- CO-PAYROLL-MIGRATION-FIX-20已经reviewed。旧27d66303存在DATETIME(6)精度丢失，真实反例已保存并退回。最终ec58072c390837d3a5a6293c96132785fc13f9c6增加首条DDL前微秒拒绝。
- 最终精确工资SHA独立25通过/0失败/0错误/0跳过；异常脚本19/19；paid_at单字段微秒反例4/4，专属拒绝、零DDL、定义和值保留均通过。报告docs/协作/Codex/CO20-reviewed-20260909.md。
- Trae候选已实际合入工资两提交，HEAD=4c84cc60f326bd18184b896c431553065c87d13f。只证明合并，不能将旧SHA的HTTP/浏览器证据自动套给新SHA。

## 当前任务与未完成

1. TR-RELEASE-RC-15仍changes_requested。Trae现有未跟踪结果显示真实合成账号登录HTTP 29/29及iPad HTTP 18/18，但驱动、结果、发布/回退脚本需提交并核对源码、实际运行JAR与候选SHA。最终候选浏览器/真实数据链、生产源码逐文件对照、法务保全、回退演练尚未收口。合成角色通过不等于秋哥/张总/张律师三个实际账号通过，未实测不能冒称。
2. CL-VISUAL-RC-PACKAGE-22已started，Claude在独立claude-visual-rc22工作树将三个视觉提交合为干净候选可复现包。精确提交清单在任务JSON，禁止夹入318fb405旧巡检。候选已跟踪EllaChatWidget.vue；不得拿根工作区未跟踪版本补入。仅Claude持有当前单进程构建窗口。
3. CO-RC15-PRINT-23已started，原Lagrange助手接续地龙额度阻断且未开工的DL-RC15-PRINT-17，先准备一个打印场景及对应报表真实HTTP/数据库回读驱动。当前禁止启动Java/MySQL/浏览器，等统筹释放运行窗口；尚无该项测试通过证据。原地龙任务保持真实ready，不冒领不代报。
4. 两龙原会话近期实际返回额度错误，地龙还出现余额/鉴权错误；不能靠重启恢复。Tianlong/Dilong本机巡检状态已标blocked，未购买额度、未改其身份/配置。

## 环境阻断与恢复

- 本机物理内存曾仅0.43GB、虚拟可用1.45GB，隔离MySQL两次退出。助手两轮25项分别23通过/2错误、20通过/5错误，原始失败和同期摘要保留，不用定点补跑相加冒充完整通过。
- 已暂停本人巡检PID60924。只读发现多份Codex消息/CUA辅助进程约4.7GB，未证明为孤儿，未关闭它们或用户软件。
- 已复用原数据目录受限恢复本机127.0.0.1:13317隔离MySQL，实际服务PID63968、监护PID58780。启动参数限制缓存和连接、关闭performance_schema；只在进程参数中生效，不改系统服务/核心配置。最终工资测试用Maven256MB、测试JVM512MB单进程通过。
- 所有co_payroll20_*、payroll_it_*和iPad测试库保留；没有清空旧数据或重建生产。
- 巡检目前暂停，不是四协作者常驻监管正常。后续先确认资源，再决定恢复；两个额度阻断成员不要重复唤醒。

## 通信事实与接续入口

- Codex仍为统筹，使用codex/current；Trae/Claude走原有桥。桥发出或适配器ready不等于送达。
- Trae原生适配器本次返回empty_model_reply，但随后实际Git读回工资两次合入，故交接以代码事实证实。不要盲目重发同一合并命令。
- Claude与助手的claim/started均已从COS回读。构建和测试串行安排，避免再次耗尽本机内存。
- 工资证据位于artifacts/coordination-r3/reviews/payroll20-ec58072c-*；iPad证据见dl-ipad16-ecf98b7a-*。助手归档提交a3c4ffb61297930feabd0137cfcba35d61184c08，已区分原始文件和同期摘要。
- 下一步：收Claude干净视觉包，收助手PRINT23可运行驱动；Trae完成RC15证据提交和生产源码只读对照，再在最终唯一SHA串行整体复验。未经这些验收不能称生产可交付。
- 本轮额度查询接口两次未返回，不能用旧余额冒充当前；没有重置或购买。

本记录留本地同名副本，并以新增文件方式上传COS；同步结果须以实际字节读回回执确认，失败不得声称完成。
